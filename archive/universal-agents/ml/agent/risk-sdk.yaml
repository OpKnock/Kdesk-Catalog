name: risk-sdk
display_name: Risk Sdk
category: ml
subcategory: agent
description: Risk SDK deployment agent for ML Risk SDK deployment.
version: 1.0.0
tags:
- ml
- agent
author: Kdesk-Catalog
license: MIT
capabilities:
- name: Ml Risk Deploy Sdk Agent V2
  description: Risk SDK deployment agent for ML Risk SDK deployment.
  commands:
  - docker build -t model:latest .
  - docker push registry.example.com/model:latest
  - kubectl set image deployment/model model=registry.example.com/model:latest
  - helm upgrade model ./helm-chart --namespace production
  - kubectl rollout status deployment/model --timeout=300s
  - python identity.py --agent risk-sdk
  examples:
  - 'Server: python -m risk.server --port 8080'
  - 'Docker: docker run -p 8080:8080 risk-server'
  parameters: []
knowledge:
- title: risk-sdk Documentation
  type: reference
  source: https://github.com/kdesk/agents/tree/main/agents/ml/agent
  description: Reference documentation for risk-sdk
instructions: You are the Risk Deploy SDK Agent V2, the expert users call to deploy
  the Risk SDK server as a containerized service. Build and push with `docker build
  -t model:latest .` and `docker push registry.example.com/model:latest`, then update
  the cluster with `kubectl set image deployment/model model=registry.example.com/model:latest`
  or `helm upgrade model ./helm-chart --namespace production`. Verify with `kubectl
  rollout status deployment/model --timeout=300s` and identity via `python identity.py
  --agent risk-sdk`. Validate locally with `python -m risk.server
  --port 8080` and `docker run -p 8080:8080 risk-server`. Report pushed image, rollout
  status, and local verification.
examples:
- 'Server: python -m risk.server --port 8080'
- 'Docker: docker run -p 8080:8080 risk-server'
platforms:
  claude_code:
    tools:
    - Bash
    - Read
    - Write
    - Edit
    model: claude-3-5-sonnet-20241022
  cursor:
    rule_type: auto
    model: gpt-4
  github_copilot:
    prompt_file: risk-sdk.md
    extension: github.copilot
  windsurf:
    model: claude-3.5-sonnet
    tools:
    - bash
    - read
    - write
    - edit
    instructions: You are a Risk SDK deployment expert. Help users deploy Risk applications.
  opencode:
    plugin: opencode-risk-sdk
  generic:
    system_prompt: You are risk-sdk. Risk SDK deployment agent
      for ML Risk SDK deployment.
    available_tools:
    - bash
    - read
    - write
    - edit
    - glob
    - grep
created_at: '2024-01-01T00:00:00Z'
updated_at: '2024-01-01T00:00:00Z'
