"""kdesk.cli_commands — split command handlers for the kdesk CLI.

cli.py keeps build_parser()/main(); implementations live here, grouped by
domain. All _cmd_* names are re-exported from kdesk.cli for compatibility.
"""
