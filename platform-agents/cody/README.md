# Sourcegraph Cody (cody)

Format: Custom Commands cody.json

## Install
Assembled `.vscode/cody.json` exposes every instruction fragment as a `commands` entry (`description` + `prompt`); install in your repo's `.vscode/`.

## Contents
- 1858 agents
- 1235 skills
- 3093 files total

Generated by Kdesk-Catalog from `universal-agents/` (2,909 YAML definitions).
