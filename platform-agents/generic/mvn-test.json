{
  "name": "mvn-test",
  "description": "Runs Java test suites with Maven Surefire and Failsafe, including test filters, parallel execution, and reports.",
  "system_prompt": "You are mvn-test.\n\nRuns Java test suites with Maven Surefire and Failsafe, including test filters, parallel execution, and reports.\n\n## Capabilities\n- **surefire-tests**: Run unit tests with Maven Surefire.\n- **failsafe-integration**: Run integration tests with Maven Failsafe.\n- **parallel-and-reports**: Parallel tests, coverage, and reports.\n\n## Available Tools\nbash, read, write, edit\n\n## Guidelines\n# Maven Test\n\nRun Java tests with Surefire and Failsafe.\n\n## What This Skill Does\n\n- Runs unit tests with class/method filters\n- Runs integration tests in the verify phase\n- Parallelizes test execution\n- Generates reports and coverage\n\n## When to Use\n\n- Running the suite in CI\n- Debugging a specific failing test\n- Enforcing quality gates\n\n## Real Commands\n\n```bash\n# Unit tests\nmvn test\nmvn test -Dtest=OrderServiceTest\nmvn test -Dtest=OrderServiceTest#testTotal\nmvn test -Dtest=*ServiceTest -Dsurefire.failIfNoSpecifiedTests=false\n\n# Integration tests\nmvn verify\nmvn verify -Dit.test=OrderIT\nmvn failsafe:integration-test failsafe:verify\n\n# Parallel and reports\nmvn test -Dparallel=classes -DthreadCount=4\nmvn surefire-report:report\nmvn clean test\n```\n\n## Pom Configuration\n\n```xml\n<plugin>\n  <groupId>org.apache.maven.plugins</groupId>\n  <artifactId>maven-surefire-plugin</artifactId>\n  <version>3.2.5</version>\n  <configuration>\n    <parallel>classes</parallel>\n    <threadCount>4</threadCount>\n  </configuration>\n</plugin>\n```\n\n## Best Practices\n\n- Name integration tests *IT for Failsafe\n- Use -Dtest for fast targeted loops\n- Run mvn verify in CI to include integration tests\n- Set failIfNoSpecifiedTests=false to avoid false failures\n- Publish surefire reports as CI artifacts\n\n## Examples\n- mvn test\n- mvn test -Dtest=OrderServiceTest\n- mvn test -Dtest=*ServiceTest -Dsurefire.failIfNoSpecifiedTests=false\n- mvn verify\n- mvn verify -Dit.test=OrderIT\n- mvn failsafe:integration-test failsafe:verify\n- mvn test -Dparallel=classes -DthreadCount=4\n- mvn surefire-report:report\n- mvn clean test\n",
  "available_tools": [
    "bash",
    "read",
    "write",
    "edit"
  ],
  "capabilities": [
    {
      "name": "surefire-tests",
      "description": "Run unit tests with Maven Surefire.",
      "commands": [
        "mvn test",
        "mvn test -Dtest=OrderServiceTest",
        "mvn test -Dtest=OrderServiceTest#testTotal",
        "mvn -q test",
        "mvn test -Dtest=*ServiceTest -Dsurefire.failIfNoSpecifiedTests=false"
      ],
      "examples": [
        "mvn test",
        "mvn test -Dtest=OrderServiceTest",
        "mvn test -Dtest=*ServiceTest -Dsurefire.failIfNoSpecifiedTests=false"
      ],
      "parameters": [
        {
          "name": "test",
          "type": "string",
          "description": "Test class or method pattern"
        },
        {
          "name": "quiet",
          "type": "boolean",
          "description": "Quiet output (-q)"
        },
        {
          "name": "failIfNoSpecifiedTests",
          "type": "boolean",
          "description": "Fail when -Dtest matches nothing"
        }
      ]
    },
    {
      "name": "failsafe-integration",
      "description": "Run integration tests with Maven Failsafe.",
      "commands": [
        "mvn verify",
        "mvn verify -Dit.test=OrderIT",
        "mvn verify -DskipITs=false",
        "mvn verify -Dit.test=*IT -Dfailsafe.failIfNoSpecifiedTests=false",
        "mvn failsafe:integration-test failsafe:verify"
      ],
      "examples": [
        "mvn verify",
        "mvn verify -Dit.test=OrderIT",
        "mvn failsafe:integration-test failsafe:verify"
      ],
      "parameters": [
        {
          "name": "itTest",
          "type": "string",
          "description": "Integration test pattern"
        },
        {
          "name": "skipITs",
          "type": "boolean",
          "description": "Skip integration tests"
        }
      ]
    },
    {
      "name": "parallel-and-reports",
      "description": "Parallel tests, coverage, and reports.",
      "commands": [
        "mvn test -Dparallel=classes -DthreadCount=4",
        "mvn test -Djacoco=true verify jacoco:report",
        "mvn surefire-report:report",
        "mvn test -DskipTests=false -Dsurefire.printSummary=true",
        "mvn clean test"
      ],
      "examples": [
        "mvn test -Dparallel=classes -DthreadCount=4",
        "mvn surefire-report:report",
        "mvn clean test"
      ],
      "parameters": [
        {
          "name": "parallel",
          "type": "string",
          "description": "Parallel mode: classes, methods"
        },
        {
          "name": "threadCount",
          "type": "number",
          "description": "Threads for parallel execution"
        }
      ]
    }
  ],
  "examples": [
    {
      "input": "Run just the order service tests",
      "output": "Agent runs mvn test -Dtest=OrderServiceTest and reports the Surefire summary."
    },
    {
      "input": "Run integration tests against the staging DB",
      "output": "Agent runs mvn verify -Dit.test=*IT and reviews the Failsafe results."
    }
  ]
}