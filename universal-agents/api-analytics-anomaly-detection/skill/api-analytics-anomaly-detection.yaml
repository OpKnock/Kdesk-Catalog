name: api-analytics-anomaly-detection
version: 2.0.0
type: skill
category: sre
description: Anomaly detection for API traffic with machine learning - detect traffic anomalies using
  time-series models (Prophet), evaluate, and alert.
keywords:
- api-analytics
- metrics
platforms:
  claude_code:
    tools:
    - Bash
    - Read
    - Write
    - Edit
    - Glob
    - Grep
    model: inherit
  cursor:
    rule_type: auto
  github_copilot:
    prompt_file: api-analytics-anomaly-detection.md
    extension: github.copilot
  windsurf:
    model: inherit
    tools:
    - bash
    - read
    - write
    - edit
  opencode:
    plugin: opencode-api-analytics-anomaly-detection
  generic:
    system_prompt: 'You take the Api Analytics Anomaly Detection seat. You do it through anomaly-detection:
      Detect API traffic anomalies with time-series ML. The commands are real, e.g. `pip install prophet
      scikit-learn`. You run and verify the real CLI commands before you claim anything.'
    available_tools:
    - bash
    - read
    - write
    - edit
    - glob
    - grep
capabilities:
- name: anomaly-detection
  description: Detect API traffic anomalies with time-series ML
  commands:
  - pip install prophet scikit-learn
  - python -c "from prophet import Prophet; print('ok')"
  - python train_model.py --history metrics.csv --model traffic.model
  - python detect.py --model traffic.model --input metrics_live.json | jq '.anomalies[] | {timestamp,
    score}'
  - curl -s http://localhost:8080/api/analytics/anomalies?window=1h | jq '.count'
  examples:
  - python forecast.py --history metrics.csv --periods 48 | jq '.forecast[-1]'
  - curl -s http://localhost:8080/api/analytics/anomalies?metric=latency_p95&window=24h | jq '.[-1]'
  - python detect.py --model traffic.model --threshold 2.5 --input live.json | jq '.flagged | length'
  parameters:
  - name: model
    type: string
    description: Trained model file
  - name: threshold
    type: number
    description: Z-score / anomaly threshold
  - name: window
    type: string
    description: Lookback window for detection
prerequisites:
- prometheus
- grafana
- elasticsearch
tools:
- prometheus
- grafana
- elasticsearch
- node.js
- python
examples:
- input: Detect unusual traffic spikes
  output: Trains a Prophet model on history and flags points exceeding the z-score threshold.
- input: Evaluate the last 24h for anomalies
  output: Scores the window via the API and lists flagged timestamps with scores.
instructions: '# API Analytics (Anomaly Detection)


  ## What this skill does

  Detect anomalies in API traffic using ML: train Prophet models on request-volume and latency history,
  score live metrics, and surface anomalies for alerting.


  ## When to use

  - Detecting traffic spikes or drops automatically

  - Catching latency regressions early

  - Reducing noise in alerting


  ## Real commands

  ```bash

  # Install

  pip install prophet scikit-learn


  # Verify install

  python -c "from prophet import Prophet; print(''ok'')"


  # Train on historical metrics

  python train_model.py --history metrics.csv --model traffic.model


  # Forecast next 48 periods

  python forecast.py --history metrics.csv --periods 48 | jq ''.forecast[-1]''


  # Detect anomalies on live input

  python detect.py --model traffic.model --input metrics_live.json | jq ''.anomalies[] | {timestamp, score}''


  # Query the anomaly endpoint

  curl -s ''http://localhost:8080/api/analytics/anomalies?window=1h'' | jq ''.count''

  ```


  ## detect.py sketch

  ```python

  from prophet import Prophet

  import json, sys

  m = Prophet().load(sys.argv[1])

  # score residuals of new points vs forecast

  # flag points where |residual| > threshold * std

  ```


  ## Best practices

  - Retrain weekly; traffic seasonality shifts

  - Use multiplicative seasonality for growing APIs

  - Threshold on residual z-score, not raw values

  - Combine with rule-based alerts (e.g. 5xx ratio)


  ## Testing

  ```bash

  python detect.py --model traffic.model --input sample_live.json | jq ''.flagged | length''

  curl -s ''http://localhost:8080/api/analytics/anomalies?window=24h'' | jq ''.[-1]''

  ```'
display_name: Api Analytics Anomaly Detection
knowledge:
- title: Prophet Docs
  type: documentation
  source: https://facebook.github.io/prophet/docs/quick_start.html
  description: Forecasting and changepoint detection with Prophet
- title: scikit-learn IsolationForest
  type: documentation
  source: https://scikit-learn.org/stable/modules/outlier_detection.html
  description: Unsupervised outlier detection
