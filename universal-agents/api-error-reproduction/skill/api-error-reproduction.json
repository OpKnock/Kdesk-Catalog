{
  "name": "api-error-reproduction",
  "version": "2.0.0",
  "type": "skill",
  "category": "backend",
  "description": "Troubleshoots API error issues: reproduce failures, trace error flows, correlate with releases, and fix root causes.",
  "keywords": [
    "api-error-reproduction"
  ],
  "platforms": {
    "claude_code": {
      "tools": [
        "Bash",
        "Read",
        "Write",
        "Edit",
        "Glob",
        "Grep"
      ],
      "model": "inherit"
    },
    "cursor": {
      "rule_type": "auto"
    },
    "github_copilot": {
      "prompt_file": "api-error-reproduction.md",
      "extension": "github.copilot"
    },
    "windsurf": {
      "model": "inherit",
      "tools": [
        "bash",
        "read",
        "write",
        "edit"
      ]
    },
    "opencode": {
      "plugin": "opencode-api-error-reproduction"
    },
    "generic": {
      "system_prompt": "You work Api Error Reproduction hands-on. You do it through error-reproduction: Reproduce error conditions with curl and scenario payloads. The commands are real, e.g. `curl -s -X POST http://localhost:3000/api/users -H Content-Type: application/json -d...`. You verify each step you report and stay honest about what you have not run.",
      "available_tools": [
        "bash",
        "read",
        "write",
        "edit",
        "glob",
        "grep"
      ]
    }
  },
  "capabilities": [
    {
      "name": "error-reproduction",
      "description": "Reproduce error conditions with curl and scenario payloads",
      "commands": [
        "curl -s -X POST http://localhost:3000/api/users -H 'Content-Type: application/json' -d '{\"email\":\"bad\"}' -w '\\n%{http_code}'",
        "curl -s http://localhost:3000/api/users/999999 -w '\\n%{http_code}'",
        "curl -s -X PUT http://localhost:3000/api/users/1 -H 'Content-Type: application/json' -d '{\"email\":\"x@y.z\"}' -w '\\n%{http_code}'",
        "curl -s -X DELETE http://localhost:3000/api/users/1 -w '\\n%{http_code}'",
        "curl -s -H 'Authorization: Bearer invalid' http://localhost:3000/api/me -w '\\n%{http_code}'"
      ],
      "examples": [
        "curl -s -X POST http://localhost:3000/api/users -H 'Content-Type: application/json' -d '{\"email\":\"bad\"}' -w '\\n%{http_code}'",
        "curl -s http://localhost:3000/api/users/999999 -w '\\n%{http_code}'",
        "curl -s -H 'Authorization: Bearer invalid' http://localhost:3000/api/me -w '\\n%{http_code}'"
      ],
      "parameters": [
        {
          "name": "url",
          "type": "string",
          "description": "Endpoint to probe"
        },
        {
          "name": "method",
          "type": "string",
          "description": "HTTP method"
        }
      ]
    },
    {
      "name": "log-analysis",
      "description": "Correlate errors in logs and metrics to find root causes",
      "commands": [
        "node -e \"console.log('trace_id=abc123 err=DB_TIMEOUT op=users.list')\"",
        "curl -s http://localhost:3000/metrics | grep -E 'http_errors|http_error_total' | head",
        "kubectl logs -l app=api --tail=200 | grep -iE 'error|exception' | tail -20",
        "node -e \"const r=require('os');console.log('pid',process.pid,'load',r.loadavg()[0])\"",
        "curl -s http://localhost:3000/health -w '\\n%{http_code}'"
      ],
      "examples": [
        "kubectl logs -l app=api --tail=200 | grep -iE 'error|exception' | tail -20",
        "curl -s http://localhost:3000/metrics | grep -E 'http_errors|http_error_total' | head",
        "curl -s http://localhost:3000/health -w '\\n%{http_code}'"
      ],
      "parameters": [
        {
          "name": "pattern",
          "type": "string",
          "description": "Log filter pattern"
        },
        {
          "name": "lines",
          "type": "string",
          "description": "Number of log lines"
        }
      ]
    }
  ],
  "prerequisites": [
    "node.js",
    "python",
    "openapi"
  ],
  "tools": [
    "node.js",
    "python",
    "openapi",
    "postman"
  ],
  "examples": [
    {
      "input": "Investigate a 500 spike after the latest deploy",
      "output": "Reproduces failing requests, greps logs for the stack trace, and correlates the error rate with the release window"
    },
    {
      "input": "Fix inconsistent error bodies across endpoints",
      "output": "Reproduces each shape, unifies the middleware, and adds regression tests per status"
    }
  ],
  "instructions": "# API Error (Troubleshooting)\n\nDiagnoses and fixes error-handling problems in running APIs.\n\n## When to Use\n- Unexplained 5xx spikes\n- Inconsistent error bodies in production\n- Errors only appear after deploys\n\n## Real Commands\n\n```bash\n# Reproduce\ncurl -s -X POST http://localhost:3000/api/users -H 'Content-Type: application/json' -d '{\"email\":\"bad\"}' -w '\\n%{http_code}'\ncurl -s http://localhost:3000/api/users/999999 -w '\\n%{http_code}'\n\n# Logs\nkubectl logs -l app=api --tail=200 | grep -iE 'error|exception' | tail -20\n\n# Metrics\ncurl -s http://localhost:3000/metrics | grep -E 'http_errors|http_error_total' | head\n\n# Health\ncurl -s http://localhost:3000/health -w '\\n%{http_code}'\n```\n\n## Root Cause Checklist\n- Is the error body consistent? (format issue)\n- When did the spike start? (release correlation)\n- Is a dependency failing? (timeouts/upstream)\n\n## Testing\nTurn each reproduction into an automated regression test.\n\n## Best Practices\n- Add trace IDs to every error response\n- Alert on rate deltas, not raw counts",
  "display_name": "Api Error Reproduction",
  "knowledge": [
    {
      "title": "kubectl Logs",
      "type": "documentation",
      "source": "https://kubernetes.io/docs/reference/kubectl/generated/kubectl_logs/",
      "description": "Inspect service logs"
    },
    {
      "title": "Prometheus Metrics",
      "type": "documentation",
      "source": "https://prometheus.io/docs/instrumenting/writing_exporters/",
      "description": "Metric naming and labels"
    }
  ],
  "id": "api-error-reproduction",
  "skills": [],
  "inputs": {
    "parameters": [
      {
        "capability": "error-reproduction",
        "name": "url",
        "type": "string",
        "description": "Endpoint to probe"
      },
      {
        "capability": "error-reproduction",
        "name": "method",
        "type": "string",
        "description": "HTTP method"
      },
      {
        "capability": "log-analysis",
        "name": "pattern",
        "type": "string",
        "description": "Log filter pattern"
      },
      {
        "capability": "log-analysis",
        "name": "lines",
        "type": "string",
        "description": "Number of log lines"
      }
    ]
  },
  "outputs": {},
  "dependencies": [
    "node.js",
    "python",
    "openapi"
  ],
  "conversion": {
    "tool": "Kdesk-Catalog yaml-to-json",
    "schema": "definition-v1",
    "source_yaml": "api-error-reproduction/skill/api-error-reproduction.yaml",
    "derived": {
      "skills": "none",
      "tools": "tools",
      "inputs": "capability.parameters",
      "outputs": "none (not present in source YAML)",
      "dependencies": "prerequisites"
    }
  },
  "_provenance": {
    "generated_by": "Kdesk-Catalog yaml-to-json",
    "generator_version": "1.0.0",
    "schema": "definition-v1",
    "source": "universal-agents/api-error-reproduction/skill/api-error-reproduction.yaml",
    "checksum": "fcabb7cdb282befb0becc72e35142b888a1f84154d129c1d683c9665582c83ab"
  }
}
