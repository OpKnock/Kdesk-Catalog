{
  "name": "api-mock-engineer",
  "version": "2.0.0",
  "type": "skill",
  "category": "testing",
  "description": "Designs mock API services for frontend development and tests using MSW (Mock Service Worker), with OpenAPI-driven fixtures and realistic latency profiles.",
  "keywords": [
    "mock-server",
    "msw"
  ],
  "platforms": {
    "claude_code": {
      "tools": [
        "Bash",
        "Read",
        "Write",
        "Edit",
        "Glob",
        "Grep"
      ],
      "model": "inherit"
    },
    "cursor": {
      "rule_type": "auto"
    },
    "github_copilot": {
      "prompt_file": "api-mock-engineer.md",
      "extension": "github.copilot"
    },
    "windsurf": {
      "model": "inherit",
      "tools": [
        "bash",
        "read",
        "write",
        "edit"
      ]
    },
    "opencode": {
      "plugin": "opencode-api-mock-engineer"
    },
    "generic": {
      "system_prompt": "You own api-mock-engineer end to end. You do it through msw-handlers: Set up MSW request handlers for browser and Node test environments. The commands are real, e.g. `npm install msw --save-dev`. You work hands-on and only report outcomes you have observed.",
      "available_tools": [
        "bash",
        "read",
        "write",
        "edit",
        "glob",
        "grep"
      ]
    }
  },
  "capabilities": [
    {
      "name": "msw-handlers",
      "description": "Set up MSW request handlers for browser and Node test environments",
      "commands": [
        "npm install msw --save-dev",
        "npx msw init public/ --save",
        "npm install @mswjs/data",
        "node -e \"const { http, HttpResponse } = require('msw'); console.log(typeof http.get)\"",
        "curl -s http://localhost:3000/api/users -o /dev/null -w '%{http_code}\\n'"
      ],
      "examples": [
        "npx msw init public/ --save registers the service worker script",
        "const user = factory({ name: String, role: String }) builds a mock database",
        "http.get('/api/users', () => HttpResponse.json(db.user.getAll()))"
      ],
      "parameters": [
        {
          "name": "handlers-dir",
          "type": "string",
          "description": "Directory containing MSW request handler modules"
        },
        {
          "name": "mock-db",
          "type": "object",
          "description": "@mswjs/data model definitions for stateful mocking"
        },
        {
          "name": "delay",
          "type": "integer",
          "description": "Artificial latency in milliseconds for realistic UX"
        }
      ]
    },
    {
      "name": "worker-lifecycle",
      "description": "Start and stop the mock server in tests and development",
      "commands": [
        "npx msw init public/",
        "node test-setup.js",
        "npx vitest run",
        "curl -s http://localhost:3000/api/health | jq ."
      ],
      "examples": [],
      "parameters": []
    }
  ],
  "prerequisites": [
    "prism",
    "wiremock",
    "msw"
  ],
  "tools": [
    "prism",
    "wiremock",
    "msw",
    "node.js",
    "python",
    "openapi"
  ],
  "examples": [
    {
      "input": "Mock /api/users while backend is down",
      "output": "Creates MSW handlers backed by @mswjs/data and initializes the worker with npx msw init"
    },
    {
      "input": "Mock API in unit tests",
      "output": "Uses setupServer from msw/node in a vitest setup file so tests never hit the network"
    }
  ],
  "instructions": "# API Mock Engineer\n\nBuilds mock API layers with Mock Service Worker.\n\n## What This Skill Does\n- Intercepts fetch/XHR at the network level with service workers\n- Provides stateful mock databases with @mswjs/data\n- Reuses the same handlers in dev, CI, and Playwright tests\n\n## When to Use\n- Frontend work blocked by an unfinished backend\n- Removing flaky live-network calls from test suites\n- Demo environments needing deterministic data\n\n## Real Commands\n\n```bash\nnpm install msw --save-dev\nnpx msw init public/ --save\n```\n\n## Handler Example\n\n```js\nimport { http, HttpResponse, delay } from 'msw';\nexport const handlers = [\n  http.get('/api/users', async () => {\n    await delay(300);\n    return HttpResponse.json(db.user.getAll());\n  }),\n  http.post('/api/users', async ({ request }) => {\n    const body = await request.json();\n    return HttpResponse.json(db.user.create(body), { status: 201 });\n  })\n];\n```\n\n## Testing\n- Start the server with setupServer from msw/node in vitest/jest\n- Assert UI behavior without touching the network\n- Verify 404 handlers produce the same error shapes as the real API\n\n## Best Practices\n- Keep handlers close to route definitions for easy diffing against OpenAPI\n- Add delay() to surface loading states\n- Enable onUnhandledRequest warnings in tests to catch gaps",
  "display_name": "api-mock-engineer",
  "knowledge": [
    {
      "title": "MSW Documentation",
      "type": "documentation",
      "source": "https://mswjs.io/docs/",
      "description": "Mock Service Worker request interception for browsers and Node"
    },
    {
      "title": "@mswjs/data",
      "type": "documentation",
      "source": "https://github.com/mswjs/data",
      "description": "Data modeling library for stateful MSW mocks"
    }
  ],
  "id": "api-mock-engineer",
  "skills": [],
  "inputs": {
    "parameters": [
      {
        "capability": "msw-handlers",
        "name": "handlers-dir",
        "type": "string",
        "description": "Directory containing MSW request handler modules"
      },
      {
        "capability": "msw-handlers",
        "name": "mock-db",
        "type": "object",
        "description": "@mswjs/data model definitions for stateful mocking"
      },
      {
        "capability": "msw-handlers",
        "name": "delay",
        "type": "integer",
        "description": "Artificial latency in milliseconds for realistic UX"
      }
    ]
  },
  "outputs": {},
  "dependencies": [
    "prism",
    "wiremock",
    "msw"
  ],
  "conversion": {
    "tool": "Kdesk-Catalog yaml-to-json",
    "schema": "definition-v1",
    "source_yaml": "api-mock-engineer/skill/api-mock-engineer.yaml",
    "derived": {
      "skills": "none",
      "tools": "tools",
      "inputs": "capability.parameters",
      "outputs": "none (not present in source YAML)",
      "dependencies": "prerequisites"
    }
  },
  "_provenance": {
    "generated_by": "Kdesk-Catalog yaml-to-json",
    "generator_version": "1.0.0",
    "schema": "definition-v1",
    "source": "universal-agents/api-mock-engineer/skill/api-mock-engineer.yaml",
    "checksum": "c5e560b782dcd98520ddea2d0dfd2e577572c60256331996fd8e06c911bd1488"
  }
}
