{
  "name": "api-mock-json",
  "version": "2.0.0",
  "type": "skill",
  "category": "testing",
  "description": "Builds zero-code REST mock servers with JSON Server: watch mode, custom routes, filtering, pagination, and CRUD persistence for rapid prototyping.",
  "keywords": [
    "mock-server"
  ],
  "platforms": {
    "claude_code": {
      "tools": [
        "Bash",
        "Read",
        "Write",
        "Edit",
        "Glob",
        "Grep"
      ],
      "model": "inherit"
    },
    "cursor": {
      "rule_type": "auto"
    },
    "github_copilot": {
      "prompt_file": "api-mock-json.md",
      "extension": "github.copilot"
    },
    "windsurf": {
      "model": "inherit",
      "tools": [
        "bash",
        "read",
        "write",
        "edit"
      ]
    },
    "opencode": {
      "plugin": "opencode-api-mock-json"
    },
    "generic": {
      "system_prompt": "This is your domain: Api Mock Json. You do it through json-server: Serve a mock REST API from a JSON database file. The commands are real, e.g. `npm install -g json-server`. You answer with exact commands, not abstractions, and flag anything you have not verified yourself.",
      "available_tools": [
        "bash",
        "read",
        "write",
        "edit",
        "glob",
        "grep"
      ]
    }
  },
  "capabilities": [
    {
      "name": "json-server",
      "description": "Serve a mock REST API from a JSON database file",
      "commands": [
        "npm install -g json-server",
        "json-server --watch db.json --port 3001",
        "json-server db.json --routes routes.json --port 3001",
        "curl -s http://localhost:3001/users | jq 'length'",
        "curl -s -X POST http://localhost:3001/users -H 'Content-Type: application/json' -d '{\"name\":\"alice\"}'"
      ],
      "examples": [
        "json-server --watch db.json --port 3001 serves full REST endpoints",
        "curl -s 'http://localhost:3001/users?_page=2&_limit=5' exercises built-in pagination",
        "curl -s 'http://localhost:3001/users?role=admin' uses query filtering"
      ],
      "parameters": [
        {
          "name": "port",
          "type": "integer",
          "description": "Port for the mock server, default 3000"
        },
        {
          "name": "db-file",
          "type": "string",
          "description": "JSON file containing collections to serve"
        },
        {
          "name": "routes-file",
          "type": "string",
          "description": "Custom route mapping file (routes.json)"
        }
      ]
    },
    {
      "name": "route-customization",
      "description": "Map custom URLs and add middleware to the mock server",
      "commands": [
        "curl -s http://localhost:3001/users/1 | jq .",
        "curl -s -X PUT http://localhost:3001/users/1 -H 'Content-Type: application/json' -d '{\"name\":\"bob\"}'",
        "curl -s -X DELETE http://localhost:3001/users/1 -o /dev/null -w '%{http_code}\\n'"
      ],
      "examples": [],
      "parameters": []
    }
  ],
  "prerequisites": [
    "prism",
    "wiremock",
    "msw"
  ],
  "tools": [
    "prism",
    "wiremock",
    "msw",
    "node.js",
    "python"
  ],
  "examples": [
    {
      "input": "Prototype a user list UI fast",
      "output": "Starts json-server on port 3001 with a db.json of users and documents the query API"
    },
    {
      "input": "Simulate nested resources",
      "output": "Adds a routes.json mapping /api/v2/users to /users and restarts the server"
    }
  ],
  "instructions": "# API Mock v2 - JSON Server\n\nInstant REST mocks from a JSON file.\n\n## What This Skill Does\n- Serves CRUD endpoints for each collection in db.json\n- Provides filtering, pagination, and sorting via query params\n- Persists mutations back to the db file in watch mode\n\n## When to Use\n- Rapid UI prototyping against REST shapes\n- Quick contract demos before the real backend exists\n- Local frontend development with fake data\n\n## Real Commands\n\n```bash\nnpm install -g json-server\njson-server --watch db.json --port 3001\ncurl -s http://localhost:3001/users | jq 'length'\ncurl -s -X POST http://localhost:3001/users -H 'Content-Type: application/json' -d '{\"name\":\"alice\"}'\n```\n\n## db.json Example\n\n```json\n{\n  \"users\": [{ \"id\": 1, \"name\": \"alice\", \"role\": \"admin\" }],\n  \"orders\": []\n}\n```\n\nEndpoints: GET/POST /users, GET/PUT/PATCH/DELETE /users/:id.\n\n## Testing\n- Query filters: /users?role=admin\n- Pagination: /users?_page=2&_limit=5\n- Sorting: /users?_sort=name&_order=asc\n- Full-text: /users?q=ali\n\n## Best Practices\n- Commit db.json with seed data for reproducibility\n- Use routes.json to simulate nested or custom paths\n- Do not use JSON Server in production traffic",
  "display_name": "Api Mock Json",
  "knowledge": [
    {
      "title": "json-server GitHub",
      "type": "documentation",
      "source": "https://github.com/typicode/json-server",
      "description": "Zero-config REST mock server from a JSON file"
    },
    {
      "title": "jq Manual",
      "type": "documentation",
      "source": "https://jqlang.github.io/jq/manual/",
      "description": "Query and transform JSON from the command line"
    }
  ],
  "id": "api-mock-json",
  "skills": [],
  "inputs": {
    "parameters": [
      {
        "capability": "json-server",
        "name": "port",
        "type": "integer",
        "description": "Port for the mock server, default 3000"
      },
      {
        "capability": "json-server",
        "name": "db-file",
        "type": "string",
        "description": "JSON file containing collections to serve"
      },
      {
        "capability": "json-server",
        "name": "routes-file",
        "type": "string",
        "description": "Custom route mapping file (routes.json)"
      }
    ]
  },
  "outputs": {},
  "dependencies": [
    "prism",
    "wiremock",
    "msw"
  ],
  "conversion": {
    "tool": "Kdesk-Catalog yaml-to-json",
    "schema": "definition-v1",
    "source_yaml": "api-mock-json/skill/api-mock-json.yaml",
    "derived": {
      "skills": "none",
      "tools": "tools",
      "inputs": "capability.parameters",
      "outputs": "none (not present in source YAML)",
      "dependencies": "prerequisites"
    }
  },
  "_provenance": {
    "generated_by": "Kdesk-Catalog yaml-to-json",
    "generator_version": "1.0.0",
    "schema": "definition-v1",
    "source": "universal-agents/api-mock-json/skill/api-mock-json.yaml",
    "checksum": "c38af11a811fb565137bba13de0bc08f5edd0d9d3830f12d93503e023b6e5f3f"
  }
}
