{
  "name": "api-webhook-bullmq-queues",
  "version": "2.0.0",
  "type": "skill",
  "category": "backend",
  "description": "Builds reliable webhook delivery with queues: BullMQ workers, Redis persistence, dead-letter handling, and delivery monitoring.",
  "keywords": [
    "delivery"
  ],
  "platforms": {
    "claude_code": {
      "tools": [
        "Bash",
        "Read",
        "Write",
        "Edit",
        "Glob",
        "Grep"
      ],
      "model": "inherit"
    },
    "cursor": {
      "rule_type": "auto"
    },
    "github_copilot": {
      "prompt_file": "api-webhook-bullmq-queues.md",
      "extension": "github.copilot"
    },
    "windsurf": {
      "model": "inherit",
      "tools": [
        "bash",
        "read",
        "write",
        "edit"
      ]
    },
    "opencode": {
      "plugin": "opencode-api-webhook-bullmq-queues"
    },
    "generic": {
      "system_prompt": "You are the Api Webhook Bullmq Queues specialist. You do it through bullmq-queues: Queue webhook deliveries with BullMQ. The commands are real, e.g. `npm install bullmq ioredis`. You work hands-on and only report outcomes you have observed.",
      "available_tools": [
        "bash",
        "read",
        "write",
        "edit",
        "glob",
        "grep"
      ]
    }
  },
  "capabilities": [
    {
      "name": "bullmq-queues",
      "description": "Queue webhook deliveries with BullMQ",
      "commands": [
        "npm install bullmq ioredis",
        "node -e \"const {Queue}=require('bullmq'); const q=new Queue('webhook-delivery'); q.add('order.created',{id:1}).then(j=>console.log('job',j.id))\"",
        "node -e \"const {Queue}=require('bullmq'); const q=new Queue('webhook-delivery'); q.getJobCounts().then(console.log)\"",
        "node worker.js",
        "redis-cli LLEN bull:webhook-delivery:wait"
      ],
      "examples": [
        "Queue.add enqueues a delivery job",
        "getJobCounts reports queue state",
        "redis-cli LLEN inspects the waiting list"
      ],
      "parameters": [
        {
          "name": "queue-name",
          "type": "string",
          "description": "Queue name"
        },
        {
          "name": "job",
          "type": "string",
          "description": "Job type"
        },
        {
          "name": "payload",
          "type": "object",
          "description": "Job data"
        }
      ]
    },
    {
      "name": "delivery-monitoring",
      "description": "Monitor retries and dead letters",
      "commands": [
        "node -e \"const {Queue}=require('bullmq'); const q=new Queue('webhook-delivery'); q.getFailed().then(jobs=>console.log('failed',jobs.length))\"",
        "redis-cli ZCARD bull:webhook-delivery:failed",
        "npx bull-board",
        "node -e \"const {Queue}=require('bullmq'); const q=new Queue('webhook-delivery'); q.obliterate().then(()=>console.log('cleared'))\""
      ],
      "examples": [],
      "parameters": []
    }
  ],
  "prerequisites": [
    "node.js",
    "python",
    "ngrok",
    "redis"
  ],
  "tools": [
    "node.js",
    "python",
    "ngrok",
    "redis",
    "stripe-cli"
  ],
  "examples": [
    {
      "input": "Queue webhook deliveries",
      "output": "Adds BullMQ producers and workers with retry backoff"
    },
    {
      "input": "Handle permanently failed deliveries",
      "output": "Watches the failed set and alerts when it grows"
    }
  ],
  "instructions": "# API Webhook v4 - Reliability\n\nQueue-backed webhook delivery.\n\n## What This Skill Does\n- Enqueues deliveries as durable jobs\n- Retries with backoff in workers\n- Dead-letters persistent failures\n\n## When to Use\n- High-volume webhook workloads\n- At-least-once delivery needs\n- Surviving worker crashes\n\n## Real Commands\n\n```bash\nnpm install bullmq ioredis\nnode -e \"const {Queue}=require('bullmq'); const q=new Queue('webhook-delivery'); q.add('order.created',{id:1}).then(j=>console.log('job',j.id))\"\nredis-cli LLEN bull:webhook-delivery:wait\n```\n\n## Worker Example\n\n```js\nconst { Worker } = require('bullmq');\nnew Worker('webhook-delivery', async (job) => {\n  await fetch(job.data.url, { method: 'POST', body: JSON.stringify(job.data) });\n}, { concurrency: 10 });\n```\n\n## Testing\n- Kill workers mid-queue and verify recovery\n- Simulate endpoint failures into dead letters\n- Monitor queue depth during bursts\n\n\n## Best Practices\n- Set job TTLs and max attempts\n- Keep payloads small; reference data\n- Alert on dead-letter growth",
  "display_name": "Api Webhook Bullmq Queues",
  "knowledge": [
    {
      "title": "BullMQ Docs",
      "type": "documentation",
      "source": "https://docs.bullmq.io/",
      "description": "Redis-backed job queues"
    },
    {
      "title": "Redis Docs",
      "type": "documentation",
      "source": "https://redis.io/docs/latest/",
      "description": "Redis data structures"
    }
  ],
  "id": "api-webhook-bullmq-queues",
  "skills": [],
  "inputs": {
    "parameters": [
      {
        "capability": "bullmq-queues",
        "name": "queue-name",
        "type": "string",
        "description": "Queue name"
      },
      {
        "capability": "bullmq-queues",
        "name": "job",
        "type": "string",
        "description": "Job type"
      },
      {
        "capability": "bullmq-queues",
        "name": "payload",
        "type": "object",
        "description": "Job data"
      }
    ]
  },
  "outputs": {},
  "dependencies": [
    "node.js",
    "python",
    "ngrok",
    "redis"
  ],
  "conversion": {
    "tool": "Kdesk-Catalog yaml-to-json",
    "schema": "definition-v1",
    "source_yaml": "api-webhook-bullmq-queues/skill/api-webhook-bullmq-queues.yaml",
    "derived": {
      "skills": "none",
      "tools": "tools",
      "inputs": "capability.parameters",
      "outputs": "none (not present in source YAML)",
      "dependencies": "prerequisites"
    }
  },
  "_provenance": {
    "generated_by": "Kdesk-Catalog yaml-to-json",
    "generator_version": "1.0.0",
    "schema": "definition-v1",
    "source": "universal-agents/api-webhook-bullmq-queues/skill/api-webhook-bullmq-queues.yaml",
    "checksum": "d9eb857b02d22ac65da59a4e48f24920949391148657daf5846822495f1d6acd"
  }
}
