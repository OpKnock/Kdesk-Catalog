{
  "name": "chaos-testing",
  "display_name": "Chaos Testing",
  "category": "api",
  "subcategory": "general",
  "description": "Chaos test APIs by injecting latency, errors, and network faults with Toxiproxy, then load-testing resilience with vegeta and hey.",
  "version": "2.0.0",
  "tags": [
    "api"
  ],
  "author": "Kdesk-Catalog",
  "license": "MIT",
  "prerequisites": [
    "hey",
    "toxiproxy-cli",
    "vegeta"
  ],
  "capabilities": [
    {
      "name": "fault-injection",
      "description": "Inject latency, bandwith, and error faults on a proxy between client and API using toxiproxy-cli",
      "commands": [
        "toxiproxy-cli create api -l localhost:8081 -u localhost:8080",
        "toxiproxy-cli toxic add -t latency -a latency=500 -a jitter=100 api",
        "toxiproxy-cli toxic add -t bandwidth -a rate=10 api",
        "toxiproxy-cli toxic add -t disconnect -a toxicity=1 api"
      ],
      "examples": [
        "toxiproxy-cli create api -l localhost:8081 -u localhost:8080",
        "toxiproxy-cli toxic add -t latency -a latency=500 -a jitter=100 api",
        "toxiproxy-cli toxic remove -t latency api"
      ],
      "parameters": [
        {
          "name": "toxin_type",
          "type": "string",
          "description": "latency, bandwidth, disconnect, timeout, or reset_peer"
        },
        {
          "name": "latency_ms",
          "type": "string",
          "description": "Amount of latency to inject in milliseconds"
        }
      ]
    },
    {
      "name": "resilience-load",
      "description": "Generate load against the faulted API and measure error rates and percentiles",
      "commands": [
        "vegeta attack -targets targets.txt -rate 50 -duration 30s | vegeta report",
        "vegeta attack -targets targets.txt -rate 50 -duration 30s -output results.bin && vegeta report -type=json results.bin",
        "hey -n 1000 -c 50 http://localhost:8080/api/users",
        "ab -n 1000 -c 50 http://localhost:8080/api/users"
      ],
      "examples": [
        "echo \"POST http://localhost:8080/api/users\" > targets.txt && echo \"Content-Type: application/json\" >> targets.txt && vegeta attack -targets targets.txt -rate 20 -duration 20s | vegeta report",
        "hey -n 500 -c 25 http://localhost:8080/api/users",
        "vegeta attack -targets targets.txt -rate 100 -duration 60s -output results.bin; vegeta report -type=hist[0,100ms,500ms,1s] results.bin"
      ],
      "parameters": [
        {
          "name": "rate",
          "type": "string",
          "description": "Requests per second for vegeta"
        },
        {
          "name": "duration",
          "type": "string",
          "description": "Test duration such as 30s or 1m"
        }
      ]
    }
  ],
  "knowledge": [
    {
      "title": "Toxiproxy Docs",
      "type": "documentation",
      "source": "https://github.com/Shopify/toxiproxy",
      "description": "Toxiproxy proxy and toxiproxy-cli reference for fault injection"
    },
    {
      "title": "Vegeta Docs",
      "type": "documentation",
      "source": "https://github.com/tsenart/vegeta",
      "description": "Vegeta HTTP load testing tool documentation"
    }
  ],
  "instructions": "# Chaos Testing\n\nTest API resilience by injecting faults through a proxy and measuring behavior under load.\n\n## When to Use\n\n- Verifying circuit breakers and retries trigger correctly\n- Measuring p95/p99 latency under injected latency\n- Confirming the API degrades gracefully, not fatally\n\n## Setup\n\n```bash\ntoxiproxy-server &\n# or in Docker\ndocker run -d -p 8474:8474 shopify/toxiproxy\nexport GOPATH=$(go env GOPATH)\ngo install github.com/tsenart/vegeta/v12@latest\n```\n\n## Proxy and Faults\n\n```bash\ntoxiproxy-cli create api -l localhost:8081 -u localhost:8080\ntoxiproxy-cli toxic add -t latency -a latency=500 -a jitter=100 api\ntoxiproxy-cli list\ntoxiproxy-cli toxic remove -t latency api\ntoxiproxy-cli delete api\n```\n\n## Load Test\n\n```bash\ncat > targets.txt <<EOF\nPOST http://localhost:8081/api/users\nContent-Type: application/json\nEOF\nvegeta attack -targets targets.txt -rate 50 -duration 30s | vegeta report\nhey -n 1000 -c 50 http://localhost:8081/api/users\n```\n\n## Interpretation\n\n- Latency 500ms injected: expect p95 to rise ~500ms\n- Disconnect toxic: expect errors to spike and circuit breaker to open\n\n## Testing\n\n```bash\n# Baseline without faults, then with faults, and compare\nvegeta attack -targets targets.txt -rate 50 -duration 30s -output baseline.bin\nvegeta report baseline.bin\n```\n\n## Best Practices\n\n- Run baseline before injecting faults\n- Inject one fault at a time to attribute effects\n- Point load at the proxy (8081), not the real API (8080)\n- Clean up toxics after each experiment\n- Combine with monitoring to correlate spikes",
  "examples": [
    {
      "input": "Inject 500ms latency into my API and see how it behaves under load.",
      "output": "The agent starts toxiproxy-server, creates proxy api (8081->8080), adds a latency toxic, then runs vegeta attack against 8081 and reports latency percentiles."
    },
    {
      "input": "Simulate a total network outage on the API.",
      "output": "The agent adds the disconnect toxic with toxicity 1 and runs hey to confirm error rates and that the circuit breaker opens."
    }
  ],
  "platforms": {
    "claude_code": {
      "tools": [
        "Bash",
        "Read",
        "Write",
        "Edit"
      ],
      "model": "inherit"
    },
    "cursor": {
      "rule_type": "auto"
    },
    "github_copilot": {
      "prompt_file": "chaos-testing.md",
      "extension": "github.copilot"
    },
    "windsurf": {
      "model": "inherit",
      "tools": [
        "bash",
        "read",
        "write",
        "edit"
      ]
    },
    "opencode": {
      "plugin": "opencode-chaos-testing"
    },
    "generic": {
      "system_prompt": "You take the Chaos Testing seat. You do it through fault-injection: Inject latency, bandwith, and error faults on a proxy between client and API using toxiproxy-cli. The commands are real, e.g. `toxiproxy-cli create api -l localhost:8081 -u localhost:8080`. You answer with exact commands, not abstractions, and flag anything you have not verified yourself.",
      "available_tools": [
        "bash",
        "read",
        "write",
        "edit"
      ]
    }
  },
  "created_at": "2024-01-01T00:00:00Z",
  "updated_at": "2024-01-01T00:00:00Z",
  "id": "chaos-testing",
  "type": "skill",
  "skills": [],
  "tools": [
    "Bash",
    "Read",
    "Write",
    "Edit"
  ],
  "inputs": {
    "parameters": [
      {
        "capability": "fault-injection",
        "name": "toxin_type",
        "type": "string",
        "description": "latency, bandwidth, disconnect, timeout, or reset_peer"
      },
      {
        "capability": "fault-injection",
        "name": "latency_ms",
        "type": "string",
        "description": "Amount of latency to inject in milliseconds"
      },
      {
        "capability": "resilience-load",
        "name": "rate",
        "type": "string",
        "description": "Requests per second for vegeta"
      },
      {
        "capability": "resilience-load",
        "name": "duration",
        "type": "string",
        "description": "Test duration such as 30s or 1m"
      }
    ]
  },
  "outputs": {},
  "dependencies": [
    "hey",
    "toxiproxy-cli",
    "vegeta"
  ],
  "conversion": {
    "tool": "Kdesk-Catalog yaml-to-json",
    "schema": "definition-v1",
    "source_yaml": "api/chaos-testing-skill.yaml",
    "derived": {
      "skills": "none",
      "tools": "platforms.claude_code.tools",
      "inputs": "capability.parameters",
      "outputs": "none (not present in source YAML)",
      "dependencies": "prerequisites"
    }
  },
  "_provenance": {
    "generated_by": "Kdesk-Catalog yaml-to-json",
    "generator_version": "1.0.0",
    "schema": "definition-v1",
    "source": "universal-agents/api/chaos-testing-skill.yaml",
    "checksum": "9f195575d8bd4cb5b7d624019bb2a716dea496e00a3ae5b1bd02b658aacf3ace"
  }
}
