{
  "name": "elasticsearch-cluster",
  "display_name": "Elasticsearch Cluster",
  "category": "api",
  "subcategory": "general",
  "description": "Elasticsearch cluster health and operations: check node status, shard allocation, pending tasks, and cluster settings from the REST API.",
  "version": "2.0.0",
  "tags": [
    "api"
  ],
  "author": "Kdesk-Catalog",
  "license": "MIT",
  "capabilities": [
    {
      "name": "cluster-health",
      "description": "Inspect cluster health, nodes, allocations, and settings; diagnose red/yellow cluster states.",
      "commands": [
        "curl -s 'localhost:9200/_cluster/health?pretty' | jq",
        "curl -s 'localhost:9200/_cat/nodes?v'",
        "curl -s 'localhost:9200/_cat/allocation?v&s=disk.percent:desc'",
        "curl -s 'localhost:9200/_cluster/pending_tasks?pretty'",
        "curl -s 'localhost:9200/_cluster/settings?include_defaults=true' | jq '.persistent'"
      ],
      "examples": [
        "curl -s 'localhost:9200/_cluster/health?pretty' | jq '{status, unassigned_shards, number_of_nodes}'",
        "curl -s 'localhost:9200/_cat/nodes?v&h=name,heap.percent,disk.used_percent,master'",
        "curl -s 'localhost:9200/_cluster/reroute?explain' | jq"
      ],
      "parameters": [
        {
          "name": "es-url",
          "type": "string",
          "description": "Elasticsearch endpoint (default localhost:9200)"
        },
        {
          "name": "timeout",
          "type": "string",
          "description": "Wait-for-status timeout like 50s for health checks"
        },
        {
          "name": "level",
          "type": "string",
          "description": "Health detail level: cluster, indices, shards"
        }
      ]
    }
  ],
  "knowledge": [
    {
      "title": "Cluster Health API",
      "type": "documentation",
      "source": "https://www.elastic.co/guide/en/elasticsearch/reference/current/cluster-health.html",
      "description": "Cluster health semantics: green/yellow/red and what each means"
    },
    {
      "title": "Cluster Settings API",
      "type": "documentation",
      "source": "https://www.elastic.co/guide/en/elasticsearch/reference/current/cluster-update-settings.html",
      "description": "Transient and persistent cluster settings"
    }
  ],
  "instructions": "# Elasticsearch Cluster\n\n## What this skill does\n\nThis skill is about the Elasticsearch cluster as a whole: node state, shard allocation, disk pressure, pending tasks, and cluster-wide settings. You operate the cluster via the REST API with curl and jq.\n\n## When to use\n\n- A cluster turns yellow or red\n- Nodes show high disk or heap pressure\n- Shards are unassigned after a node restart\n\n## Real commands\n\n```bash\n# Overall status\ncurl -s 'localhost:9200/_cluster/health?pretty' | jq '{status, unassigned_shards, number_of_nodes, active_shards_percent_as_number}'\n\n# Node inventory: heap, disk, master eligibility\ncurl -s 'localhost:9200/_cat/nodes?v&h=name,heap.percent,disk.used_percent,master,node.role'\n\n# Disk allocation per node\ncurl -s 'localhost:9200/_cat/allocation?v&s=disk.percent:desc'\n\n# Why are shards unassigned?\ncurl -s 'localhost:9200/_cluster/reroute?explain' | jq '.explanations[].deciders'\n\n# Cluster settings (persistent vs transient)\ncurl -s 'localhost:9200/_cluster/settings?include_defaults=true' | jq '.persistent'\n```\n\n## Status meaning\n\n- green: all primary and replica shards allocated\n- yellow: primaries allocated, replicas missing (e.g. one node)\n- red: at least one primary unassigned - data at risk\n\n## Common fixes\n\n```bash\n# Set a watermark and reroute with retries\ncurl -s -X PUT 'localhost:9200/_cluster/settings' -H 'Content-Type: application/json' -d '{\"persistent\":{\"cluster.routing.allocation.disk.watermark.high\":\"85%\"}}' | jq\n\n# Retry failed allocations\ncurl -s -X POST 'localhost:9200/_cluster/reroute?retry_failed=true' | jq\n```\n\n## Best practices\n\n- Alert on yellow for more than 5 minutes and red immediately.\n- Keep disk below 80%; watch `_cat/allocation` sorted by percent.\n- Prefer `retry_failed=true` over manual shard moves.\n- Never set transient settings in automation; use persistent.\n",
  "examples": [
    {
      "input": "Cluster shows yellow after a node went down",
      "output": "Run _cat/nodes to confirm node count, _cluster/health for unassigned_shards, and check _cat/allocation for disk pressure; replicas will reallocate once a node returns."
    },
    {
      "input": "Disk usage is critical on one node",
      "output": "Check _cat/allocation?&s=disk.percent:desc, set a higher watermark persistently, and let reroute rebalance shards off the hot node."
    }
  ],
  "platforms": {
    "claude_code": {
      "tools": [
        "Bash",
        "Read",
        "Write",
        "Edit"
      ],
      "model": "inherit"
    },
    "cursor": {
      "rule_type": "auto"
    },
    "github_copilot": {
      "prompt_file": "elasticsearch-cluster.md",
      "extension": "github.copilot"
    },
    "windsurf": {
      "model": "inherit",
      "tools": [
        "bash",
        "read",
        "write",
        "edit"
      ]
    },
    "opencode": {
      "plugin": "opencode-elasticsearch-cluster"
    },
    "generic": {
      "system_prompt": "You take the Elasticsearch Cluster seat. You do it through cluster-health: Inspect cluster health, nodes, allocations, and settings; diagnose red/yellow cluster states. The commands are real, e.g. `curl -s localhost:9200/_cluster/health?pretty | jq`. You run and verify the real CLI commands before you claim anything.",
      "available_tools": [
        "bash",
        "read",
        "write",
        "edit"
      ]
    }
  },
  "created_at": "2024-01-01T00:00:00Z",
  "updated_at": "2024-01-01T00:00:00Z",
  "id": "elasticsearch-cluster",
  "type": "skill",
  "skills": [],
  "tools": [
    "Bash",
    "Read",
    "Write",
    "Edit"
  ],
  "inputs": {
    "parameters": [
      {
        "capability": "cluster-health",
        "name": "es-url",
        "type": "string",
        "description": "Elasticsearch endpoint (default localhost:9200)"
      },
      {
        "capability": "cluster-health",
        "name": "timeout",
        "type": "string",
        "description": "Wait-for-status timeout like 50s for health checks"
      },
      {
        "capability": "cluster-health",
        "name": "level",
        "type": "string",
        "description": "Health detail level: cluster, indices, shards"
      }
    ]
  },
  "outputs": {},
  "dependencies": [],
  "conversion": {
    "tool": "Kdesk-Catalog yaml-to-json",
    "schema": "definition-v1",
    "source_yaml": "api/elasticsearch-cluster-skill.yaml",
    "derived": {
      "skills": "none",
      "tools": "platforms.claude_code.tools",
      "inputs": "capability.parameters",
      "outputs": "none (not present in source YAML)",
      "dependencies": "none"
    }
  },
  "_provenance": {
    "generated_by": "Kdesk-Catalog yaml-to-json",
    "generator_version": "1.0.0",
    "schema": "definition-v1",
    "source": "universal-agents/api/elasticsearch-cluster-skill.yaml",
    "checksum": "5888aa24b6a17b33c68f8690418e70e4e579b37b7939b19efa6958f51a099087"
  }
}
