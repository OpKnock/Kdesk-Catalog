{
  "name": "field-encryption",
  "display_name": "Field Encryption",
  "category": "api",
  "subcategory": "general",
  "description": "Column-level and field-level encryption: encrypt sensitive database fields with pgcrypto, envelope encryption in app code, and searchability trade-offs.",
  "version": "2.0.0",
  "tags": [
    "api"
  ],
  "author": "Kdesk-Catalog",
  "license": "MIT",
  "prerequisites": [
    "aws",
    "psql"
  ],
  "capabilities": [
    {
      "name": "column-encryption",
      "description": "Encrypt and decrypt specific fields with pgcrypto and application-level envelope keys.",
      "commands": [
        "psql -c \"CREATE EXTENSION IF NOT EXISTS pgcrypto;\"",
        "psql -c \"SELECT pgp_sym_encrypt('sensitive value', 'secret-passphrase') AS cipher;\"",
        "psql -c \"SELECT pgp_sym_decrypt(pgp_sym_encrypt('value','pass'), 'pass');\"",
        "aws kms generate-data-key --key-id alias/my-key --key-spec AES_256 --output json | jq -r '.CiphertextBlob'",
        "psql -c \"SELECT id, pgp_sym_decrypt(email_cipher, 'app-key') FROM users WHERE id = 1;\""
      ],
      "examples": [
        "psql -c \"CREATE EXTENSION IF NOT EXISTS pgcrypto;\"",
        "aws kms generate-data-key --key-id alias/my-key --key-spec AES_256 --output json | jq -r '.CiphertextBlob'",
        "psql -c \"SELECT id, pgp_sym_decrypt(email_cipher, 'app-key') FROM users WHERE id = 1;\""
      ],
      "parameters": [
        {
          "name": "field",
          "type": "string",
          "description": "Column/field to encrypt, e.g. email"
        },
        {
          "name": "cipher",
          "type": "string",
          "description": "Cipher choice: pgp_sym, AES-256 via app, or KMS envelope"
        },
        {
          "name": "key-source",
          "type": "string",
          "description": "Where keys live: DB passphrase, env var, or KMS"
        }
      ]
    }
  ],
  "knowledge": [
    {
      "title": "pgcrypto documentation",
      "type": "documentation",
      "source": "https://www.postgresql.org/docs/current/pgcrypto.html",
      "description": "pgp_sym_encrypt/decrypt, digest, and other cryptographic functions"
    },
    {
      "title": "AWS KMS Data Keys",
      "type": "documentation",
      "source": "https://docs.aws.amazon.com/kms/latest/developerguide/concepts.html#data-keys",
      "description": "Generate and use data keys for envelope encryption"
    }
  ],
  "instructions": "# Field Encryption\n\n## What this skill does\n\nField encryption protects individual columns or object fields (emails, SSNs, payment tokens) rather than the whole disk. pgcrypto encrypts inside PostgreSQL; app-level envelope encryption uses KMS data keys.\n\n## When to use\n\n- Compliance requirements targeting specific data elements\n- Protecting PII in databases that can't be fully encrypted\n- Limiting blast radius if a backup leaks\n\n## Real commands\n\n```bash\n# pgcrypto in PostgreSQL\npsql -c \"CREATE EXTENSION IF NOT EXISTS pgcrypto;\"\npsql -c \"SELECT pgp_sym_encrypt('sensitive value', 'secret-passphrase') AS cipher;\"\npsql -c \"SELECT pgp_sym_decrypt(pgp_sym_encrypt('value','pass'), 'pass');\"\n\n# KMS data key for envelope encryption\naws kms generate-data-key --key-id alias/my-key --key-spec AES_256 --output json | jq -r '.CiphertextBlob'\naws kms decrypt --ciphertext-blob fileb://envelope.blob --output json | jq -r '.Plaintext'\n```\n\n## Schema example\n\n```sql\nALTER TABLE users ADD COLUMN email_cipher bytea;\nUPDATE users SET email_cipher = pgp_sym_encrypt(email, 'app-key');\nALTER TABLE users DROP COLUMN email;\n```\n\n## Envelope encryption pattern\n\n1. `generate-data-key` in KMS: returns plaintext key + encrypted key blob.\n2. Encrypt the field with the plaintext data key (AES-256-GCM) in the app.\n3. Store ciphertext + encrypted key blob; discard the plaintext key.\n4. To decrypt: `kms decrypt` the blob, decrypt the field.\n\n## Testing\n\n```bash\n# Round trip must be lossless\npsql -t -c \"SELECT pgp_sym_decrypt(pgp_sym_encrypt('roundtrip','k'), 'k') = 'roundtrip';\"\n```\n\n## Best practices\n\n- Encrypt at rest AND at the field level for highest-risk attributes only; every field has a cost.\n- Keep keys out of the DB; use KMS or a vault, with rotation.\n- Be aware you lose equality lookups: precompute a hash column for exact match.\n- Log decryption access (e.g. pgaudit or app audit) for sensitive fields.\n- Test key rotation end to end before rolling out.\n",
  "examples": [
    {
      "input": "Encrypt user emails in PostgreSQL",
      "output": "Create pgcrypto, add an email_cipher column, backfill with pgp_sym_encrypt, drop the plaintext column, and add a decryption-only path via an app function."
    },
    {
      "input": "Field encryption without DB extensions",
      "output": "Use KMS generate-data-key for envelope encryption in the application layer and store the encrypted key blob with the ciphertext."
    }
  ],
  "platforms": {
    "claude_code": {
      "tools": [
        "Bash",
        "Read",
        "Write",
        "Edit"
      ],
      "model": "inherit"
    },
    "cursor": {
      "rule_type": "auto"
    },
    "github_copilot": {
      "prompt_file": "field-encryption.md",
      "extension": "github.copilot"
    },
    "windsurf": {
      "model": "inherit",
      "tools": [
        "bash",
        "read",
        "write",
        "edit"
      ]
    },
    "opencode": {
      "plugin": "opencode-field-encryption"
    },
    "generic": {
      "system_prompt": "You work Field Encryption hands-on. You do it through column-encryption: Encrypt and decrypt specific fields with pgcrypto and application-level envelope keys. The commands are real, e.g. `psql -c CREATE EXTENSION IF NOT EXISTS pgcrypto;`. You prefer the shortest safe path, and you reproduce failures before diagnosing them.",
      "available_tools": [
        "bash",
        "read",
        "write",
        "edit"
      ]
    }
  },
  "created_at": "2024-01-01T00:00:00Z",
  "updated_at": "2024-01-01T00:00:00Z",
  "id": "field-encryption",
  "type": "skill",
  "skills": [],
  "tools": [
    "Bash",
    "Read",
    "Write",
    "Edit"
  ],
  "inputs": {
    "parameters": [
      {
        "capability": "column-encryption",
        "name": "field",
        "type": "string",
        "description": "Column/field to encrypt, e.g. email"
      },
      {
        "capability": "column-encryption",
        "name": "cipher",
        "type": "string",
        "description": "Cipher choice: pgp_sym, AES-256 via app, or KMS envelope"
      },
      {
        "capability": "column-encryption",
        "name": "key-source",
        "type": "string",
        "description": "Where keys live: DB passphrase, env var, or KMS"
      }
    ]
  },
  "outputs": {},
  "dependencies": [
    "aws",
    "psql"
  ],
  "conversion": {
    "tool": "Kdesk-Catalog yaml-to-json",
    "schema": "definition-v1",
    "source_yaml": "api/field-encryption-skill.yaml",
    "derived": {
      "skills": "none",
      "tools": "platforms.claude_code.tools",
      "inputs": "capability.parameters",
      "outputs": "none (not present in source YAML)",
      "dependencies": "prerequisites"
    }
  },
  "_provenance": {
    "generated_by": "Kdesk-Catalog yaml-to-json",
    "generator_version": "1.0.0",
    "schema": "definition-v1",
    "source": "universal-agents/api/field-encryption-skill.yaml",
    "checksum": "00b86cc0357278cd314b17777e1e5a168e1b2eae91bba86bf52456d8e7875aff"
  }
}
