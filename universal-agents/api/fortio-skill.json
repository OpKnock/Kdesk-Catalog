{
  "name": "fortio",
  "display_name": "Fortio",
  "category": "api",
  "subcategory": "general",
  "description": "Load testing with Fortio: run HTTP load tests, generate reports, and inspect latency percentiles and errors from the CLI.",
  "version": "2.0.0",
  "tags": [
    "api"
  ],
  "author": "Kdesk-Catalog",
  "license": "MIT",
  "prerequisites": [
    "fortio"
  ],
  "capabilities": [
    {
      "name": "fortio-load",
      "description": "Run HTTP load tests with configurable rate, connections, and duration.",
      "commands": [
        "fortio load -c 50 -t 30s -qps 500 http://localhost:8080/api/orders",
        "fortio load -c 10 -n 1000 -httpbuffered=false http://localhost:8080/health",
        "fortio load -H 'Authorization: Bearer token' -c 20 -t 10s http://localhost:8080/api/orders/1",
        "fortio report",
        "fortio load -c 50 -t 60s -json result.json http://localhost:8080/ | jq '.DurationHistogram.Percentiles'"
      ],
      "examples": [
        "fortio load -c 50 -t 30s -qps 500 http://localhost:8080/api/orders",
        "fortio load -c 50 -t 60s -json result.json http://localhost:8080/ | jq '.DurationHistogram.Percentiles'",
        "fortio report"
      ],
      "parameters": [
        {
          "name": "connections",
          "type": "integer",
          "description": "Concurrent connections"
        },
        {
          "name": "qps",
          "type": "integer",
          "description": "Target queries per second (0 = max)"
        },
        {
          "name": "duration",
          "type": "string",
          "description": "Test duration like 30s"
        }
      ]
    }
  ],
  "knowledge": [
    {
      "title": "Fortio README",
      "type": "documentation",
      "source": "https://github.com/fortio/fortio",
      "description": "Load testing CLI usage, flags, and JSON report format"
    },
    {
      "title": "Fortio load guide",
      "type": "documentation",
      "source": "https://fortio.org/docs/fortio-load/",
      "description": "Example load tests and report interpretation"
    }
  ],
  "instructions": "# Fortio\n\n## What this skill does\n\nFortio is Istio's load testing tool: it runs HTTP/HTTPS/gRPC load tests with precise rate control, collects latency histograms, and writes JSON reports.\n\n## When to use\n\n- Validating SLOs under controlled QPS\n- Comparing latency across service versions\n- Pre-launch capacity checks\n\n## Real commands\n\n```bash\n# Fixed rate test: 50 conns, 500 qps, 30s\nfortio load -c 50 -t 30s -qps 500 http://localhost:8080/api/orders\n\n# Max throughput (no rate cap)\nfortio load -c 10 -n 1000 -httpbuffered=false http://localhost:8080/health\n\n# With headers\nfortio load -H 'Authorization: Bearer token' -c 20 -t 10s http://localhost:8080/api/orders/1\n\n# JSON report for CI\nfortio load -c 50 -t 60s -json result.json http://localhost:8080/\n\n# Browse the last report\nfortio report\n```\n\n## Reading the report\n\n```bash\nfortio load -c 50 -t 60s -json result.json http://localhost:8080/ | jq '{qps: .ActualQPS, p50: .DurationHistogram.Percentiles[0].Value, p99: .DurationHistogram.Percentiles[4].Value, errors: .RetCodes}'\n```\n\n## Best practices\n\n- Use `-qps` to test at the expected production rate; unthrottled only for max-capacity runs.\n- Save `-json` reports and diff percentiles across releases.\n- Run a warmup burst (short -n) before timed runs.\n- Watch both P99 and error codes; a low P99 with 5xx is still failure.\n- Use `-httpbuffered=false` for latency-sensitive keep-alive workloads.\n",
  "examples": [
    {
      "input": "Does the API hold 500 qps under 200ms p99?",
      "output": "Run fortio load -c 50 -t 30s -qps 500 with -json output and check the DurationHistogram p99 plus RetCodes for 5xx."
    },
    {
      "input": "Compare two service versions",
      "output": "Run identical fortio load commands against both, save -json reports, and compare ActualQPS and Percentiles."
    }
  ],
  "platforms": {
    "claude_code": {
      "tools": [
        "Bash",
        "Read",
        "Write",
        "Edit"
      ],
      "model": "inherit"
    },
    "cursor": {
      "rule_type": "auto"
    },
    "github_copilot": {
      "prompt_file": "fortio.md",
      "extension": "github.copilot"
    },
    "windsurf": {
      "model": "inherit",
      "tools": [
        "bash",
        "read",
        "write",
        "edit"
      ]
    },
    "opencode": {
      "plugin": "opencode-fortio"
    },
    "generic": {
      "system_prompt": "This is your domain: Fortio. You do it through fortio-load: Run HTTP load tests with configurable rate, connections, and duration. The commands are real, e.g. `fortio load -c 50 -t 30s -qps 500 http://localhost:8080/api/orders`. You run and verify the real CLI commands before you claim anything.",
      "available_tools": [
        "bash",
        "read",
        "write",
        "edit"
      ]
    }
  },
  "created_at": "2024-01-01T00:00:00Z",
  "updated_at": "2024-01-01T00:00:00Z",
  "id": "fortio",
  "type": "skill",
  "skills": [],
  "tools": [
    "Bash",
    "Read",
    "Write",
    "Edit"
  ],
  "inputs": {
    "parameters": [
      {
        "capability": "fortio-load",
        "name": "connections",
        "type": "integer",
        "description": "Concurrent connections"
      },
      {
        "capability": "fortio-load",
        "name": "qps",
        "type": "integer",
        "description": "Target queries per second (0 = max)"
      },
      {
        "capability": "fortio-load",
        "name": "duration",
        "type": "string",
        "description": "Test duration like 30s"
      }
    ]
  },
  "outputs": {},
  "dependencies": [
    "fortio"
  ],
  "conversion": {
    "tool": "Kdesk-Catalog yaml-to-json",
    "schema": "definition-v1",
    "source_yaml": "api/fortio-skill.yaml",
    "derived": {
      "skills": "none",
      "tools": "platforms.claude_code.tools",
      "inputs": "capability.parameters",
      "outputs": "none (not present in source YAML)",
      "dependencies": "prerequisites"
    }
  },
  "_provenance": {
    "generated_by": "Kdesk-Catalog yaml-to-json",
    "generator_version": "1.0.0",
    "schema": "definition-v1",
    "source": "universal-agents/api/fortio-skill.yaml",
    "checksum": "176004ed1b45be28897e77bc524b267a34a36d26053ad829c1de43d8269c8966"
  }
}
