name: fortio
display_name: Fortio
category: api
subcategory: general
description: 'Load testing with Fortio: run HTTP load tests, generate reports, and inspect latency percentiles
  and errors from the CLI.'
version: 2.0.0
tags:
- api
author: Kdesk
license: MIT
prerequisites:
- fortio
capabilities:
- name: fortio-load
  description: Run HTTP load tests with configurable rate, connections, and duration.
  commands:
  - fortio load -c 50 -t 30s -qps 500 http://localhost:8080/api/orders
  - fortio load -c 10 -n 1000 -httpbuffered=false http://localhost:8080/health
  - 'fortio load -H ''Authorization: Bearer token'' -c 20 -t 10s http://localhost:8080/api/orders/1'
  - fortio report
  - fortio load -c 50 -t 60s -json result.json http://localhost:8080/ | jq '.DurationHistogram.Percentiles'
  examples:
  - fortio load -c 50 -t 30s -qps 500 http://localhost:8080/api/orders
  - fortio load -c 50 -t 60s -json result.json http://localhost:8080/ | jq '.DurationHistogram.Percentiles'
  - fortio report
  parameters:
  - name: connections
    type: integer
    description: Concurrent connections
  - name: qps
    type: integer
    description: Target queries per second (0 = max)
  - name: duration
    type: string
    description: Test duration like 30s
knowledge:
- title: Fortio README
  type: documentation
  source: https://github.com/fortio/fortio
  description: Load testing CLI usage, flags, and JSON report format
- title: Fortio load guide
  type: documentation
  source: https://fortio.org/docs/fortio-load/
  description: Example load tests and report interpretation
instructions: '# Fortio


  ## What this skill does


  Fortio is Istio''s load testing tool: it runs HTTP/HTTPS/gRPC load tests with precise rate control,
  collects latency histograms, and writes JSON reports.


  ## When to use


  - Validating SLOs under controlled QPS

  - Comparing latency across service versions

  - Pre-launch capacity checks


  ## Real commands


  ```bash

  # Fixed rate test: 50 conns, 500 qps, 30s

  fortio load -c 50 -t 30s -qps 500 http://localhost:8080/api/orders


  # Max throughput (no rate cap)

  fortio load -c 10 -n 1000 -httpbuffered=false http://localhost:8080/health


  # With headers

  fortio load -H ''Authorization: Bearer token'' -c 20 -t 10s http://localhost:8080/api/orders/1


  # JSON report for CI

  fortio load -c 50 -t 60s -json result.json http://localhost:8080/


  # Browse the last report

  fortio report

  ```


  ## Reading the report


  ```bash

  fortio load -c 50 -t 60s -json result.json http://localhost:8080/ | jq ''{qps: .ActualQPS, p50: .DurationHistogram.Percentiles[0].Value,
  p99: .DurationHistogram.Percentiles[4].Value, errors: .RetCodes}''

  ```


  ## Best practices


  - Use `-qps` to test at the expected production rate; unthrottled only for max-capacity runs.

  - Save `-json` reports and diff percentiles across releases.

  - Run a warmup burst (short -n) before timed runs.

  - Watch both P99 and error codes; a low P99 with 5xx is still failure.

  - Use `-httpbuffered=false` for latency-sensitive keep-alive workloads.

  '
examples:
- input: Does the API hold 500 qps under 200ms p99?
  output: Run fortio load -c 50 -t 30s -qps 500 with -json output and check the DurationHistogram p99
    plus RetCodes for 5xx.
- input: Compare two service versions
  output: Run identical fortio load commands against both, save -json reports, and compare ActualQPS and
    Percentiles.
platforms:
  claude_code:
    tools:
    - Bash
    - Read
    - Write
    - Edit
    model: inherit
  cursor:
    rule_type: auto
  github_copilot:
    prompt_file: fortio.md
    extension: github.copilot
  windsurf:
    model: inherit
    tools:
    - bash
    - read
    - write
    - edit
  opencode:
    plugin: opencode-fortio
  generic:
    system_prompt: 'This is your domain: Fortio. You do it through fortio-load: Run HTTP load tests with
      configurable rate, connections, and duration. The commands are real, e.g. `fortio load -c 50 -t
      30s -qps 500 http://localhost:8080/api/orders`. You run and verify the real CLI commands before
      you claim anything.'
    available_tools:
    - bash
    - read
    - write
    - edit
created_at: '2026-06-20T00:00:00Z'
updated_at: '2026-08-19T00:00:00Z'
