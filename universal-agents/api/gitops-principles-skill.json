{
  "name": "gitops-principles",
  "display_name": "Gitops Principles",
  "category": "api",
  "subcategory": "general",
  "description": "GitOps principles and workflows: declarative cluster state in git, pull-based sync, and safe rollback practices with kubectl and flux.",
  "version": "2.0.0",
  "tags": [
    "api"
  ],
  "author": "Kdesk-Catalog",
  "license": "MIT",
  "prerequisites": [
    "flux",
    "kubectl"
  ],
  "capabilities": [
    {
      "name": "gitops-workflow",
      "description": "Apply GitOps practices: declarative manifests, sync reconciliation, and rollbacks.",
      "commands": [
        "kubectl apply --dry-run=client -f manifests/ -o yaml > /dev/null && echo 'valid'",
        "kubectl diff -f manifests/",
        "kubectl apply -f manifests/",
        "flux reconcile kustomization apps --with-source",
        "kubectl rollout status deployment/orders -n app --timeout=120s",
        "kubectl rollout undo deployment/orders -n app"
      ],
      "examples": [
        "kubectl diff -f manifests/ && kubectl apply -f manifests/",
        "kubectl rollout undo deployment/orders -n app",
        "flux reconcile kustomization apps --with-source"
      ],
      "parameters": [
        {
          "name": "manifest-dir",
          "type": "string",
          "description": "Directory with declarative manifests"
        },
        {
          "name": "deployment",
          "type": "string",
          "description": "Deployment to verify or roll back"
        },
        {
          "name": "namespace",
          "type": "string",
          "description": "Target namespace"
        }
      ]
    }
  ],
  "knowledge": [
    {
      "title": "GitOps Principles (CNCF)",
      "type": "documentation",
      "source": "https://opengitops.dev/",
      "description": "The OpenGitOps principles: declarative, versioned, pulled, and continuous"
    },
    {
      "title": "Weaveworks GitOps guide",
      "type": "documentation",
      "source": "https://www.weave.works/technologies/gitops/",
      "description": "GitOps workflow patterns and benefits"
    }
  ],
  "instructions": "# GitOps Principles\n\n## What this skill does\n\nGitOps treats git as the single source of truth for infrastructure: desired state is declarative in repos, agents (Flux/Argo) pull and apply it, and rollback is a git revert.\n\n## When to use\n\n- Moving from click-ops to pull-based delivery\n- Making every change reviewable and revertible\n- Recovering a cluster by re-syncing from git\n\n## Real commands\n\n```bash\n# Validate before applying\nkubectl apply --dry-run=client -f manifests/ -o yaml > /dev/null && echo 'valid'\n\n# Diff what would change\nkubectl diff -f manifests/\n\n# Apply (when not using a GitOps agent)\nkubectl apply -f manifests/\n\n# Force the agent to sync\nflux reconcile kustomization apps --with-source\n\n# Verify and roll back\nkubectl rollout status deployment/orders -n app --timeout=120s\nkubectl rollout undo deployment/orders -n app\n```\n\n## Core principles\n\n1. Declarative: desired state expressed as manifests.\n2. Versioned: changes go through git with review.\n3. Pulled: agents fetch and apply; nobody applies ad hoc.\n4. Continuous: the cluster always converges to git.\n\n## Workflow example\n\n```bash\ngit checkout -b fix/rollback-orders\ngit revert <breaking-commit>\nkubectl diff -f manifests/ | head -40\n# review the diff, then merge; the agent applies the revert\n```\n\n## Best practices\n\n- Review every change via MR/PR before merge.\n- Prefer agents (Flux/Argo) over human kubectl apply.\n- Keep secrets out of git (Sealed Secrets, external-secrets).\n- Add drift detection alerts for out-of-sync resources.\n- Practice disaster recovery: re-sync a blank cluster from git.\n",
  "examples": [
    {
      "input": "Revert a bad deployment the GitOps way",
      "output": "git revert the commit that changed the manifests, merge, and let the sync agent converge; verify with kubectl rollout status."
    },
    {
      "input": "Ensure no one applies ad hoc changes",
      "output": "Move all changes through MRs, use a GitOps agent with prune, and alert on resources that diverge from git."
    }
  ],
  "platforms": {
    "claude_code": {
      "tools": [
        "Bash",
        "Read",
        "Write",
        "Edit"
      ],
      "model": "inherit"
    },
    "cursor": {
      "rule_type": "auto"
    },
    "github_copilot": {
      "prompt_file": "gitops-principles.md",
      "extension": "github.copilot"
    },
    "windsurf": {
      "model": "inherit",
      "tools": [
        "bash",
        "read",
        "write",
        "edit"
      ]
    },
    "opencode": {
      "plugin": "opencode-gitops-principles"
    },
    "generic": {
      "system_prompt": "You are the Gitops Principles specialist. You do it through gitops-workflow: Apply GitOps practices: declarative manifests, sync reconciliation, and rollbacks. The commands are real, e.g. `kubectl apply --dry-run=client -f manifests/ -o yaml > /dev/null && echo valid`. You verify each step you report and stay honest about what you have not run.",
      "available_tools": [
        "bash",
        "read",
        "write",
        "edit"
      ]
    }
  },
  "created_at": "2024-01-01T00:00:00Z",
  "updated_at": "2024-01-01T00:00:00Z",
  "id": "gitops-principles",
  "type": "skill",
  "skills": [],
  "tools": [
    "Bash",
    "Read",
    "Write",
    "Edit"
  ],
  "inputs": {
    "parameters": [
      {
        "capability": "gitops-workflow",
        "name": "manifest-dir",
        "type": "string",
        "description": "Directory with declarative manifests"
      },
      {
        "capability": "gitops-workflow",
        "name": "deployment",
        "type": "string",
        "description": "Deployment to verify or roll back"
      },
      {
        "capability": "gitops-workflow",
        "name": "namespace",
        "type": "string",
        "description": "Target namespace"
      }
    ]
  },
  "outputs": {},
  "dependencies": [
    "flux",
    "kubectl"
  ],
  "conversion": {
    "tool": "Kdesk-Catalog yaml-to-json",
    "schema": "definition-v1",
    "source_yaml": "api/gitops-principles-skill.yaml",
    "derived": {
      "skills": "none",
      "tools": "platforms.claude_code.tools",
      "inputs": "capability.parameters",
      "outputs": "none (not present in source YAML)",
      "dependencies": "prerequisites"
    }
  },
  "_provenance": {
    "generated_by": "Kdesk-Catalog yaml-to-json",
    "generator_version": "1.0.0",
    "schema": "definition-v1",
    "source": "universal-agents/api/gitops-principles-skill.yaml",
    "checksum": "12ef11df71f7efbb074c252d8ffe941dd56ffdc5c1d8ae6a751330ef4b5addcf"
  }
}
