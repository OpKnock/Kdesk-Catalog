{
  "name": "grafana-loki",
  "display_name": "Grafana Loki",
  "category": "api",
  "subcategory": "general",
  "description": "Log aggregation with Grafana Loki: query logs with logcli, filter with LogQL, and manage labels and retention.",
  "version": "2.0.0",
  "tags": [
    "api"
  ],
  "author": "Kdesk-Catalog",
  "license": "MIT",
  "prerequisites": [
    "logcli"
  ],
  "capabilities": [
    {
      "name": "loki-queries",
      "description": "Query Loki logs via logcli and inspect label usage.",
      "commands": [
        "logcli query '{job=\"orders\"} |= \"ERROR\"' --limit=50 --since=1h",
        "logcli labels --since=24h",
        "logcli series --since=24h --match='{job=\"orders\"}'",
        "logcli query 'sum by (level) (count_over_time({job=\"orders\"}[5m]))' --since=1h",
        "curl -s 'http://localhost:3100/loki/api/v1/labels' | jq '.data.values'"
      ],
      "examples": [
        "logcli query '{job=\"orders\"} |= \"ERROR\"' --limit=50 --since=1h",
        "logcli labels --since=24h",
        "logcli query 'sum by (level) (count_over_time({job=\"orders\"}[5m]))' --since=1h"
      ],
      "parameters": [
        {
          "name": "label-filter",
          "type": "string",
          "description": "LogQL label selector like {job=\"orders\"}"
        },
        {
          "name": "line-filter",
          "type": "string",
          "description": "Line filter like |= \"ERROR\""
        },
        {
          "name": "since",
          "type": "string",
          "description": "Lookback window like 1h"
        }
      ]
    }
  ],
  "knowledge": [
    {
      "title": "LogQL reference",
      "type": "documentation",
      "source": "https://grafana.com/docs/loki/latest/logql/",
      "description": "Label selectors, line filters, and log metric queries"
    },
    {
      "title": "logcli docs",
      "type": "documentation",
      "source": "https://grafana.com/docs/loki/latest/query/logcli/",
      "description": "logcli query and series commands"
    }
  ],
  "instructions": "# Grafana Loki\n\n## What this skill does\n\nLoki is a log aggregation system that indexes labels, not content, making it cheap to store high-volume logs. logcli queries logs with LogQL: label selectors, line filters, and log-range metrics.\n\n## When to use\n\n- Centralized log search across services\n- Error rate derivation from logs (count_over_time)\n- Auditing label cardinality\n\n## Real commands\n\n```bash\n# Simple search with line filter\nlogcli query '{job=\"orders\"} |= \"ERROR\"' --limit=50 --since=1h\n\n# All labels in use\nlogcli labels --since=24h\n\n# Series for a selector\nlogcli series --since=24h --match='{job=\"orders\"}'\n\n# Log-rate metric: errors per minute by level\nlogcli query 'sum by (level) (count_over_time({job=\"orders\"}[5m]))' --since=1h\n\n# REST API\ncurl -s 'http://localhost:3100/loki/api/v1/labels' | jq '.data.values'\n```\n\n## LogQL patterns\n\n```logql\n# Line filters (fast, cheap)\n{job=\"orders\"} |~ \"(5\\d\\d|panic)\"\n# Parsing JSON fields\n{job=\"orders\"} | json | level = \"error\"\n# Rate of matched lines\nsum(rate({job=\"orders\"} |= \"ERROR\" [5m]))\n```\n\n## Testing\n\n```bash\n# Verify a known log line is queryable\nlogcli query '{job=\"orders\", service=\"payments\"}' --since=5m | grep 'order-123'\n```\n\n## Best practices\n\n- Use label selectors for cost, line filters for precision.\n- Keep label cardinality low: no per-user or per-request labels.\n- Prefer `|= \"string\"` over regex where possible (faster).\n- Derive error rates with count_over_time instead of shipping separate metrics.\n- Set retention per tenant/stream; Loki grows with cardinality, not volume.\n",
  "examples": [
    {
      "input": "Find all ERROR lines in orders in the last hour",
      "output": "logcli query '{job=\"orders\"} |= \"ERROR\"' --since=1h and iterate the line filter to narrow context."
    },
    {
      "input": "Check error rate from logs for an SLO",
      "output": "Run sum(rate({job=\"orders\"} |= \"ERROR\"[5m])) as a LogQL metric query and chart it in Grafana."
    }
  ],
  "platforms": {
    "claude_code": {
      "tools": [
        "Bash",
        "Read",
        "Write",
        "Edit"
      ],
      "model": "inherit"
    },
    "cursor": {
      "rule_type": "auto"
    },
    "github_copilot": {
      "prompt_file": "grafana-loki.md",
      "extension": "github.copilot"
    },
    "windsurf": {
      "model": "inherit",
      "tools": [
        "bash",
        "read",
        "write",
        "edit"
      ]
    },
    "opencode": {
      "plugin": "opencode-grafana-loki"
    },
    "generic": {
      "system_prompt": "This is your domain: Grafana Loki. You do it through loki-queries: Query Loki logs via logcli and inspect label usage. The commands are real, e.g. `logcli query {job=orders} |= ERROR --limit=50 --since=1h`. You verify each step you report and stay honest about what you have not run.",
      "available_tools": [
        "bash",
        "read",
        "write",
        "edit"
      ]
    }
  },
  "created_at": "2024-01-01T00:00:00Z",
  "updated_at": "2024-01-01T00:00:00Z",
  "id": "grafana-loki",
  "type": "skill",
  "skills": [],
  "tools": [
    "Bash",
    "Read",
    "Write",
    "Edit"
  ],
  "inputs": {
    "parameters": [
      {
        "capability": "loki-queries",
        "name": "label-filter",
        "type": "string",
        "description": "LogQL label selector like {job=\"orders\"}"
      },
      {
        "capability": "loki-queries",
        "name": "line-filter",
        "type": "string",
        "description": "Line filter like |= \"ERROR\""
      },
      {
        "capability": "loki-queries",
        "name": "since",
        "type": "string",
        "description": "Lookback window like 1h"
      }
    ]
  },
  "outputs": {},
  "dependencies": [
    "logcli"
  ],
  "conversion": {
    "tool": "Kdesk-Catalog yaml-to-json",
    "schema": "definition-v1",
    "source_yaml": "api/grafana-loki-skill.yaml",
    "derived": {
      "skills": "none",
      "tools": "platforms.claude_code.tools",
      "inputs": "capability.parameters",
      "outputs": "none (not present in source YAML)",
      "dependencies": "prerequisites"
    }
  },
  "_provenance": {
    "generated_by": "Kdesk-Catalog yaml-to-json",
    "generator_version": "1.0.0",
    "schema": "definition-v1",
    "source": "universal-agents/api/grafana-loki-skill.yaml",
    "checksum": "ff32c28fce6083442b9d8b251a0939476bc374fbea913544d22438094a1fb284"
  }
}
