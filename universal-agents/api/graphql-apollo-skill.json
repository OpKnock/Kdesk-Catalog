{
  "name": "graphql-apollo",
  "display_name": "Graphql Apollo",
  "category": "api",
  "subcategory": "general",
  "description": "Apollo GraphQL ecosystem: set up Apollo Server and Client, run codegen, and manage the schema registry with the Apollo CLI.",
  "version": "2.0.0",
  "tags": [
    "api"
  ],
  "author": "Kdesk-Catalog",
  "license": "MIT",
  "prerequisites": [
    "node",
    "npm",
    "npx"
  ],
  "capabilities": [
    {
      "name": "apollo-tooling",
      "description": "Scaffold Apollo Server, introspect schemas, and run codegen.",
      "commands": [
        "npm install @apollo/server graphql",
        "npx apollo server:init",
        "curl -s -X POST http://localhost:4000/ -H 'Content-Type: application/json' -d '{\"query\":\"{ __schema { types { name } } }\"}' | jq '.data.__schema.types[0].name'",
        "npx apollo codegen:generate --target=typescript --outputFlat src/__generated__",
        "node -e \"const {ApolloServer}=require('@apollo/server');const s=new ApolloServer({typeDefs:'type Query{hello:String}',resolvers:{Query:{hello:()=>'hi'}}});console.log(s)\""
      ],
      "examples": [
        "curl -s -X POST http://localhost:4000/ -H 'Content-Type: application/json' -d '{\"query\":\"{ __schema { types { name } } }\"}' | jq '.data.__schema.types[0].name'",
        "npx apollo codegen:generate --target=typescript --outputFlat src/__generated__",
        "npm install @apollo/server graphql && node index.js"
      ],
      "parameters": [
        {
          "name": "endpoint",
          "type": "string",
          "description": "GraphQL endpoint URL"
        },
        {
          "name": "target-language",
          "type": "string",
          "description": "codegen target: typescript, swift, etc"
        },
        {
          "name": "output-dir",
          "type": "string",
          "description": "Codegen output directory"
        }
      ]
    }
  ],
  "knowledge": [
    {
      "title": "Apollo Server docs",
      "type": "documentation",
      "source": "https://www.apollographql.com/docs/apollo-server/",
      "description": "Server setup, resolvers, context, and error handling"
    },
    {
      "title": "Apollo Client docs",
      "type": "documentation",
      "source": "https://www.apollographql.com/docs/react/",
      "description": "Client queries, caching, and hooks"
    }
  ],
  "instructions": "# GraphQL Apollo\n\n## What this skill does\n\nThe Apollo ecosystem covers server (Apollo Server), client (Apollo Client), and tooling (codegen, schema registry). This skill covers setup, introspection, and typed codegen.\n\n## When to use\n\n- Standing up a GraphQL server with a polished toolkit\n- Generating typed clients from the schema\n- Introspecting a running endpoint\n\n## Real commands\n\n```bash\n# Server scaffold\nnpm install @apollo/server graphql\nnpx apollo server:init\n\n# Introspect a running endpoint\ncurl -s -X POST http://localhost:4000/ -H 'Content-Type: application/json' -d '{\"query\":\"{ __schema { types { name } } }\"}' | jq '.data.__schema.types[0].name'\n\n# Typed codegen for the client\nnpx apollo codegen:generate --target=typescript --outputFlat src/__generated__\n```\n\n## Server example\n\n```javascript\nconst { ApolloServer } = require('@apollo/server')\nconst { startStandaloneServer } = require('@apollo/server/standalone')\n\nconst server = new ApolloServer({\n  typeDefs: `type Query { hello: String }`,\n  resolvers: { Query: { hello: () => 'world' } }\n})\n\nstartStandaloneServer(server, { listen: { port: 4000 } })\n```\n\n## Testing\n\n```bash\n# Smoke the server before wiring the client\ncurl -s -X POST http://localhost:4000/ -H 'Content-Type: application/json' -d '{\"query\":\"{ hello }\"}' | jq\n```\n\n## Best practices\n\n- Keep resolvers thin; delegate to services.\n- Use context for auth and data source injection.\n- Run codegen against the schema artifact, not a live server, in CI.\n- Enable persisted queries for production caching.\n- Version the schema via the registry before rolling client changes.\n",
  "examples": [
    {
      "input": "Set up Apollo Server quickly",
      "output": "npm install @apollo/server graphql, define typeDefs/resolvers, startStandaloneServer on 4000, and curl the hello query."
    },
    {
      "input": "Generate typed hooks for the React client",
      "output": "Run npx apollo codegen:generate --target=typescript pointing at the schema and generate __generated__ types."
    }
  ],
  "platforms": {
    "claude_code": {
      "tools": [
        "Bash",
        "Read",
        "Write",
        "Edit"
      ],
      "model": "inherit"
    },
    "cursor": {
      "rule_type": "auto"
    },
    "github_copilot": {
      "prompt_file": "graphql-apollo.md",
      "extension": "github.copilot"
    },
    "windsurf": {
      "model": "inherit",
      "tools": [
        "bash",
        "read",
        "write",
        "edit"
      ]
    },
    "opencode": {
      "plugin": "opencode-graphql-apollo"
    },
    "generic": {
      "system_prompt": "You are the Graphql Apollo specialist. You do it through apollo-tooling: Scaffold Apollo Server, introspect schemas, and run codegen. The commands are real, e.g. `npm install @apollo/server graphql`. You run and verify the real CLI commands before you claim anything.",
      "available_tools": [
        "bash",
        "read",
        "write",
        "edit"
      ]
    }
  },
  "created_at": "2024-01-01T00:00:00Z",
  "updated_at": "2024-01-01T00:00:00Z",
  "id": "graphql-apollo",
  "type": "skill",
  "skills": [],
  "tools": [
    "Bash",
    "Read",
    "Write",
    "Edit"
  ],
  "inputs": {
    "parameters": [
      {
        "capability": "apollo-tooling",
        "name": "endpoint",
        "type": "string",
        "description": "GraphQL endpoint URL"
      },
      {
        "capability": "apollo-tooling",
        "name": "target-language",
        "type": "string",
        "description": "codegen target: typescript, swift, etc"
      },
      {
        "capability": "apollo-tooling",
        "name": "output-dir",
        "type": "string",
        "description": "Codegen output directory"
      }
    ]
  },
  "outputs": {},
  "dependencies": [
    "node",
    "npm",
    "npx"
  ],
  "conversion": {
    "tool": "Kdesk-Catalog yaml-to-json",
    "schema": "definition-v1",
    "source_yaml": "api/graphql-apollo-skill.yaml",
    "derived": {
      "skills": "none",
      "tools": "platforms.claude_code.tools",
      "inputs": "capability.parameters",
      "outputs": "none (not present in source YAML)",
      "dependencies": "prerequisites"
    }
  },
  "_provenance": {
    "generated_by": "Kdesk-Catalog yaml-to-json",
    "generator_version": "1.0.0",
    "schema": "definition-v1",
    "source": "universal-agents/api/graphql-apollo-skill.yaml",
    "checksum": "03e5c7ffd9e80508e50f9bb0075f826f60ef08512f7e31357f942d8bf1e30525"
  }
}
