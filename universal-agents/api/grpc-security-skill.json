{
  "name": "grpc-security",
  "display_name": "Grpc Security",
  "category": "api",
  "subcategory": "general",
  "description": "Secure gRPC communication: TLS/mTLS with generated certificates, grpcurl client auth flags, authorization interceptors, and channel credentials in Go.",
  "version": "2.0.0",
  "tags": [
    "api"
  ],
  "author": "Kdesk-Catalog",
  "license": "MIT",
  "prerequisites": [
    "grpcurl",
    "openssl"
  ],
  "capabilities": [
    {
      "name": "grpc-tls-mtls",
      "description": "Configure TLS and mutual TLS for gRPC servers and clients, and verify with grpcurl.",
      "commands": [
        "openssl req -x509 -newkey rsa:2048 -nodes -keyout ca.key -out ca.crt -days 365 -subj \"/CN=my-ca\"",
        "openssl req -newkey rsa:2048 -nodes -keyout server.key -out server.csr -subj \"/CN=localhost\"",
        "grpcurl -cacert ca.crt -cert client.pem -key client.key -servername localhost -authority localhost localhost:50051 list",
        "grpcurl -insecure localhost:50051 list",
        "openssl s_client -connect localhost:50051 -servername localhost -tls1_2 2>/dev/null | grep -E \"Protocol|Cipher\""
      ],
      "examples": [
        "grpcurl -cacert ca.crt localhost:50051 helloworld.Greeter/SayHello -d '{\"name\":\"tls\"}'",
        "grpcurl -insecure -plaintext false localhost:50051 list",
        "openssl x509 -in server.crt -noout -text | grep -A1 \"Subject Alternative\""
      ],
      "parameters": [
        {
          "name": "ca_cert",
          "type": "string",
          "description": "Path to the CA certificate for server verification."
        },
        {
          "name": "client_cert",
          "type": "string",
          "description": "Client certificate for mTLS."
        },
        {
          "name": "client_key",
          "type": "string",
          "description": "Client private key for mTLS."
        }
      ]
    }
  ],
  "knowledge": [
    {
      "title": "gRPC Auth Guide",
      "type": "documentation",
      "source": "https://grpc.io/docs/guides/auth/",
      "description": "Official gRPC authentication guide: TLS, mTLS, and call credentials."
    },
    {
      "title": "grpcurl TLS options",
      "type": "documentation",
      "source": "https://github.com/fullstorydev/grpcurl#using-grpcurl-with-tls",
      "description": "grpcurl TLS and mTLS command-line flags."
    }
  ],
  "instructions": "# gRPC Security\n\nSecure gRPC endpoints with TLS, mutual TLS, and authorization interceptors.\n\n## What this skill does\n\n- Generates CA, server, and client certificates for gRPC TLS.\n- Configures server-side TLS and client channel credentials.\n- Verifies encrypted gRPC connections with grpcurl and openssl s_client.\n- Enforces authz via interceptors on the server.\n\n## When to use\n\n- gRPC traffic crosses network boundaries and must be encrypted.\n- Compliance requires mutual TLS between microservices.\n- Auditing which services speak TLS (openssl s_client check).\n\n## Real commands\n\n```bash\n# Generate a CA and server cert for localhost\nopenssl req -x509 -newkey rsa:2048 -nodes -keyout ca.key -out ca.crt -days 365 -subj \"/CN=my-ca\"\nopenssl req -newkey rsa:2048 -nodes -keyout server.key -out server.csr -subj \"/CN=localhost\"\nopenssl x509 -req -in server.csr -CA ca.crt -CAkey ca.key -CAcreateserial -out server.crt -days 365\n\n# Probe a TLS-enabled server\ngrpcurl -cacert ca.crt -servername localhost localhost:50051 list\n\n# mTLS client\ngrpcurl -cacert ca.crt -cert client.pem -key client.key -servername localhost localhost:50051 list\n\n# Skip verification (dev only)\ngrpcurl -insecure localhost:50051 list\n```\n\n## Go server TLS\n\n```go\ncreds, _ := credentials.NewServerTLSFromFile(\"server.crt\", \"server.key\")\ns := grpc.NewServer(grpc.Creds(creds))\n```\n\n## Go client TLS\n\n```go\ncreds, _ := credentials.NewClientTLSFromFile(\"ca.crt\", \"localhost\")\nconn, _ := grpc.NewClient(\"localhost:50051\", grpc.WithTransportCredentials(creds))\n```\n\n## Authz interceptor\n\n```go\nfunc authzInterceptor(ctx context.Context, req any, info *grpc.UnaryServerInfo, handler grpc.UnaryHandler) (any, error) {\n    md, ok := metadata.FromIncomingContext(ctx)\n    if !ok || len(md.Get(\"authorization\")) == 0 {\n        return nil, status.Error(codes.Unauthenticated, \"missing token\")\n    }\n    return handler(ctx, req)\n}\n```\n\n## Testing\n\n```bash\n# Verify cipher + protocol over the wire\nopenssl s_client -connect localhost:50051 -servername localhost -tls1_2 2>/dev/null | grep -E \"Protocol|Cipher\"\n```\n\n## Best practices\n\n- Never use `-insecure` or insecure credentials outside dev.\n- Set `-servername` to the cert CN to avoid hostname mismatch errors.\n- Rotate CA keys and reissue server certs on a schedule.\n- Use interceptors for authz, never trust the client to be honest.\n\n## Example exchange\n\n```\nUser: grpcurl says \"certificate signed by unknown authority\".\nAgent: Pass the CA bundle: grpcurl -cacert ca.crt -servername localhost localhost:50051 list\n```",
  "examples": [
    {
      "input": "Check if the gRPC port speaks TLS",
      "output": "openssl s_client -connect localhost:50051 -servername localhost -tls1_2 2>/dev/null | grep -E \"Protocol|Cipher\""
    },
    {
      "input": "Call a mTLS-protected service",
      "output": "grpcurl -cacert ca.crt -cert client.pem -key client.key -servername localhost localhost:50051 list"
    }
  ],
  "platforms": {
    "claude_code": {
      "tools": [
        "Bash",
        "Read",
        "Write",
        "Edit"
      ],
      "model": "inherit"
    },
    "cursor": {
      "rule_type": "auto"
    },
    "github_copilot": {
      "prompt_file": "grpc-security.md",
      "extension": "github.copilot"
    },
    "windsurf": {
      "model": "inherit",
      "tools": [
        "bash",
        "read",
        "write",
        "edit"
      ]
    },
    "opencode": {
      "plugin": "opencode-grpc-security"
    },
    "generic": {
      "system_prompt": "Your lane is Grpc Security. You do it through grpc-tls-mtls: Configure TLS and mutual TLS for gRPC servers and clients, and verify with grpcurl. The commands are real, e.g. `openssl req -x509 -newkey rsa:2048 -nodes -keyout ca.key -out ca.crt -days 365 -subj...`. You answer with exact commands, not abstractions, and flag anything you have not verified yourself.",
      "available_tools": [
        "bash",
        "read",
        "write",
        "edit"
      ]
    }
  },
  "created_at": "2024-01-01T00:00:00Z",
  "updated_at": "2024-01-01T00:00:00Z",
  "id": "grpc-security",
  "type": "skill",
  "skills": [],
  "tools": [
    "Bash",
    "Read",
    "Write",
    "Edit"
  ],
  "inputs": {
    "parameters": [
      {
        "capability": "grpc-tls-mtls",
        "name": "ca_cert",
        "type": "string",
        "description": "Path to the CA certificate for server verification."
      },
      {
        "capability": "grpc-tls-mtls",
        "name": "client_cert",
        "type": "string",
        "description": "Client certificate for mTLS."
      },
      {
        "capability": "grpc-tls-mtls",
        "name": "client_key",
        "type": "string",
        "description": "Client private key for mTLS."
      }
    ]
  },
  "outputs": {},
  "dependencies": [
    "grpcurl",
    "openssl"
  ],
  "conversion": {
    "tool": "Kdesk-Catalog yaml-to-json",
    "schema": "definition-v1",
    "source_yaml": "api/grpc-security-skill.yaml",
    "derived": {
      "skills": "none",
      "tools": "platforms.claude_code.tools",
      "inputs": "capability.parameters",
      "outputs": "none (not present in source YAML)",
      "dependencies": "prerequisites"
    }
  },
  "_provenance": {
    "generated_by": "Kdesk-Catalog yaml-to-json",
    "generator_version": "1.0.0",
    "schema": "definition-v1",
    "source": "universal-agents/api/grpc-security-skill.yaml",
    "checksum": "2180298c6d395b7b6cff1066bd47d1439bfc1576c3443fe088ac02396559c717"
  }
}
