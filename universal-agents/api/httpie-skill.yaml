name: httpie
display_name: Httpie
category: api
subcategory: general
description: 'HTTPie CLI: expressive HTTP requests with intuitive syntax, JSON bodies, auth flags, file
  downloads, and streaming responses.'
version: 2.0.0
tags:
- api
author: Kdesk-Catalog
license: MIT
prerequisites:
- http
capabilities:
- name: httpie-requests
  description: Compose and send HTTP requests with HTTPie's shorthand syntax.
  commands:
  - http http://localhost:8000/api/users
  - http POST http://localhost:8000/api/users name=John age:=30
  - http -a user:pass http://localhost:8000/protected
  - http --stream http://localhost:8000/events
  - http -d https://example.com/file.zip
  examples:
  - http GET http://localhost:8000/api/users/42 Accept:application/json
  - http PUT http://localhost:8000/api/users/42 name=Jane
  - http -f POST http://localhost:8000/upload file@photo.png
  parameters:
  - name: method
    type: string
    description: HTTP method, defaults to GET.
  - name: url
    type: string
    description: Target URL.
  - name: headers
    type: string
    description: Header shorthand, e.g. Accept:application/json.
knowledge:
- title: HTTPie Docs
  type: documentation
  source: https://httpie.io/docs
  description: 'Official HTTPie documentation: CLI reference and usage examples.'
- title: HTTPie CLI GitHub
  type: documentation
  source: https://github.com/httpie/cli
  description: HTTPie CLI repository with README examples.
instructions: '# HTTPie


  Human-friendly HTTP requests from the terminal.


  ## What this skill does


  - Sends HTTP requests with intuitive `key=value` and `key:=json` syntax.

  - Handles JSON bodies, file uploads, and downloads.

  - Streams responses with --stream.

  - Shows colorized request/response pairs by default.


  ## When to use


  - Everyday API debugging where curl is too terse.

  - Demonstrating API calls in docs or demos.

  - Quick checks of headers and status codes.


  ## Real commands


  ```bash

  # Simple GET

  http http://localhost:8000/api/users


  # POST JSON body

  http POST http://localhost:8000/api/users name=John age:=30


  # Explicit header

  http GET http://localhost:8000/api/users/42 Accept:application/json


  # Basic auth

  http -a user:pass http://localhost:8000/protected


  # Stream SSE-style responses

  http --stream http://localhost:8000/events


  # Download a file

  http -d https://example.com/file.zip


  # File upload

  http -f POST http://localhost:8000/upload file@photo.png


  # Raw JSON body

  http POST http://localhost:8000/api/users <<< ''{"name": "John"}''

  ```


  ## Syntax cheat sheet


  - `name=John` sends a string field.

  - `age:=30` sends a raw JSON value.

  - `Accept:application/json` sends a header.

  - `file@photo.png` attaches a file (with -f for forms).


  ## Testing


  ```bash

  http -h http://localhost:8000/health  # headers only

  ```


  ## Best practices


  - Use `-h`/`-b` to show only headers or only body in scripts.

  - Combine with jq for assertions: `http -b ... | jq .id`.

  - Use `--check-status` to fail on 4xx/5xx in CI.

  - Prefer `key:=` for numbers/booleans to avoid string coercion.


  ## Example exchange


  ```

  User: Create a user with JSON fields name and age.

  Agent: http POST http://localhost:8000/api/users name=John age:=30

  ```'
examples:
- input: GET /api/users with JSON accept header
  output: http GET http://localhost:8000/api/users Accept:application/json
- input: POST a user with typed fields
  output: http POST http://localhost:8000/api/users name=John age:=30
platforms:
  claude_code:
    tools:
    - Bash
    - Read
    - Write
    - Edit
    model: inherit
  cursor:
    rule_type: auto
  github_copilot:
    prompt_file: httpie.md
    extension: github.copilot
  windsurf:
    model: inherit
    tools:
    - bash
    - read
    - write
    - edit
  opencode:
    plugin: opencode-httpie
  generic:
    system_prompt: 'You work Httpie hands-on. You do it through httpie-requests: Compose and send HTTP
      requests with HTTPies shorthand syntax. The commands are real, e.g. `http http://localhost:8000/api/users`.
      You prefer the shortest safe path, and you reproduce failures before diagnosing them.'
    available_tools:
    - bash
    - read
    - write
    - edit
created_at: '2024-01-01T00:00:00Z'
updated_at: '2024-01-01T00:00:00Z'
