{
  "name": "identity-provider",
  "display_name": "Identity Provider",
  "category": "api",
  "subcategory": "general",
  "description": "Identity provider operations with Keycloak: kcadm.sh realm and user administration, OIDC discovery, client registration, and token introspection.",
  "version": "2.0.0",
  "tags": [
    "api"
  ],
  "author": "Kdesk-Catalog",
  "license": "MIT",
  "prerequisites": [
    "kcadm.sh"
  ],
  "capabilities": [
    {
      "name": "keycloak-admin",
      "description": "Administer Keycloak realms, users, and clients from the CLI.",
      "commands": [
        "kcadm.sh config credentials --server http://localhost:8080 --realm master --user admin --password admin",
        "kcadm.sh get realms",
        "kcadm.sh create users -r myrealm -s username=alice -s enabled=true -p",
        "kcadm.sh get clients -r myrealm",
        "curl http://localhost:8080/realms/myrealm/.well-known/openid-configuration"
      ],
      "examples": [
        "kcadm.sh create realms -s realm=myrealm -s enabled=true",
        "kcadm.sh update users/6c1d -r myrealm -s 'email=alice@example.com'",
        "kcadm.sh get users -r myrealm --query email=alice@example.com"
      ],
      "parameters": [
        {
          "name": "realm",
          "type": "string",
          "description": "Keycloak realm name."
        },
        {
          "name": "server_url",
          "type": "string",
          "description": "Keycloak base URL, e.g. http://localhost:8080."
        },
        {
          "name": "admin_user",
          "type": "string",
          "description": "Admin username for token acquisition."
        }
      ]
    }
  ],
  "knowledge": [
    {
      "title": "Keycloak Server Admin Guide",
      "type": "documentation",
      "source": "https://www.keycloak.org/docs/latest/server_admin/",
      "description": "Keycloak administration: realms, users, clients, roles."
    },
    {
      "title": "OpenID Connect Core",
      "type": "documentation",
      "source": "https://openid.net/specs/openid-connect-core-1_0.html",
      "description": "OIDC specification: discovery, authorization code flow, and userinfo."
    }
  ],
  "instructions": "# Identity Provider\n\nRun and administer a Keycloak-based identity provider.\n\n## What this skill does\n\n- Configures kcadm.sh credentials and manages realms.\n- Creates and queries users and clients.\n- Verifies OIDC discovery metadata.\n- Introspects tokens for debugging.\n\n## When to use\n\n- Standing up SSO for a new environment.\n- Provisioning a test user or client programmatically.\n- Debugging OIDC flows against a real IdP.\n\n## Real commands\n\n```bash\n# Login once per session\nkcadm.sh config credentials --server http://localhost:8080 --realm master --user admin --password admin\n\n# Realms\nkcadm.sh get realms\nkcadm.sh create realms -s realm=myrealm -s enabled=true\n\n# Users\nkcadm.sh create users -r myrealm -s username=alice -s enabled=true -p\nkcadm.sh get users -r myrealm --query email=alice@example.com\nkcadm.sh update users/<id> -r myrealm -s 'email=alice@example.com'\n\n# Clients\nkcadm.sh get clients -r myrealm\nkcadm.sh create clients -r myrealm -s clientId=webapp -s publicClient=true\n\n# OIDC discovery\ncurl http://localhost:8080/realms/myrealm/.well-known/openid-configuration | jq '.authorization_endpoint, .token_endpoint'\n```\n\n## Token exchange for testing\n\n```bash\nTOKEN=$(curl -s -X POST http://localhost:8080/realms/myrealm/protocol/openid-connect/token \\\n  -d client_id=webapp -d username=alice -d password=pass -d grant_type=password | jq -r .access_token)\ncurl -s http://localhost:8080/realms/myrealm/protocol/openid-connect/userinfo -H \"Authorization: Bearer $TOKEN\"\n```\n\n## Testing\n\n```bash\nkcadm.sh get realms -r myrealm | jq '.realm, .enabled'\n```\n\n## Best practices\n\n- Never use password grant in production; use authorization code + PKCE.\n- Scope kcadm.sh tokens to a service account, not the master admin.\n- Version realm config: export with `kcadm.sh get realms -r myrealm --export` for drift checks.\n- Rotate client secrets and revoke tokens via `kcadm.sh logout` when needed.\n\n## Example exchange\n\n```\nUser: Create realm myrealm with user alice.\nAgent: kcadm.sh config credentials ... ; kcadm.sh create realms -s realm=myrealm -s enabled=true\n       kcadm.sh create users -r myrealm -s username=alice -s enabled=true -p\n```",
  "examples": [
    {
      "input": "List all realms",
      "output": "kcadm.sh config credentials --server http://localhost:8080 --realm master --user admin --password admin && kcadm.sh get realms"
    },
    {
      "input": "Fetch OIDC discovery metadata",
      "output": "curl http://localhost:8080/realms/myrealm/.well-known/openid-configuration | jq ."
    }
  ],
  "platforms": {
    "claude_code": {
      "tools": [
        "Bash",
        "Read",
        "Write",
        "Edit"
      ],
      "model": "inherit"
    },
    "cursor": {
      "rule_type": "auto"
    },
    "github_copilot": {
      "prompt_file": "identity-provider.md",
      "extension": "github.copilot"
    },
    "windsurf": {
      "model": "inherit",
      "tools": [
        "bash",
        "read",
        "write",
        "edit"
      ]
    },
    "opencode": {
      "plugin": "opencode-identity-provider"
    },
    "generic": {
      "system_prompt": "You are the Identity Provider specialist. You do it through keycloak-admin: Administer Keycloak realms, users, and clients from the CLI. The commands are real, e.g. `kcadm.sh config credentials --server http://localhost:8080 --realm master --user admin...`. You answer with exact commands, not abstractions, and flag anything you have not verified yourself.",
      "available_tools": [
        "bash",
        "read",
        "write",
        "edit"
      ]
    }
  },
  "created_at": "2024-01-01T00:00:00Z",
  "updated_at": "2024-01-01T00:00:00Z",
  "id": "identity-provider",
  "type": "skill",
  "skills": [],
  "tools": [
    "Bash",
    "Read",
    "Write",
    "Edit"
  ],
  "inputs": {
    "parameters": [
      {
        "capability": "keycloak-admin",
        "name": "realm",
        "type": "string",
        "description": "Keycloak realm name."
      },
      {
        "capability": "keycloak-admin",
        "name": "server_url",
        "type": "string",
        "description": "Keycloak base URL, e.g. http://localhost:8080."
      },
      {
        "capability": "keycloak-admin",
        "name": "admin_user",
        "type": "string",
        "description": "Admin username for token acquisition."
      }
    ]
  },
  "outputs": {},
  "dependencies": [
    "kcadm.sh"
  ],
  "conversion": {
    "tool": "Kdesk-Catalog yaml-to-json",
    "schema": "definition-v1",
    "source_yaml": "api/identity-provider-skill.yaml",
    "derived": {
      "skills": "none",
      "tools": "platforms.claude_code.tools",
      "inputs": "capability.parameters",
      "outputs": "none (not present in source YAML)",
      "dependencies": "prerequisites"
    }
  },
  "_provenance": {
    "generated_by": "Kdesk-Catalog yaml-to-json",
    "generator_version": "1.0.0",
    "schema": "definition-v1",
    "source": "universal-agents/api/identity-provider-skill.yaml",
    "checksum": "fe92dff8b5baf916c644f301cce8ab02966263a647a82d5cbdf635c223419cb0"
  }
}
