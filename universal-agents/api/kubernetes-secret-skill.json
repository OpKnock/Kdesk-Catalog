{
  "name": "kubernetes-secret",
  "display_name": "Kubernetes Secret",
  "category": "api",
  "subcategory": "general",
  "description": "Manage Kubernetes Secrets: create from literals/files, TLS secrets, reference in pods, and decode safely for audits.",
  "version": "2.0.0",
  "tags": [
    "api"
  ],
  "author": "Kdesk-Catalog",
  "license": "MIT",
  "prerequisites": [
    "kubectl"
  ],
  "capabilities": [
    {
      "name": "secret-create",
      "description": "Create generic and TLS secrets.",
      "commands": [
        "kubectl create secret generic db-creds --from-literal=DB_USER=admin --from-literal=DB_PASSWORD=s3cret",
        "kubectl create secret generic app-secrets --from-file=tls.crt=cert.pem --from-file=tls.key=key.pem",
        "kubectl create secret tls my-tls --cert=cert.pem --key=key.pem",
        "kubectl create secret generic app-secrets --from-env-file=secrets.env"
      ],
      "examples": [
        "kubectl create secret generic db-creds --from-literal=DB_USER=admin --from-literal=DB_PASSWORD=s3cret",
        "kubectl create secret tls my-tls --cert=cert.pem --key=key.pem",
        "kubectl create secret generic app-secrets --from-env-file=secrets.env"
      ],
      "parameters": [
        {
          "name": "name",
          "type": "string",
          "description": "Secret name."
        },
        {
          "name": "from_literal",
          "type": "string",
          "description": "key=value literals."
        },
        {
          "name": "cert",
          "type": "string",
          "description": "TLS certificate file."
        },
        {
          "name": "key",
          "type": "string",
          "description": "TLS private key file."
        }
      ]
    },
    {
      "name": "secret-ops",
      "description": "List, decode, and reference secrets in workloads.",
      "commands": [
        "kubectl get secrets",
        "kubectl get secret db-creds -o jsonpath='{.data.DB_PASSWORD}' | base64 -d",
        "kubectl describe secret db-creds",
        "kubectl delete secret db-creds",
        "kubectl get secret my-tls -o yaml"
      ],
      "examples": [
        "kubectl get secret db-creds -o jsonpath='{.data.DB_PASSWORD}' | base64 -d",
        "kubectl get secrets",
        "kubectl describe secret db-creds"
      ],
      "parameters": [
        {
          "name": "name",
          "type": "string",
          "description": "Secret name."
        },
        {
          "name": "key",
          "type": "string",
          "description": "Secret key to decode."
        }
      ]
    }
  ],
  "knowledge": [
    {
      "title": "Kubernetes Secrets",
      "type": "documentation",
      "source": "https://kubernetes.io/docs/concepts/configuration/secret/",
      "description": "Official Secrets concept documentation."
    },
    {
      "title": "Distribute Credentials Securely",
      "type": "documentation",
      "source": "https://kubernetes.io/docs/tasks/inject-data-application/distribute-credentials-secure/",
      "description": "Using secrets in pods."
    }
  ],
  "instructions": "# Kubernetes Secrets\n\nStore and inject sensitive data into Kubernetes workloads.\n\n## What this skill does\n\n- Creates generic and TLS secrets from CLI input or files.\n- Injects secrets as env vars or mounted files.\n- Decodes values safely for audits.\n\n## When to use\n\n- DB credentials, API keys, and tokens for workloads.\n- TLS cert/key pairs for ingress and service TLS.\n- Keeping secrets out of images and git.\n\n## Real commands\n\n```bash\n# Generic secret\nkubectl create secret generic db-creds \\\n  --from-literal=DB_USER=admin --from-literal=DB_PASSWORD=s3cret\n\n# From env file\nkubectl create secret generic app-secrets --from-env-file=secrets.env\n\n# TLS secret for ingress\nkubectl create secret tls my-tls --cert=cert.pem --key=key.pem\n\n# List\nkubectl get secrets\n\n# Decode a value (base64 in etcd/API)\nkubectl get secret db-creds -o jsonpath='{.data.DB_PASSWORD}' | base64 -d\n\n# Describe shows keys only, not values\nkubectl describe secret db-creds\n```\n\n## Pod reference example\n\n```yaml\nspec:\n  containers:\n    - name: app\n      image: myapp:1.2\n      env:\n        - name: DB_PASSWORD\n          valueFrom:\n            secretKeyRef:\n              name: db-creds\n              key: DB_PASSWORD\n      volumeMounts:\n        - name: tls\n          mountPath: /etc/tls\n          readOnly: true\n  volumes:\n    - name: tls\n      secret:\n        secretName: my-tls\n```\n\n## Testing\n\n```bash\nkubectl exec deploy/app -- env | grep DB_PASSWORD\nkubectl exec deploy/app -- cat /etc/tls/tls.crt\n```\n\n## Best practices\n\n- Values are base64, not encrypted; use encryption at rest (KMS) or ExternalSecrets.\n- Prefer mounted secrets over env vars for large or rotating values.\n- Never commit secret manifests; generate them in pipelines.\n",
  "examples": [
    {
      "input": "Store DB credentials for my pod",
      "output": "kubectl create secret generic db-creds --from-literal=DB_USER=admin --from-literal=DB_PASSWORD=s3cret, then reference via secretKeyRef."
    },
    {
      "input": "Check what a secret contains",
      "output": "kubectl get secret db-creds -o jsonpath='{.data.DB_PASSWORD}' | base64 -d"
    }
  ],
  "platforms": {
    "claude_code": {
      "tools": [
        "Bash",
        "Read",
        "Write",
        "Edit"
      ],
      "model": "inherit"
    },
    "cursor": {
      "rule_type": "auto"
    },
    "github_copilot": {
      "prompt_file": "kubernetes-secret.md",
      "extension": "github.copilot"
    },
    "windsurf": {
      "model": "inherit",
      "tools": [
        "bash",
        "read",
        "write",
        "edit"
      ]
    },
    "opencode": {
      "plugin": "opencode-kubernetes-secret"
    },
    "generic": {
      "system_prompt": "You take the Kubernetes Secret seat. You do it through secret-create: Create generic and TLS secrets. The commands are real, e.g. `kubectl create secret generic db-creds --from-literal=DB_USER=admin...`. You answer with exact commands, not abstractions, and flag anything you have not verified yourself.",
      "available_tools": [
        "bash",
        "read",
        "write",
        "edit"
      ]
    }
  },
  "created_at": "2024-01-01T00:00:00Z",
  "updated_at": "2024-01-01T00:00:00Z",
  "id": "kubernetes-secret",
  "type": "skill",
  "skills": [],
  "tools": [
    "Bash",
    "Read",
    "Write",
    "Edit"
  ],
  "inputs": {
    "parameters": [
      {
        "capability": "secret-create",
        "name": "name",
        "type": "string",
        "description": "Secret name."
      },
      {
        "capability": "secret-create",
        "name": "from_literal",
        "type": "string",
        "description": "key=value literals."
      },
      {
        "capability": "secret-create",
        "name": "cert",
        "type": "string",
        "description": "TLS certificate file."
      },
      {
        "capability": "secret-create",
        "name": "key",
        "type": "string",
        "description": "TLS private key file."
      },
      {
        "capability": "secret-ops",
        "name": "name",
        "type": "string",
        "description": "Secret name."
      },
      {
        "capability": "secret-ops",
        "name": "key",
        "type": "string",
        "description": "Secret key to decode."
      }
    ]
  },
  "outputs": {},
  "dependencies": [
    "kubectl"
  ],
  "conversion": {
    "tool": "Kdesk-Catalog yaml-to-json",
    "schema": "definition-v1",
    "source_yaml": "api/kubernetes-secret-skill.yaml",
    "derived": {
      "skills": "none",
      "tools": "platforms.claude_code.tools",
      "inputs": "capability.parameters",
      "outputs": "none (not present in source YAML)",
      "dependencies": "prerequisites"
    }
  },
  "_provenance": {
    "generated_by": "Kdesk-Catalog yaml-to-json",
    "generator_version": "1.0.0",
    "schema": "definition-v1",
    "source": "universal-agents/api/kubernetes-secret-skill.yaml",
    "checksum": "e739571aedbe288c4a0e233896f8c4f8e836a052cfbfaf24c4ead7c95d630a4c"
  }
}
