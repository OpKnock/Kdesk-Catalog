{
  "name": "logging-elasticsearch-ops",
  "display_name": "Logging Elasticsearch Ops",
  "category": "api",
  "subcategory": "general",
  "description": "Centralized logging with the ELK stack: Elasticsearch index setup, Filebeat shipping, Logstash pipelines, and querying logs with curl.",
  "version": "2.0.0",
  "tags": [
    "api"
  ],
  "author": "Kdesk-Catalog",
  "license": "MIT",
  "prerequisites": [
    "filebeat",
    "logstash"
  ],
  "capabilities": [
    {
      "name": "elasticsearch-ops",
      "description": "Create indices and query log documents in Elasticsearch.",
      "commands": [
        "curl -s -X PUT 'localhost:9200/app-logs?pretty' -H 'Content-Type: application/json'",
        "curl -s 'localhost:9200/app-logs/_search?q=level:ERROR&size=10' | jq '.hits.hits[]._source'",
        "curl -s 'localhost:9200/_cat/indices?v' | grep app-logs",
        "curl -s -X POST 'localhost:9200/app-logs/_doc' -H 'Content-Type: application/json' -d '{\"level\":\"info\",\"msg\":\"smoke test\"}'"
      ],
      "examples": [
        "curl -s -X PUT 'localhost:9200/app-logs?pretty' -H 'Content-Type: application/json'",
        "curl -s 'localhost:9200/app-logs/_search?q=level:ERROR&size=10' | jq '.hits.hits[]._source'",
        "curl -s 'localhost:9200/_cat/indices?v' | grep app-logs"
      ],
      "parameters": [
        {
          "name": "index",
          "type": "string",
          "description": "Elasticsearch index name."
        },
        {
          "name": "query",
          "type": "string",
          "description": "Lucene query string, e.g. level:ERROR."
        }
      ]
    },
    {
      "name": "ship-process",
      "description": "Ship logs with Filebeat and process with Logstash pipelines.",
      "commands": [
        "filebeat -e -c filebeat.yml",
        "filebeat test config -c filebeat.yml",
        "filebeat test output -c filebeat.yml",
        "logstash -f logstash.conf",
        "logstash --config.test_and_exit -f logstash.conf"
      ],
      "examples": [
        "filebeat -e -c filebeat.yml",
        "filebeat test output -c filebeat.yml",
        "logstash --config.test_and_exit -f logstash.conf"
      ],
      "parameters": [
        {
          "name": "config",
          "type": "string",
          "description": "Filebeat or Logstash config file."
        }
      ]
    }
  ],
  "knowledge": [
    {
      "title": "Elasticsearch Query DSL",
      "type": "documentation",
      "source": "https://www.elastic.co/guide/en/elasticsearch/reference/current/query-dsl.html",
      "description": "Elasticsearch query reference."
    },
    {
      "title": "Filebeat Docs",
      "type": "documentation",
      "source": "https://www.elastic.co/guide/en/beats/filebeat/current/",
      "description": "Official Filebeat documentation."
    },
    {
      "title": "Logstash Docs",
      "type": "documentation",
      "source": "https://www.elastic.co/guide/en/logstash/current/",
      "description": "Official Logstash documentation."
    }
  ],
  "instructions": "# Logging v2 (ELK Stack)\n\nCentralized log analysis with Elasticsearch, Filebeat, and Logstash.\n\n## What this skill does\n\n- Creates Elasticsearch indices for log data.\n- Ships files with Filebeat and processes with Logstash.\n- Queries and aggregates logs via the ES REST API.\n\n## When to use\n\n- Organization-wide log search and retention.\n- Auditing error rates by service and timeframe.\n- Feeding logs into Kibana dashboards.\n\n## Real commands\n\n```bash\n# Create the log index\ncurl -s -X PUT 'localhost:9200/app-logs?pretty' \\\n  -H 'Content-Type: application/json'\n\n# Search errors\ncurl -s 'localhost:9200/app-logs/_search?q=level:ERROR&size=10' \\\n  | jq '.hits.hits[]._source'\n\n# Index health\ncurl -s 'localhost:9200/_cat/indices?v' | grep app-logs\n\n# Ingest a document directly\ncurl -s -X POST 'localhost:9200/app-logs/_doc' \\\n  -H 'Content-Type: application/json' \\\n  -d '{\"level\":\"info\",\"msg\":\"smoke test\"}'\n\n# Filebeat: validate config and output\nfilebeat test config -c filebeat.yml\nfilebeat test output -c filebeat.yml\n\n# Filebeat: run\nfilebeat -e -c filebeat.yml\n\n# Logstash: validate then run\nlogstash --config.test_and_exit -f logstash.conf\nlogstash -f logstash.conf\n```\n\n## filebeat.yml example\n\n```yaml\nfilebeat.inputs:\n  - type: filestream\n    id: app-logs\n    paths:\n      - /var/log/app/*.log\noutput.elasticsearch:\n  hosts: [\"localhost:9200\"]\n  index: \"app-logs\"\n```\n\n## logstash.conf example\n\n```\ninput { beats { port => 5044 } }\nfilter {\n  if [message] =~ /^\\{/ {\n    json { source => \"message\" }\n  }\n}\noutput { elasticsearch { hosts => [\"localhost:9200\"] index => \"app-logs\" } }\n```\n\n## Testing\n\n```bash\necho '{\"level\":\"error\",\"msg\":\"test\"}' | nc localhost 5044\ncurl -s 'localhost:9200/app-logs/_count?q=level:error'\n```\n\n## Best practices\n\n- Use index lifecycle management (ILM) for retention and rollover.\n- Send everything to one pipeline; parse JSON as early as possible.\n- Test configs with --config.test_and_exit / filebeat test before deploy.\n",
  "examples": [
    {
      "input": "How many errors today?",
      "output": "curl -s 'localhost:9200/app-logs/_count?q=level:ERROR' | jq '.count'"
    },
    {
      "input": "Validate my filebeat config",
      "output": "filebeat test config -c filebeat.yml && filebeat test output -c filebeat.yml"
    }
  ],
  "platforms": {
    "claude_code": {
      "tools": [
        "Bash",
        "Read",
        "Write",
        "Edit"
      ],
      "model": "inherit"
    },
    "cursor": {
      "rule_type": "auto"
    },
    "github_copilot": {
      "prompt_file": "logging-elasticsearch-ops.md",
      "extension": "github.copilot"
    },
    "windsurf": {
      "model": "inherit",
      "tools": [
        "bash",
        "read",
        "write",
        "edit"
      ]
    },
    "opencode": {
      "plugin": "opencode-logging-elasticsearch-ops"
    },
    "generic": {
      "system_prompt": "This is your domain: Logging Elasticsearch Ops. You do it through elasticsearch-ops: Create indices and query log documents in Elasticsearch. The commands are real, e.g. `curl -s -X PUT localhost:9200/app-logs?pretty -H Content-Type: application/json`. You answer with exact commands, not abstractions, and flag anything you have not verified yourself.",
      "available_tools": [
        "bash",
        "read",
        "write",
        "edit"
      ]
    }
  },
  "created_at": "2024-01-01T00:00:00Z",
  "updated_at": "2024-01-01T00:00:00Z",
  "id": "logging-elasticsearch-ops",
  "type": "skill",
  "skills": [],
  "tools": [
    "Bash",
    "Read",
    "Write",
    "Edit"
  ],
  "inputs": {
    "parameters": [
      {
        "capability": "elasticsearch-ops",
        "name": "index",
        "type": "string",
        "description": "Elasticsearch index name."
      },
      {
        "capability": "elasticsearch-ops",
        "name": "query",
        "type": "string",
        "description": "Lucene query string, e.g. level:ERROR."
      },
      {
        "capability": "ship-process",
        "name": "config",
        "type": "string",
        "description": "Filebeat or Logstash config file."
      }
    ]
  },
  "outputs": {},
  "dependencies": [
    "filebeat",
    "logstash"
  ],
  "conversion": {
    "tool": "Kdesk-Catalog yaml-to-json",
    "schema": "definition-v1",
    "source_yaml": "api/logging-elasticsearch-ops-skill.yaml",
    "derived": {
      "skills": "none",
      "tools": "platforms.claude_code.tools",
      "inputs": "capability.parameters",
      "outputs": "none (not present in source YAML)",
      "dependencies": "prerequisites"
    }
  },
  "_provenance": {
    "generated_by": "Kdesk-Catalog yaml-to-json",
    "generator_version": "1.0.0",
    "schema": "definition-v1",
    "source": "universal-agents/api/logging-elasticsearch-ops-skill.yaml",
    "checksum": "e4452e85e46e2b1ed512f0c44f00ce621360e7c404d449003e12441aecfabf29"
  }
}
