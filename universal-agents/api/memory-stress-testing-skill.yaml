name: memory-stress-testing
display_name: Memory Stress Testing
category: api
subcategory: general
description: 'Stress test memory on Linux hosts: stress-ng and memtester workloads, OOM behavior checks,
  and system memory monitoring.'
version: 2.0.0
tags:
- api
author: Kdesk-Catalog
license: MIT
prerequisites:
- cat
- free
- memtester
- stress
- stress-ng
- vmstat
capabilities:
- name: stress-ng
  description: Run memory stress workloads with stress-ng.
  commands:
  - stress-ng --vm 4 --vm-bytes 2G --timeout 60s
  - stress-ng --vm 2 --vm-bytes 75% --vm-method all --timeout 30s
  - stress-ng --vm 1 --vm-bytes 1G --vm-hang 10 --timeout 60s
  - stress-ng --vm 4 --vm-bytes 2G --metrics-brief --timeout 60s
  examples:
  - stress-ng --vm 4 --vm-bytes 2G --timeout 60s
  - stress-ng --vm 2 --vm-bytes 75% --vm-method all --timeout 30s
  - stress-ng --vm 4 --vm-bytes 2G --metrics-brief --timeout 60s
  parameters:
  - name: vm
    type: integer
    description: Number of memory stressors.
  - name: vm_bytes
    type: string
    description: 'Bytes per stressor: 2G or 75%.'
  - name: timeout
    type: string
    description: Run duration, e.g. 60s.
- name: memtester-monitor
  description: Run memtester and monitor memory pressure.
  commands:
  - memtester 512M 5
  - free -h
  - vmstat 1 10
  - cat /proc/meminfo | head -8
  - stress --vm 2 --vm-bytes 1G --vm-hang 0 --timeout 120
  examples:
  - memtester 512M 5
  - free -h
  - vmstat 1 10
  parameters:
  - name: size
    type: string
    description: Memory size to test, e.g. 512M.
  - name: iterations
    type: integer
    description: memtester iterations.
knowledge:
- title: stress-ng
  type: documentation
  source: https://github.com/ColinIanKing/stress-ng
  description: stress-ng repository with all stressor options.
- title: memtester
  type: documentation
  source: https://pyropus.ca/software/memtester/
  description: memtester documentation.
instructions: '# Memory Stress Testing


  Stress and validate memory subsystems on Linux hosts.


  ## What this skill does


  - Runs memory stressors with stress-ng (allocation, dirtying, hang).

  - Tests RAM correctness with memtester.

  - Monitors pressure with free, vmstat, and /proc/meminfo.


  ## When to use


  - Validating VM sizing before production load.

  - Hunting OOM-killer surprises under high allocation.

  - Verifying memory cgroup limits behave.


  ## Real commands


  ```bash

  # 4 stressors, 2G each, 60s

  stress-ng --vm 4 --vm-bytes 2G --timeout 60s


  # 75% of available RAM, all methods

  stress-ng --vm 2 --vm-bytes 75% --vm-method all --timeout 30s


  # Allocate and hold (swap-test)

  stress-ng --vm 1 --vm-bytes 1G --vm-hang 10 --timeout 60s


  # With metrics summary

  stress-ng --vm 4 --vm-bytes 2G --metrics-brief --timeout 60s


  # RAM correctness test (512 MiB, 5 passes)

  memtester 512M 5


  # Classic stress

  stress --vm 2 --vm-bytes 1G --vm-hang 0 --timeout 120


  # Monitor

  free -h

  vmstat 1 10

  cat /proc/meminfo | head -8

  ```


  ## Testing


  ```bash

  # Watch swap and OOM behavior while stressing

  stress-ng --vm 4 --vm-bytes 2G --timeout 60s &

  vmstat 1 10

  dmesg --level=err | grep -i ''out of memory'' || echo ''no OOM''

  ```


  ## Best practices


  - Never stress memory on production boxes without change control.

  - Use --vm-method all for thorough fault coverage in staging.

  - Combine with swap limits (ulimit -v) to test graceful degradation.

  '
examples:
- input: Verify the box survives 8G of allocation
  output: stress-ng --vm 4 --vm-bytes 2G --timeout 60s and watch free -h plus dmesg for OOM.
- input: Check RAM for bad sectors
  output: memtester 512M 5
platforms:
  claude_code:
    tools:
    - Bash
    - Read
    - Write
    - Edit
    model: inherit
  cursor:
    rule_type: auto
  github_copilot:
    prompt_file: memory-stress-testing.md
    extension: github.copilot
  windsurf:
    model: inherit
    tools:
    - bash
    - read
    - write
    - edit
  opencode:
    plugin: opencode-memory-stress-testing
  generic:
    system_prompt: 'You own Memory Stress Testing end to end. You do it through stress-ng: Run memory
      stress workloads with stress-ng. The commands are real, e.g. `stress-ng --vm 4 --vm-bytes 2G --timeout
      60s`. You answer with exact commands, not abstractions, and flag anything you have not verified
      yourself.'
    available_tools:
    - bash
    - read
    - write
    - edit
created_at: '2024-01-01T00:00:00Z'
updated_at: '2024-01-01T00:00:00Z'
