name: mongodb-sharding
display_name: Mongodb Sharding
category: api
subcategory: general
description: 'Deploy and manage MongoDB sharded clusters: config servers, mongos routers, shard collections
  and balanced data.'
version: 2.0.0
tags:
- api
author: Mehul Wagde
license: MIT
prerequisites:
- mongod
- mongos
- mongosh
capabilities:
- name: sharded-cluster-operations
  description: Configure shardsvr mongods, start mongos, enable sharding and manage chunk distribution.
  commands:
  - mongod --shardsvr --replSet shard1 --dbpath /data/shard1 --port 27018
  - mongos --configdb cfgReplSet/cfg1:27019,cfg2:27019,cfg3:27019 --bind_ip 0.0.0.0 --port 27017
  - mongosh --eval "sh.enableSharding('appdb')"
  - 'mongosh --eval "sh.shardCollection(''appdb.orders'', {customer_id: ''hashed''})"'
  - mongosh --eval "sh.status()"
  examples:
  - 'mongosh --eval "sh.shardCollection(''appdb.orders'', {_id: ''hashed''})"'
  - 'mongosh --eval "sh.moveChunk(''appdb.orders'', {customer_id: MinKey}, ''shard1'')"'
  - mongosh --eval "sh.addShard('shard1/mongo1:27018,mongo2:27018')"
  parameters:
  - name: shardKey
    type: string
    description: Field(s) used as the shard key, e.g. _id or customer_id
  - name: shard
    type: string
    description: Shard name or connection string for addShard
  - name: namespace
    type: string
    description: database.collection to enable sharding on
knowledge:
- title: MongoDB Sharding Docs
  type: documentation
  source: https://www.mongodb.com/docs/manual/sharding/
  description: Sharded cluster architecture, shard keys and chunk balancing
- title: sh helper methods
  type: documentation
  source: https://www.mongodb.com/docs/manual/reference/method/js-sharding-administration/
  description: sh.enableSharding, sh.shardCollection, sh.status reference
instructions: '# MongoDB Sharding


  Sharding distributes data across multiple machines so write-heavy collections scale horizontally.


  ## What this skill does


  - Starts shardsvr mongods behind a replica set name

  - Runs mongos routers against the config server replica set

  - Enables sharding and chooses shard keys


  ## When to use


  - A collection outgrows a single server

  - Write throughput needs horizontal scaling

  - Building out a new sharded cluster


  ## Real commands


  ```bash

  # Shard member (needs --shardsvr and a replSet)

  mongod --shardsvr --replSet shard1 --dbpath /data/shard1 --port 27018


  # Config server (--configsvr, replica set cfgReplSet)

  mongod --configsvr --replSet cfgReplSet --dbpath /data/cfg --port 27019


  # mongos router

  mongos --configdb cfgReplSet/cfg1:27019,cfg2:27019,cfg3:27019 --port 27017


  # Through mongos:

  sh.addShard(''shard1/mongo1:27018,mongo2:27018'')

  sh.enableSharding(''appdb'')

  sh.shardCollection(''appdb.orders'', {customer_id: ''hashed''})

  sh.status()

  ```


  ## Shard key guidance


  - Hashed keys give even distribution; ranged keys allow locality

  - Never choose a low-cardinality or monotonically increasing key


  ## Best practices


  - Always run shards as replica sets

  - Monitor `sh.status()` for chunk imbalance

  - Use `balancerStart/Stop` during maintenance windows'
examples:
- input: Design a sharded cluster for a multi-tenant orders collection
  output: The agent selects a hashed customer_id shard key, provides the mongod/mongos startup commands
    and the sh.shardCollection call.
- input: My chunks are unevenly balanced
  output: The agent checks sh.status(), explains balancer settings and shows sh.moveChunk usage to rebalance
    manually.
platforms:
  claude_code:
    tools:
    - Bash
    - Read
    - Write
    - Edit
    model: inherit
  cursor:
    rule_type: auto
  github_copilot:
    prompt_file: mongodb-sharding.md
    extension: github.copilot
  windsurf:
    model: inherit
    tools:
    - bash
    - read
    - write
    - edit
  opencode:
    plugin: opencode-mongodb-sharding
  generic:
    system_prompt: 'Your lane is Mongodb Sharding. You do it through sharded-cluster-operations: Configure
      shardsvr mongods, start mongos, enable sharding and manage chunk distribution. The commands are
      real, e.g. `mongod --shardsvr --replSet shard1 --dbpath /data/shard1 --port 27018`. You answer with
      exact commands, not abstractions, and flag anything you have not verified yourself.'
    available_tools:
    - bash
    - read
    - write
    - edit
created_at: '2025-06-15T00:00:00Z'
updated_at: '2026-08-19T00:00:00Z'
