name: mqtt-vernemq
display_name: Mqtt Vernemq
category: api
subcategory: general
description: 'Operate VerneMQ brokers: start/stop, vmq-admin inspection, clustering, and session/retainer
  management.'
version: 2.0.0
tags:
- api
author: Kdesk-Catalog
license: MIT
prerequisites:
- vernemq
- vmq-admin
capabilities:
- name: vernemq-operations
  description: Control the VerneMQ daemon and inspect nodes, listeners, sessions, and retained messages
    via vmq-admin.
  commands:
  - vernemq start
  - vernemq console
  - vmq-admin status
  - vmq-admin listener show
  - vmq-admin session show --client-id test-client
  examples:
  - vmq-admin cluster show
  - vmq-admin topic subscription add --client-id cli --topic sensors/#
  - vmq-admin retainer show
  parameters:
  - name: client_id
    type: string
    description: Client identifier for session inspection
  - name: topic
    type: string
    description: Topic filter for subscriptions
  - name: node
    type: string
    description: Node name for cluster commands
knowledge:
- title: VerneMQ Documentation
  type: documentation
  source: https://docs.vernemq.com/
  description: Installation, configuration, clustering and vmq-admin reference
- title: VerneMQ GitHub
  type: documentation
  source: https://github.com/vernemq/vernemq
  description: Source and release information
instructions: '# VerneMQ


  VerneMQ is a high-performance MQTT broker written in Erlang, built for clustering and scale.


  ## What this skill does


  - Starts/stops the broker and runs the interactive console

  - Inspects listeners, sessions, subscriptions and retained messages

  - Manages clusters with vmq-admin


  ## When to use


  - High-throughput MQTT workloads

  - Multi-node MQTT clusters

  - Brokers that must handle millions of retained messages


  ## Real commands


  ```bash

  # Daemon lifecycle

  vernemq start

  vernemq stop

  vernemq console   # foreground Erlang console


  # Status and listeners

  vmq-admin status

  vmq-admin listener show


  # Sessions and subscriptions

  vmq-admin session show --client-id test-client

  vmq-admin topic subscription add --client-id cli --topic sensors/#


  # Retained messages

  vmq-admin retainer show


  # Cluster

  vmq-admin cluster show

  ```


  ## Configuration


  - `/etc/vernemq/vernemq.conf` - listeners, allow_anonymous, plugins

  - Listener ports: 1883 plain, 8883 TLS, 8080 websockets


  ## Best practices


  - Check `vmq-admin status` before and after restarts

  - Monitor session counts for leaked persistent sessions

  - Configure `allow_anonymous=off` for production'
examples:
- input: Start VerneMQ and verify it's listening
  output: The agent runs `vernemq start`, checks `vmq-admin status` and `vmq-admin listener show`, then
    publishes with mosquitto_pub.
- input: Which clients are currently connected?
  output: The agent runs `vmq-admin session show` to list live sessions and explains how to filter by
    client-id.
platforms:
  claude_code:
    tools:
    - Bash
    - Read
    - Write
    - Edit
    model: inherit
  cursor:
    rule_type: auto
  github_copilot:
    prompt_file: mqtt-vernemq.md
    extension: github.copilot
  windsurf:
    model: inherit
    tools:
    - bash
    - read
    - write
    - edit
  opencode:
    plugin: opencode-mqtt-vernemq
  generic:
    system_prompt: 'You take the Mqtt Vernemq seat. You do it through vernemq-operations: Control the
      VerneMQ daemon and inspect nodes, listeners, sessions, and retained messages via vmq-admin. The
      commands are real, e.g. `vernemq start`. You prefer the shortest safe path, and you reproduce failures
      before diagnosing them.'
    available_tools:
    - bash
    - read
    - write
    - edit
created_at: '2024-01-01T00:00:00Z'
updated_at: '2024-01-01T00:00:00Z'
