name: nats-consumers
display_name: Nats Consumers
category: api
subcategory: general
description: 'Manage NATS JetStream consumers: create push/pull consumers, inspect delivery, and handle
  ack policies.'
version: 2.0.0
tags:
- api
author: Kdesk
license: MIT
prerequisites:
- nats
capabilities:
- name: jetstream-consumers
  description: 'Create and manage JetStream consumers with the nats CLI: pull/push modes, ack policies,
    and inspection.'
  commands:
  - nats consumer add ORDERS NEW --pull --deliver all --max-deliver 5 --ack explicit
  - nats consumer add ORDERS PUSH --push --deliver subject ords.push
  - nats consumer ls ORDERS
  - nats consumer info ORDERS NEW
  - nats consumer rm ORDERS NEW
  examples:
  - nats consumer add ORDERS WORKER --pull --ack explicit --max-deliver 3 --backoff 5s
  - nats consumer info ORDERS WORKER
  - nats consumer ls ORDERS --names
  parameters:
  - name: stream
    type: string
    description: Stream name the consumer belongs to
  - name: consumer
    type: string
    description: Consumer durable name
  - name: ack_policy
    type: string
    description: explicit, all, none or at-least-once
knowledge:
- title: NATS JetStream Consumers
  type: documentation
  source: https://docs.nats.io/using-nats/developer/develop_jetstream/consumers
  description: Push vs pull consumers, ack policies, delivery semantics
- title: nats CLI consumer docs
  type: documentation
  source: https://docs.nats.io/using-nats/command-line/
  description: nats consumer add/info/ls/pause reference
instructions: '# NATS Consumers


  JetStream consumers read messages from a stream, tracking delivery and acknowledgements per consumer.


  ## What this skill does


  - Creates push and pull consumers

  - Configures ack policies, max-deliver and backoff

  - Inspects delivery stats and errors


  ## When to use


  - Reliable event processing with redelivery

  - Worker pools pulling batches of messages


  ## Real commands


  ```bash

  # Pull consumer with explicit acks and redelivery

  nats consumer add ORDERS NEW --pull --deliver all --ack explicit --max-deliver 5


  # Push consumer to a delivery subject

  nats consumer add ORDERS PUSH --push --deliver subject ords.push


  # List and inspect

  nats consumer ls ORDERS

  nats consumer info ORDERS NEW


  # Delete

  nats consumer rm ORDERS NEW

  ```


  ## Ack policies


  - `--ack explicit`: ack each message (default for pull)

  - `--ack all`: ack all up to sequence number

  - `--ack none`: fire-and-forget

  - `--ack at-least-once`: auto-ack on receipt


  ## Best practices


  - Name consumers for their workload (e.g. `email-sender`)

  - Set `--max-deliver` to bound poison messages

  - Monitor `num_ack_pending` and `num_redelivered` in consumer info'
examples:
- input: Create a worker consumer that retries failures 3 times
  output: The agent runs `nats consumer add ORDERS WORKER --pull --ack explicit --max-deliver 3` and explains
    redelivery.
- input: Why is my consumer stuck with pending messages?
  output: The agent checks `nats consumer info` for num_ack_pending and inactive delivery, and shows pause/resume
    commands.
platforms:
  claude_code:
    tools:
    - Bash
    - Read
    - Write
    - Edit
    model: inherit
  cursor:
    rule_type: auto
  github_copilot:
    prompt_file: nats-consumers.md
    extension: github.copilot
  windsurf:
    model: inherit
    tools:
    - bash
    - read
    - write
    - edit
  opencode:
    plugin: opencode-nats-consumers
  generic:
    system_prompt: 'Your lane is Nats Consumers. You do it through jetstream-consumers: Create and manage
      JetStream consumers with the nats CLI: pull/push modes, ack policies, and inspection. The commands
      are real, e.g. `nats consumer add ORDERS NEW --pull --deliver all --max-deliver 5 --ack explicit`.
      You verify each step you report and stay honest about what you have not run.'
    available_tools:
    - bash
    - read
    - write
    - edit
created_at: '2025-06-15T00:00:00Z'
updated_at: '2026-08-19T00:00:00Z'
