name: redis-security
display_name: Redis Security
category: api
subcategory: general
description: 'Hand-crafted Redis security skill: ACL users, requirepass, TLS listener configuration, dangerous
  command rename, and exposure auditing.'
version: 2.0.0
tags:
- api
author: Kdesk-Catalog
license: MIT
prerequisites:
- redis-cli
capabilities:
- name: redis-hardening
  description: Lock down Redis with ACLs, passwords, TLS, and command renaming
  commands:
  - redis-cli ACL LIST
  - redis-cli ACL SETUSER appuser on >StrongPass123 ~cache:* +get +set +del
  - redis-cli --no-auth-warning -a 'pass' ACL GETUSER appuser
  - redis-cli CONFIG SET requirepass 's3cr3t'
  - redis-cli CONFIG REWRITE
  - redis-cli -p 6380 --tls --cacert /etc/redis/ca.crt PING
  examples:
  - redis-cli ACL SETUSER readonly on >pw123 ~cache:* +get -set
  - redis-cli CONFIG GET bind
  - redis-cli --tls --cacert ca.crt -a $REDIS_PASS ping
  parameters:
  - name: username
    type: string
    description: ACL user to create or modify
  - name: key_pattern
    type: string
    description: Key pattern the user may touch, e.g. cache:*
  - name: commands
    type: string
    description: Allow/deny command list, e.g. +get -set
knowledge:
- title: Redis Security
  type: documentation
  source: https://redis.io/docs/latest/operate/oss_and_stack/management/security/
  description: Official ACL, requirepass, TLS, and protection docs
- title: ACL command reference
  type: documentation
  source: https://redis.io/docs/latest/commands/acl-setuser/
  description: ACL SETUSER rule syntax and examples
instructions: '# Redis Security


  Hand-crafted skill for hardening Redis instances against exposure and misuse.


  ## What this skill does


  - Creates least-privilege ACL users with key and command allowlists

  - Enables requirepass for the default user and rewrites the config

  - Configures a TLS listener and renames dangerous commands like FLUSHALL


  ## When to use


  - A Redis instance is reachable from outside localhost

  - Multiple teams share one server and need isolated permissions

  - Compliance reviews ask for auth on all datastores


  ## Real commands


  ```bash

  # Inventory users

  redis-cli ACL LIST


  # Create a scoped app user

  redis-cli ACL SETUSER appuser on >StrongPass123 ~cache:* +get +set +del


  # Inspect a user''s effective rules

  redis-cli --no-auth-warning -a ''pass'' ACL GETUSER appuser


  # Password-protect the default user

  redis-cli CONFIG SET requirepass ''s3cr3t''

  redis-cli CONFIG REWRITE


  # TLS listener check

  redis-cli -p 6380 --tls --cacert /etc/redis/ca.crt PING

  ```


  ## Config example


  ```conf

  # redis.conf

  bind 127.0.0.1 10.0.0.5

  protected-mode yes

  requirepass s3cr3t

  rename-command FLUSHALL ""


  port 6379

  port 6380 tls

  tls-port 6380

  tls-cert-file /etc/redis/server.crt

  tls-key-file /etc/redis/server.key

  tls-ca-cert-file /etc/redis/ca.crt

  ```


  ## Testing


  ```bash

  redis-cli -a ''s3cr3t'' ping

  redis-cli -p 6380 --tls --cacert ca.crt -a ''s3cr3t'' ping

  redis-cli ACL WHOAMI

  ```


  ## Best practices


  - Never bind 0.0.0.0 without TLS plus a strong password

  - Give each service its own ACL user with only the commands it needs

  - Rename FLUSHALL/FLUSHDB/KEYS on shared servers

  '
examples:
- input: Give a background worker read-write access to only its cache keys
  output: redis-cli ACL SETUSER worker on >WorkerPass123 ~worker:* +get +set +del and verify with ACL
    GETUSER worker.
- input: Check if my Redis is exposed publicly
  output: Run redis-cli CONFIG GET bind and CONFIG GET protected-mode; if bind is 0.0.0.0 and protected-mode
    no, fix immediately.
platforms:
  claude_code:
    tools:
    - Bash
    - Read
    - Write
    - Edit
    model: inherit
  cursor:
    rule_type: auto
  github_copilot:
    prompt_file: redis-security.md
    extension: github.copilot
  windsurf:
    model: inherit
    tools:
    - bash
    - read
    - write
    - edit
  opencode:
    plugin: opencode-redis-security
  generic:
    system_prompt: 'You own Redis Security end to end. You do it through redis-hardening: Lock down Redis
      with ACLs, passwords, TLS, and command renaming. The commands are real, e.g. `redis-cli ACL LIST`.
      You run and verify the real CLI commands before you claim anything.'
    available_tools:
    - bash
    - read
    - write
    - edit
created_at: '2024-01-01T00:00:00Z'
updated_at: '2024-01-01T00:00:00Z'
