name: redis-streams
display_name: Redis Streams
category: api
subcategory: general
description: 'Expert Redis Streams reference covering XADD/XREAD/XRANGE/XREVRANGE basics, ID structure, blocking reads, and range queries suited to event logs and time series data.'
version: 2.0.0
tags:
- api
author: Mehul Wagde
license: MIT
prerequisites:
- redis-cli
capabilities:
- name: redis-streams-core
  description: 'Core Redis Streams operations: append, range read, block, length'
  commands:
  - redis-cli XADD temperature:2026-08 * sensor office value 22.5
  - redis-cli XRANGE temperature:2026-08 - +
  - redis-cli XREVRANGE temperature:2026-08 + - COUNT 3
  - redis-cli XLEN temperature:2026-08
  - redis-cli XREAD BLOCK 5000 COUNT 2 STREAMS temperature:2026-08 $
  examples:
  - redis-cli XRANGE temperature:2026-08 - + COUNT 5
  - redis-cli XRANGE temperature:2026-08 1723000000000-0 1723999999999-0
  - redis-cli XREAD BLOCK 5000 COUNT 2 STREAMS temperature:2026-08 $
  parameters:
  - name: stream
    type: string
    description: Stream key name
  - name: count
    type: integer
    description: Number of entries to return
  - name: block
    type: integer
    description: Blocking read timeout in milliseconds
knowledge:
- title: Redis Streams data type
  type: documentation
  source: https://redis.io/docs/latest/develop/data-types/streams/
  description: Stream structure, IDs, and core commands
- title: XADD command
  type: documentation
  source: https://redis.io/docs/latest/commands/xadd/
  description: Auto ID generation and entry limits
instructions: '# Redis Streams
 
 
  Expert skill for the core Redis Streams data type.


  ## What this skill does


  - Appends field-value entries with XADD and auto-generated IDs

  - Reads ranges forward and backward with XRANGE/XREVRANGE

  - Blocks for new entries with XREAD and checks stream length


  ## When to use


  - Append-only event logs with per-message timestamps

  - Sensor or metrics time series

  - Learning streams before moving to consumer groups


  ## Real commands


  ```bash

  # Append an entry; * lets Redis generate <ms>-<seq> ID

  redis-cli XADD temperature:2026-08 * sensor office value 22.5


  # All entries, oldest to newest

  redis-cli XRANGE temperature:2026-08 - +


  # Newest 3 entries

  redis-cli XREVRANGE temperature:2026-08 + - COUNT 3


  # Entry count

  redis-cli XLEN temperature:2026-08


  # Block up to 5s for new entries (offset $ = end of stream)

  redis-cli XREAD BLOCK 5000 COUNT 2 STREAMS temperature:2026-08 $


  # Range by time window: IDs are <ms>-<seq>

  redis-cli XRANGE temperature:2026-08 1723000000000-0 1723999999999-0

  ```


  ## ID anatomy


  - IDs are <milliseconds-time>-<sequence> in UTC

  - Same-ms entries get incrementing sequence numbers

  - XADD MAXLEN ~ 1000 keeps the stream bounded while appending


  ## Testing


  ```bash

  redis-cli XADD temperature:2026-08 * sensor lab value 21.0

  redis-cli XRANGE temperature:2026-08 - + COUNT 5

  redis-cli XLEN temperature:2026-08

  ```


  ## Best practices


  - Use * to let Redis generate monotonic IDs

  - Give streams descriptive keys since IDs encode time already

  - Prefer MAXLEN trimming at insert time for hot streams

  '
examples:
- input: Log a temperature reading to a stream
  output: redis-cli XADD temperature:2026-08 * sensor office value 22.5 returns the generated ID like
    1723000000000-0.
- input: Get the 5 newest readings
  output: redis-cli XREVRANGE temperature:2026-08 + - COUNT 5 lists them newest first.
platforms:
  claude_code:
    tools:
    - Bash
    - Read
    - Write
    - Edit
    model: inherit
  cursor:
    rule_type: auto
  github_copilot:
    prompt_file: redis-streams.md
    extension: github.copilot
  windsurf:
    model: inherit
    tools:
    - bash
    - read
    - write
    - edit
  opencode:
    plugin: opencode-redis-streams
  generic:
    system_prompt: 'Your lane is Redis Streams. You do it through redis-streams-core: Core Redis Streams
      operations: append, range read, block, length. The commands are real, e.g. `redis-cli XADD temperature:2026-08
      * sensor office value 22.5`. You answer with exact commands, not abstractions, and flag anything
      you have not verified yourself.'
    available_tools:
    - bash
    - read
    - write
    - edit
created_at: '2025-06-15T00:00:00Z'
updated_at: '2026-08-19T00:00:00Z'
