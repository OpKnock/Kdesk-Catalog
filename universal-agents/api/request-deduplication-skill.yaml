name: request-deduplication
display_name: Request Deduplication
category: api
subcategory: general
description: 'Hand-crafted request deduplication skill: idempotency keys for unsafe methods, ETag/If-None-Match
  caching for GETs, and Redis SETNX guard patterns.'
version: 2.0.0
tags:
- api
author: Kdesk-Catalog
license: MIT
prerequisites:
- redis-cli
capabilities:
- name: idempotency-etag
  description: Deduplicate requests with idempotency keys and conditional GETs
  commands:
  - 'curl -i -X POST http://localhost:8080/payments -H "Idempotency-Key: 9f8c-4a2b" -d ''{"amount":100}'''
  - 'curl -i -X POST http://localhost:8080/payments -H "Idempotency-Key: 9f8c-4a2b" -d ''{"amount":100}'''
  - 'curl -i -H ''If-None-Match: "a1b2c3"'' http://localhost:8080/orders/42'
  - redis-cli SETNX idem:9f8c-4a2b processing
  - redis-cli EXPIRE idem:9f8c-4a2b 3600
  examples:
  - 'curl -i -H ''If-None-Match: "a1b2c3"'' http://localhost:8080/orders/42'
  - 'curl -s -o /dev/null -w ''%{http_code}'' -X POST http://localhost:8080/payments -H "Idempotency-Key:
    9f8c-4a2b" -d ''{"amount":100}'''
  - redis-cli SETNX idem:9f8c-4a2b processing
  parameters:
  - name: idempotency_key
    type: string
    description: Client-generated key identifying a logical operation
  - name: etag
    type: string
    description: Response fingerprint used with If-None-Match
  - name: ttl_seconds
    type: integer
    description: How long dedup records live in Redis
knowledge:
- title: HTTP ETag spec (RFC 7232)
  type: documentation
  source: https://datatracker.ietf.org/doc/html/rfc7232
  description: If-None-Match, If-Match, ETag semantics
- title: Stripe idempotency guide
  type: documentation
  source: https://docs.stripe.com/api/idempotent_requests
  description: Production idempotency key design pattern
instructions: '# Request Deduplication


  Hand-crafted skill for making API requests idempotent and deduplicated.


  ## What this skill does


  - Uses Idempotency-Key headers so retried POSTs return the stored response

  - Caches GETs with ETag/If-None-Match to avoid duplicate work

  - Guards concurrent duplicates with a Redis SETNX lock


  ## When to use


  - Payment or order endpoints where retries must not double-charge

  - Expensive report endpoints hit repeatedly by clients

  - Designing APIs where the same request may arrive twice in a short window


  ## Real commands


  ```bash

  # Send the same mutation twice with one key: second call short-circuits

  curl -i -X POST http://localhost:8080/payments -H "Idempotency-Key: 9f8c-4a2b" -d ''{"amount":100}''

  curl -i -X POST http://localhost:8080/payments -H "Idempotency-Key: 9f8c-4a2b" -d ''{"amount":100}''


  # Conditional GET: 304 if the ETag still matches

  curl -i -H ''If-None-Match: "a1b2c3"'' http://localhost:8080/orders/42


  # Atomic dedup guard in Redis

  redis-cli SETNX idem:9f8c-4a2b processing

  redis-cli EXPIRE idem:9f8c-4a2b 3600

  ```


  ## Server behavior


  - On first POST with a key: execute, store response, return it

  - On repeat POST with the same key: return the stored response unchanged

  - GET responses carry ETag headers; clients echo them with If-None-Match


  ## Testing


  ```bash

  # Twice, expect identical bodies and no double side effects

  curl -s -X POST http://localhost:8080/payments -H "Idempotency-Key: k1" -d ''{"amount":100}''

  curl -s -X POST http://localhost:8080/payments -H "Idempotency-Key: k1" -d ''{"amount":100}''


  # Expect 304 on cached GET

  curl -i -H ''If-None-Match: "a1b2c3"'' http://localhost:8080/orders/42

  ```


  ## Best practices


  - Never generate idempotency keys server-side for client mutations

  - Store keys with a TTL aligned to your retry window (1-24h)

  - Make the dedup record atomic with SETNX so concurrent calls cannot double-run

  '
examples:
- input: Prevent double charges when clients retry POST /payments
  output: Require an Idempotency-Key header, store first-response per key in Redis with SETNX plus EXPIRE,
    and replay the stored response on duplicates.
- input: Cut traffic on a hot GET endpoint
  output: 'Emit ETag headers and support If-None-Match; verify with curl -i -H ''If-None-Match: "a1b2c3"''
    expecting 304.'
platforms:
  claude_code:
    tools:
    - Bash
    - Read
    - Write
    - Edit
    model: inherit
  cursor:
    rule_type: auto
  github_copilot:
    prompt_file: request-deduplication.md
    extension: github.copilot
  windsurf:
    model: inherit
    tools:
    - bash
    - read
    - write
    - edit
  opencode:
    plugin: opencode-request-deduplication
  generic:
    system_prompt: 'You work Request Deduplication hands-on. You do it through idempotency-etag: Deduplicate
      requests with idempotency keys and conditional GETs. The commands are real, e.g. `curl -i -X POST
      http://localhost:8080/payments -H Idempotency-Key: 9f8c-4a2b -d {amount:100}`. You run and verify
      the real CLI commands before you claim anything.'
    available_tools:
    - bash
    - read
    - write
    - edit
created_at: '2024-01-01T00:00:00Z'
updated_at: '2024-01-01T00:00:00Z'
