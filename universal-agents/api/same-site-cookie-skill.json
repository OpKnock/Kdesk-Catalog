{
  "name": "same-site-cookie",
  "display_name": "Same Site Cookie",
  "category": "api",
  "subcategory": "general",
  "description": "Hand-crafted SameSite cookie skill: Strict/Lax/None semantics, curl header inspection, server-side Set-Cookie configuration, and CSRF impact analysis.",
  "version": "2.0.0",
  "tags": [
    "api"
  ],
  "author": "Kdesk-Catalog",
  "license": "MIT",
  "prerequisites": [
    "python"
  ],
  "capabilities": [
    {
      "name": "samesite-config",
      "description": "Configure and verify SameSite cookie attributes on requests",
      "commands": [
        "curl -i -H 'Cookie: session=abc123' https://app.example.com/me | grep -i 'set-cookie'",
        "curl -i https://app.example.com/login -X POST -d 'user=ada&pass=secret' | grep -i set-cookie",
        "curl -s -o /dev/null -w '%{http_code}\\n' https://app.example.com/me -b 'session=abc123'",
        "python -c 'from http.cookies import SimpleCookie; c=SimpleCookie(); c[\"session\"]=\"abc\"; c[\"session\"][\"samesite\"]=\"Lax\"; c[\"session\"][\"secure\"]=True; print(c.output())'"
      ],
      "examples": [
        "curl -i https://app.example.com/me | grep -i set-cookie",
        "curl -s -o /dev/null -w '%{http_code}\\n' https://app.example.com/me -b 'session=abc123; SameSite=Strict'",
        "python -c 'from http.cookies import SimpleCookie; c=SimpleCookie(); c[\"s\"]=\"1\"; c[\"s\"][\"samesite\"]=\"None\"; print(c.output())'"
      ],
      "parameters": [
        {
          "name": "samesite_value",
          "type": "string",
          "description": "Strict, Lax, or None"
        },
        {
          "name": "secure",
          "type": "boolean",
          "description": "Require HTTPS before sending the cookie"
        },
        {
          "name": "cookie_name",
          "type": "string",
          "description": "Name of the session cookie"
        }
      ]
    }
  ],
  "knowledge": [
    {
      "title": "MDN SameSite cookies",
      "type": "documentation",
      "source": "https://developer.mozilla.org/en-US/docs/Web/HTTP/Headers/Set-Cookie/SameSite",
      "description": "Strict/Lax/None semantics and browser behavior"
    },
    {
      "title": "OWASP session cheat sheet",
      "type": "documentation",
      "source": "https://cheatsheetseries.owasp.org/cheatsheets/Session_Management_Cheat_Sheet.html",
      "description": "Session cookie attributes and CSRF implications"
    }
  ],
  "instructions": "# SameSite Cookies\n\nHand-crafted skill for SameSite cookie attributes and CSRF defense.\n\n## What this skill does\n\n- Sets Strict, Lax, or None on session cookies server-side\n- Inspects Set-Cookie and Cookie headers with curl\n- Reasons about CSRF exposure per SameSite value\n\n## When to use\n\n- Hardening session cookies after a CSRF review\n- Debugging why cookies are not sent on third-party requests\n- Switching to SameSite=None for cross-site embedding\n\n## Real commands\n\n```bash\n# What does the server set?\ncurl -i -H 'Cookie: session=abc123' https://app.example.com/me | grep -i 'set-cookie'\ncurl -i https://app.example.com/login -X POST -d 'user=ada&pass=secret' | grep -i set-cookie\n\n# Send a cookie manually and check acceptance\ncurl -s -o /dev/null -w '%{http_code}\\n' https://app.example.com/me -b 'session=abc123'\n\n# Model the Set-Cookie header in Python\npython -c 'from http.cookies import SimpleCookie; c=SimpleCookie(); c[\"session\"]=\"abc\"; c[\"session\"][\"samesite\"]=\"Lax\"; c[\"session\"][\"secure\"]=True; print(c.output())'\n```\n\n## Server config\n\n```python\n# Flask\nresponse.set_cookie(\"session\", value=tok, samesite=\"Lax\", secure=True, httponly=True)\n```\n\n```nginx\n# nginx proxy: forward the header untouched\nproxy_set_header Cookie $http_cookie;\n```\n\n## Semantics\n\n- Strict: never sent on cross-site requests (strongest, worst UX)\n- Lax: sent on top-level navigations only (default, safe middle ground)\n- None: sent everywhere; requires Secure or browsers reject it\n\n## Testing\n\n```bash\ncurl -i https://app.example.com/me | grep -i set-cookie   # expect samesite=lax; secure\n```\n\n## Best practices\n\n- Use Lax by default; Strict only when UX allows\n- SameSite=None must pair with Secure on every deployment\n- Keep cookies HttpOnly and add CSRF tokens regardless of SameSite\n",
  "examples": [
    {
      "input": "Why is my session cookie dropped in iframes?",
      "output": "The cookie is probably SameSite=Strict/Lax; cross-site embeds need SameSite=None plus Secure, e.g. response.set_cookie(..., samesite=\"None\", secure=True)."
    },
    {
      "input": "Harden a login cookie",
      "output": "Set SameSite=Lax, Secure, HttpOnly; verify with curl -i https://app.example.com/me | grep -i set-cookie."
    }
  ],
  "platforms": {
    "claude_code": {
      "tools": [
        "Bash",
        "Read",
        "Write",
        "Edit"
      ],
      "model": "inherit"
    },
    "cursor": {
      "rule_type": "auto"
    },
    "github_copilot": {
      "prompt_file": "same-site-cookie.md",
      "extension": "github.copilot"
    },
    "windsurf": {
      "model": "inherit",
      "tools": [
        "bash",
        "read",
        "write",
        "edit"
      ]
    },
    "opencode": {
      "plugin": "opencode-same-site-cookie"
    },
    "generic": {
      "system_prompt": "Your lane is Same Site Cookie. You do it through samesite-config: Configure and verify SameSite cookie attributes on requests. The commands are real, e.g. `curl -i -H Cookie: session=abc123 https://app.example.com/me | grep -i set-cookie`. You run and verify the real CLI commands before you claim anything.",
      "available_tools": [
        "bash",
        "read",
        "write",
        "edit"
      ]
    }
  },
  "created_at": "2024-01-01T00:00:00Z",
  "updated_at": "2024-01-01T00:00:00Z",
  "id": "same-site-cookie",
  "type": "skill",
  "skills": [],
  "tools": [
    "Bash",
    "Read",
    "Write",
    "Edit"
  ],
  "inputs": {
    "parameters": [
      {
        "capability": "samesite-config",
        "name": "samesite_value",
        "type": "string",
        "description": "Strict, Lax, or None"
      },
      {
        "capability": "samesite-config",
        "name": "secure",
        "type": "boolean",
        "description": "Require HTTPS before sending the cookie"
      },
      {
        "capability": "samesite-config",
        "name": "cookie_name",
        "type": "string",
        "description": "Name of the session cookie"
      }
    ]
  },
  "outputs": {},
  "dependencies": [
    "python"
  ],
  "conversion": {
    "tool": "Kdesk-Catalog yaml-to-json",
    "schema": "definition-v1",
    "source_yaml": "api/same-site-cookie-skill.yaml",
    "derived": {
      "skills": "none",
      "tools": "platforms.claude_code.tools",
      "inputs": "capability.parameters",
      "outputs": "none (not present in source YAML)",
      "dependencies": "prerequisites"
    }
  },
  "_provenance": {
    "generated_by": "Kdesk-Catalog yaml-to-json",
    "generator_version": "1.0.0",
    "schema": "definition-v1",
    "source": "universal-agents/api/same-site-cookie-skill.yaml",
    "checksum": "aa4460feb3d2d4bc914a3077017beecf4415063fec9928d62c1b90a6fda428ec"
  }
}
