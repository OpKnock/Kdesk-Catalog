name: same-site-cookie
display_name: Same Site Cookie
category: api
subcategory: general
description: 'Expert reference covering Strict/Lax/None semantics, curl header inspection, server-side Set-Cookie configuration, and CSRF impact analysis.'
version: 2.0.0
tags:
- api
author: Mehul Wagde
license: MIT
prerequisites:
- python
capabilities:
- name: samesite-config
  description: Configure and verify SameSite cookie attributes on requests
  commands:
  - 'curl -i -H ''Cookie: session=abc123'' https://app.your-app.test/me | grep -i ''set-cookie'''
  - curl -i https://app.your-app.test/login -X POST -d 'user=ada&pass=secret' | grep -i set-cookie
  - curl -s -o /dev/null -w '%{http_code}\n' https://app.your-app.test/me -b 'session=abc123'
  - python -c 'from http.cookies import SimpleCookie; c=SimpleCookie(); c["session"]="abc"; c["session"]["samesite"]="Lax";
    c["session"]["secure"]=True; print(c.output())'
  examples:
  - curl -i https://app.your-app.test/me | grep -i set-cookie
  - curl -s -o /dev/null -w '%{http_code}\n' https://app.your-app.test/me -b 'session=abc123; SameSite=Strict'
  - python -c 'from http.cookies import SimpleCookie; c=SimpleCookie(); c["s"]="1"; c["s"]["samesite"]="None";
    print(c.output())'
  parameters:
  - name: samesite_value
    type: string
    description: Strict, Lax, or None
  - name: secure
    type: boolean
    description: Require HTTPS before sending the cookie
  - name: cookie_name
    type: string
    description: Name of the session cookie
knowledge:
- title: MDN SameSite cookies
  type: documentation
  source: https://developer.mozilla.org/en-US/docs/Web/HTTP/Headers/Set-Cookie/SameSite
  description: Strict/Lax/None semantics and browser behavior
- title: OWASP session cheat sheet
  type: documentation
  source: https://cheatsheetseries.owasp.org/cheatsheets/Session_Management_Cheat_Sheet.html
  description: Session cookie attributes and CSRF implications
instructions: '# SameSite Cookies
 
 
  Expert skill for SameSite cookie attributes and CSRF defense.


  ## What this skill does


  - Sets Strict, Lax, or None on session cookies server-side

  - Inspects Set-Cookie and Cookie headers with curl

  - Reasons about CSRF exposure per SameSite value


  ## When to use


  - Hardening session cookies after a CSRF review

  - Debugging why cookies are not sent on third-party requests

  - Switching to SameSite=None for cross-site embedding


  ## Real commands


  ```bash

  # What does the server set?

  curl -i -H ''Cookie: session=abc123'' https://app.your-app.test/me | grep -i ''set-cookie''

  curl -i https://app.your-app.test/login -X POST -d ''user=ada&pass=secret'' | grep -i set-cookie


  # Send a cookie manually and check acceptance

  curl -s -o /dev/null -w ''%{http_code}\n'' https://app.your-app.test/me -b ''session=abc123''


  # Model the Set-Cookie header in Python

  python -c ''from http.cookies import SimpleCookie; c=SimpleCookie(); c["session"]="abc"; c["session"]["samesite"]="Lax";
  c["session"]["secure"]=True; print(c.output())''

  ```


  ## Server config


  ```python

  # Flask

  response.set_cookie("session", value=tok, samesite="Lax", secure=True, httponly=True)

  ```


  ```nginx

  # nginx proxy: forward the header untouched

  proxy_set_header Cookie $http_cookie;

  ```


  ## Semantics


  - Strict: never sent on cross-site requests (strongest, worst UX)

  - Lax: sent on top-level navigations only (default, safe middle ground)

  - None: sent everywhere; requires Secure or browsers reject it


  ## Testing


  ```bash

  curl -i https://app.your-app.test/me | grep -i set-cookie   # expect samesite=lax; secure

  ```


  ## Best practices


  - Use Lax by default; Strict only when UX allows

  - SameSite=None must pair with Secure on every deployment

  - Keep cookies HttpOnly and add CSRF tokens regardless of SameSite

  '
examples:
- input: Why is my session cookie dropped in iframes?
  output: The cookie is probably SameSite=Strict/Lax; cross-site embeds need SameSite=None plus Secure,
    e.g. response.set_cookie(..., samesite="None", secure=True).
- input: Harden a login cookie
  output: Set SameSite=Lax, Secure, HttpOnly; verify with curl -i https://app.your-app.test/me | grep -i
    set-cookie.
platforms:
  claude_code:
    tools:
    - Bash
    - Read
    - Write
    - Edit
    model: inherit
  cursor:
    rule_type: auto
  github_copilot:
    prompt_file: same-site-cookie.md
    extension: github.copilot
  windsurf:
    model: inherit
    tools:
    - bash
    - read
    - write
    - edit
  opencode:
    plugin: opencode-same-site-cookie
  generic:
    system_prompt: 'Your lane is Same Site Cookie. You do it through samesite-config: Configure and verify
      SameSite cookie attributes on requests. The commands are real, e.g. `curl -i -H Cookie: session=abc123
      https://app.your-app.test/me | grep -i set-cookie`. You run and verify the real CLI commands before
      you claim anything.'
    available_tools:
    - bash
    - read
    - write
    - edit
created_at: '2025-06-15T00:00:00Z'
updated_at: '2026-08-19T00:00:00Z'
