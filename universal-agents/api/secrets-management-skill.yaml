name: secrets-management
display_name: Secrets Management
version: 2.0.0
type: skill
category: api
subcategory: general
description: "Manages the complete secret lifecycle across Kubernetes, SOPS-encrypted files, HashiCorp Vault, and AWS Secrets Manager. Handles creation, rotation, and access patterns for production credentials without exposing plaintext in repositories or container images."
author: Mehul Wagde
created_at: '2025-06-15T00:00:00Z'
updated_at: '2026-08-19T00:00:00Z'
tags:
  - api
  - kubernetes
  - vault
  - sops
  - aws-secrets-manager
prerequisites:
  - kubectl
  - sops
  - vault
  - aws
prerequisites:
- aws
- kubectl
- sops
- vault
capabilities:
- name: secrets-lifecycle
  description: Create, read, encrypt, and rotate secrets across stores
  commands:
  - kubectl create secret generic api --from-file=config.json --from-literal=API_KEY=xxx
  - kubectl get secret api -o jsonpath='{.data.API_KEY}' | base64 -d
  - sops --encrypt --in-place secrets/application.yaml
  - vault kv put secret/payments apikey=sk_live_123
  - aws secretsmanager get-secret-value --secret-id payments/api --query SecretString --output text
  examples:
  - kubectl create secret generic api --from-literal=API_KEY=xxx
  - vault kv get secret/payments
  - sops --decrypt secrets/application.yaml
  parameters:
  - name: store
    type: string
    description: k8s, sops, vault, or aws-secretsmanager
  - name: secret_name
    type: string
    description: Secret identifier in the chosen store
  - name: file
    type: string
    description: Config file path for SOPS encryption
knowledge:
  - title: Kubernetes Secrets Documentation
    type: documentation
    source: https://kubernetes.io/docs/concepts/configuration/secret/
    description: Secret types, creation methods, and consumption patterns in pods
  - title: SOPS Documentation
    type: documentation
    source: https://getsops.io/docs/
    description: File encryption workflows with age, PGP, and KMS backends
  - title: Vault KV Secrets Engine v2
    type: documentation
    source: https://developer.hashicorp.com/vault/docs/secrets/kv/kv-v2
    description: Versioned key-value storage with check-and-set operations
  - title: AWS Secrets Manager User Guide
    type: documentation
    source: https://docs.aws.amazon.com/secretsmanager/latest/userguide/intro.html
    description: Rotation, replication, and cross-account access patterns
instructions: |
  # Secrets Management

  Manages the complete secret lifecycle across Kubernetes, SOPS-encrypted files, HashiCorp Vault, and AWS Secrets Manager.

  ## What this skill does

  - Creates and reads Kubernetes Secrets from files and literals
  - Encrypts config files in git with SOPS using age, PGP, or KMS
  - Stores and retrieves secrets in Vault KV v2 and AWS Secrets Manager
  - Rotates credentials without application restarts

  ## When to use

  - Deciding where a new credential should live (k8s, Vault, AWS, or SOPS)
  - Rotating database passwords or API keys during an incident
  - Migrating from environment variables to a proper secret store
  - Storing encrypted configuration in GitOps repositories

  ## Real commands

  ```bash
  # Kubernetes: create and read secrets
  kubectl create secret generic api --from-file=config.json --from-literal=API_KEY=xxx
  kubectl get secret api -o jsonpath='{.data.API_KEY}' | base64 -d

  # SOPS: encrypt/decrypt a config file in place
  sops --encrypt --in-place secrets/application.yaml
  sops --decrypt secrets/application.yaml

  # Vault KV v2: write and read versioned secrets
  vault kv put secret/payments apikey=sk_live_123
  vault kv get secret/payments

  # AWS Secrets Manager: retrieve secret value
  aws secretsmanager get-secret-value --secret-id payments/api --query SecretString --output text
  ```

  ## Rotation pattern

  ```bash
  # New value in, old value retired
  vault kv put secret/payments apikey=sk_live_456
  kubectl rollout restart deployment/api
  ```

  ## Testing

  ```bash
  sops --decrypt secrets/application.yaml | yq '.database.password'
  vault kv get secret/payments
  ```

  ## Best practices

  - Kubernetes Secrets alone are base64, not encryption: use Sealed Secrets or external stores
  - Rotate on a schedule, not only after leaks
  - Audit access: Vault audit logs, AWS CloudTrail, Kubernetes audit log
  - Use SOPS creation rules (`.sops.yaml`) so encryption needs no flags


  Hand-crafted skill for managing secrets across Kubernetes, SOPS, Vault, and AWS.


  ## What this skill does


  - Creates and reads Kubernetes Secrets from files and literals

  - Encrypts config files in git with SOPS

  - Stores and retrieves secrets in Vault and AWS Secrets Manager


  ## When to use


  - Deciding where a new secret should live

  - Rotating credentials during an incident

  - Migrating from env vars to a real secret store


  ## Real commands


  ```bash

  # Kubernetes

  kubectl create secret generic api --from-file=config.json --from-literal=API_KEY=xxx

  kubectl get secret api -o jsonpath=''{.data.API_KEY}'' | base64 -d


  # SOPS: encrypt/decrypt a config file in place

  sops --encrypt --in-place secrets/application.yaml

  sops --decrypt secrets/application.yaml


  # Vault (kv v2)

  vault kv put secret/payments apikey=sk_live_123

  vault kv get secret/payments


  # AWS Secrets Manager

  aws secretsmanager get-secret-value --secret-id payments/api --query SecretString --output text

  ```


  ## Rotation pattern


  ```bash

  # New value in, old value retired

  vault kv put secret/payments apikey=sk_live_456

  kubectl rollout restart deployment/api

  ```


  ## Testing


  ```bash

  sops --decrypt secrets/application.yaml | yq ''.database.password''

  vault kv get secret/payments

  ```


  ## Best practices


  - Kubernetes Secrets alone are base64, not encryption: use a store or Sealed Secrets

  - Rotate on a schedule, not only after leaks

  - Audit access: Vault audit logs, AWS CloudTrail, k8s audit log

  '
examples:
  - input: Store the database password for a Kubernetes deployment
    output: kubectl create secret generic db --from-literal=password=xxx, then reference secretKeyRef in the Deployment manifest.
  - input: Where should I keep configuration files containing credentials?
    output: Encrypt with sops --encrypt --in-place secrets/application.yaml and commit the encrypted file to Git.
  - input: Rotate the Stripe API key without downtime
    output: vault kv put secret/stripe api_key=sk_live_new; kubectl rollout restart deployment/api
platforms:
  claude_code:
    tools:
      - Bash
      - Read
      - Write
      - Edit
    model: inherit
  cursor:
    rule_type: auto
  github_copilot:
    prompt_file: secrets-management.md
    extension: github.copilot
  windsurf:
    model: inherit
    tools:
      - bash
      - read
      - write
      - edit
  opencode:
    plugin: opencode-secrets-management
  generic:
    system_prompt: "You are the Secrets Management specialist. You operate through secrets-lifecycle: Create, read, encrypt, and rotate secrets across stores. The commands are real, e.g. `kubectl create secret generic api --from-file=config.json --from-literal=API_KEY=xxx`. You verify each step you report and stay honest about what you have not run."
    available_tools:
      - bash
      - read
      - write
      - edit
