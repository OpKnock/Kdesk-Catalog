name: sentry-error-tracking
display_name: Sentry Error Tracking
version: 2.0.0
type: skill
category: api
subcategory: general
description: "Operates Sentry end-to-end: bootstraps the Python SDK with DSN and release context, manages releases and deploys via sentry-cli with commit linking, and verifies the ingest pipeline with raw envelope submissions."
author: Mehul Wagde
created_at: '2025-06-15T00:00:00Z'
updated_at: '2026-08-19T00:00:00Z'
tags:
  - api
  - sentry
  - error-tracking
  - release-management
prerequisites:
  - pip
  - sentry-cli
capabilities:
- name: sentry-release-workflow
  description: Initialize the SDK, track releases/deploys, and ingest test events
  commands:
  - pip install sentry-sdk
  - sentry-cli --version
  - sentry-cli releases new -p backend 2026.08.1
  - sentry-cli releases set-commits --auto 2026.08.1
  - sentry-cli releases deploys 2026.08.1 new -e production
  - 'curl -X POST "https://o1.ingest.sentry.io/api/0/store/?sentry_key=DSN_PUBLIC" -H ''Content-Type:
    application/json'' -d ''{"event_id":"0123456789abcdef0123456789abcdef","message":"smoke","level":"warning"}'''
  examples:
  - sentry-cli releases new -p backend 2026.08.1 && sentry-cli releases set-commits --auto 2026.08.1
  - 'curl -X POST "https://o1.ingest.sentry.io/api/0/store/?sentry_key=DSN_PUBLIC" -H ''Content-Type:
    application/json'' -d ''{"message":"test","level":"info"}'''
  - sentry-cli releases list -p backend
  parameters:
  - name: dsn
    type: string
    description: Sentry DSN (public key part for ingestion)
  - name: release
    type: string
    description: Release name, e.g. 2026.08.1
  - name: environment
    type: string
    description: Environment for deploys, e.g. production
knowledge:
  - title: Sentry Python SDK Documentation
    type: documentation
    source: https://docs.sentry.io/platforms/python/
    description: sentry_sdk.init options, scopes, and performance monitoring
  - title: sentry-cli Reference
    type: documentation
    source: https://docs.sentry.io/cli/
    description: Release, deploy, and event management commands
  - title: Sentry Envelope API
    type: documentation
    source: https://develop.sentry.dev/sdk/envelopes/
    description: Raw envelope format for direct ingestion testing
instructions: |
  # Sentry Error Tracking

  Operates Sentry end-to-end: SDK initialization, release/deploy management, and ingest verification.

  ## What this skill does

  - Bootstraps the Python SDK with DSN, environment, and release context
  - Creates releases and links commits with sentry-cli
  - Sends raw events to the ingest endpoint for smoke testing

  ## When to use

  - Onboarding a new service to Sentry
  - Release hygiene: source maps and commits per release
  - Verifying the ingest pipeline with a test event

  ## Real commands

  ```bash
  # SDK installation
  pip install sentry-sdk

  # Release management
  sentry-cli releases new -p backend 2026.08.1
  sentry-cli releases set-commits --auto 2026.08.1
  sentry-cli releases deploys 2026.08.1 new -e production
  sentry-cli releases list -p backend

  # Raw ingest smoke test
  curl -X POST "https://o1.ingest.sentry.io/api/0/store/?sentry_key=DSN_PUBLIC" \
    -H 'Content-Type: application/json' \
    -d '{"event_id":"0123456789abcdef0123456789abcdef","message":"smoke","level":"warning"}'
  ```

  ## SDK init

  ```python
  import sentry_sdk

  sentry_sdk.init(
      dsn="https://public@o1.ingest.sentry.io/1234",
      environment="production",
      release="2026.08.1",
      traces_sample_rate=0.1,
  )
  ```

  ## Testing

  ```bash
  sentry-cli releases new -p backend smoke-$(date +%s)
  curl -X POST "https://o1.ingest.sentry.io/api/0/store/?sentry_key=DSN_PUBLIC" \
    -d '{"message":"test","level":"info"}'
  ```

  ## Best practices

  - Set release and environment in init; filters depend on them
  - Run sentry-cli set-commits in CI right after deploy
  - Keep the DSN public key safe in client code but never the auth token
examples:
  - input: Attach commits to a release
    output: sentry-cli releases new -p backend 2026.08.1 && sentry-cli releases set-commits --auto 2026.08.1 in CI after the deploy.
  - input: Is ingestion working?
    output: POST a raw event with curl to /api/0/store/?sentry_key=DSN_PUBLIC and look for it in the project's Issues feed.
platforms:
  claude_code:
    tools:
      - Bash
      - Read
      - Write
      - Edit
    model: inherit
  cursor:
    rule_type: auto
  github_copilot:
    prompt_file: sentry-error-tracking.md
    extension: github.copilot
  windsurf:
    model: inherit
    tools:
      - bash
      - read
      - write
      - edit
  opencode:
    plugin: opencode-sentry-error-tracking
  generic:
    system_prompt: "You own Sentry Error Tracking end to end. You do it through sentry-release-workflow: Initialize the SDK, track releases/deploys, and ingest test events. The commands are real, e.g. `pip install sentry-sdk`. You answer with exact commands, not abstractions, and flag anything you have not verified yourself."
    available_tools:
      - bash
      - read
      - write
      - edit
