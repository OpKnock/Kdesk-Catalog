{
  "name": "slo-sli",
  "display_name": "Slo Sli",
  "category": "api",
  "subcategory": "general",
  "description": "Hand-crafted SLO/SLI skill: compute error budgets from Prometheus rate queries, validate rules with promtool, and design SLIs for availability and latency.",
  "version": "2.0.0",
  "tags": [
    "api"
  ],
  "author": "Kdesk-Catalog",
  "license": "MIT",
  "prerequisites": [
    "promtool"
  ],
  "capabilities": [
    {
      "name": "slo-computation",
      "description": "Define SLIs and compute SLO error budgets with Prometheus",
      "commands": [
        "promtool query instant --url=http://localhost:9090 'rate(http_requests_total{job=\"api\",code=~\"5..\"}[5m])'",
        "curl -g 'http://localhost:9090/api/v1/query?query=sum(rate(http_requests_total[1h]))' | jq '.data.result'",
        "promtool check rules slo.rules.yml",
        "curl -g 'http://localhost:9090/api/v1/query?query=sum(rate(http_requests_total{job=\"api\",code!~\"5..\"}[30d])) / sum(rate(http_requests_total{job=\"api\"}[30d]))' | jq '.data.result[0].value[1]'"
      ],
      "examples": [
        "promtool query instant --url=http://localhost:9090 'sum(rate(http_requests_total{job=\"api\"}[5m]))'",
        "promtool check rules slo.rules.yml",
        "curl -g 'http://localhost:9090/api/v1/query?query=histogram_quantile(0.99, sum(rate(http_request_duration_seconds_bucket[5m])) by (le))' | jq '.data.result[0].value[1]'"
      ],
      "parameters": [
        {
          "name": "job",
          "type": "string",
          "description": "Prometheus job label for the service"
        },
        {
          "name": "window",
          "type": "string",
          "description": "Evaluation window, e.g. 30d"
        },
        {
          "name": "slo_target",
          "type": "float",
          "description": "Availability target like 0.99"
        }
      ]
    }
  ],
  "knowledge": [
    {
      "title": "Prometheus HTTP API",
      "type": "documentation",
      "source": "https://prometheus.io/docs/prometheus/latest/querying/api/",
      "description": "query, query_range, targets endpoints"
    },
    {
      "title": "Google SRE workbook SLOs",
      "type": "documentation",
      "source": "https://sre.google/workbook/implementing-slos/",
      "description": "SLI selection and error budget design"
    }
  ],
  "instructions": "# SLO / SLI\n\nHand-crafted skill for defining SLIs and computing SLO error budgets.\n\n## What this skill does\n\n- Computes availability and latency SLIs from Prometheus counters\n- Evaluates error budget burn over 30d windows\n- Validates recording rules with promtool before deploying\n\n## When to use\n\n- Defining reliability targets for a new service\n- Checking whether an incident burned the budget\n- Alerting on burn rate instead of raw errors\n\n## Real commands\n\n```bash\n# Error rate over 5m\npromtool query instant --url=http://localhost:9090 'rate(http_requests_total{job=\"api\",code=~\"5..\"}[5m])'\n\n# Total requests per hour\ncurl -g 'http://localhost:9090/api/v1/query?query=sum(rate(http_requests_total[1h]))' | jq '.data.result'\n\n# 30-day availability SLI: good / total\ncurl -g 'http://localhost:9090/api/v1/query?query=sum(rate(http_requests_total{job=\"api\",code!~\"5..\"}[30d])) / sum(rate(http_requests_total{job=\"api\"}[30d]))' | jq '.data.result[0].value[1]'\n\n# p99 latency from a histogram\ncurl -g 'http://localhost:9090/api/v1/query?query=histogram_quantile(0.99, sum(rate(http_request_duration_seconds_bucket[5m])) by (le))' | jq '.data.result[0].value[1]'\n\n# Validate SLO rules before reload\npromtool check rules slo.rules.yml\n```\n\n## Error budget rule\n\n```yaml\ngroups:\n  - name: slo.rules\n    rules:\n      - record: slo:api:error_budget_remaining\n        expr: 1 - ((1 - sli:api:availability:ratio30d) / 0.01)\n```\n\n## Testing\n\n```bash\npromtool check rules slo.rules.yml\npromtool query instant --url=http://localhost:9090 'slo:api:error_budget_remaining'\n```\n\n## Best practices\n\n- Pick SLIs you can measure: availability from status codes, latency from histograms\n- Use 30d windows for budget; alert on fast burn (5% budget in 6h)\n- Never change the SLI definition retroactively\n",
  "examples": [
    {
      "input": "What is the API's 30-day availability?",
      "output": "Run the good/total rate query over [30d] and compare with the target; the ratio like 0.9947 is the availability SLI."
    },
    {
      "input": "Are we burning budget fast?",
      "output": "Alert on burn rate: if the [5m] error rate exceeds budget/28d equivalent, page; validate rules with promtool check rules slo.rules.yml."
    }
  ],
  "platforms": {
    "claude_code": {
      "tools": [
        "Bash",
        "Read",
        "Write",
        "Edit"
      ],
      "model": "inherit"
    },
    "cursor": {
      "rule_type": "auto"
    },
    "github_copilot": {
      "prompt_file": "slo-sli.md",
      "extension": "github.copilot"
    },
    "windsurf": {
      "model": "inherit",
      "tools": [
        "bash",
        "read",
        "write",
        "edit"
      ]
    },
    "opencode": {
      "plugin": "opencode-slo-sli"
    },
    "generic": {
      "system_prompt": "You own Slo Sli end to end. You do it through slo-computation: Define SLIs and compute SLO error budgets with Prometheus. The commands are real, e.g. `promtool query instant --url=http://localhost:9090...`. You run and verify the real CLI commands before you claim anything.",
      "available_tools": [
        "bash",
        "read",
        "write",
        "edit"
      ]
    }
  },
  "created_at": "2024-01-01T00:00:00Z",
  "updated_at": "2024-01-01T00:00:00Z",
  "id": "slo-sli",
  "type": "skill",
  "skills": [],
  "tools": [
    "Bash",
    "Read",
    "Write",
    "Edit"
  ],
  "inputs": {
    "parameters": [
      {
        "capability": "slo-computation",
        "name": "job",
        "type": "string",
        "description": "Prometheus job label for the service"
      },
      {
        "capability": "slo-computation",
        "name": "window",
        "type": "string",
        "description": "Evaluation window, e.g. 30d"
      },
      {
        "capability": "slo-computation",
        "name": "slo_target",
        "type": "float",
        "description": "Availability target like 0.99"
      }
    ]
  },
  "outputs": {},
  "dependencies": [
    "promtool"
  ],
  "conversion": {
    "tool": "Kdesk-Catalog yaml-to-json",
    "schema": "definition-v1",
    "source_yaml": "api/slo-sli-skill.yaml",
    "derived": {
      "skills": "none",
      "tools": "platforms.claude_code.tools",
      "inputs": "capability.parameters",
      "outputs": "none (not present in source YAML)",
      "dependencies": "prerequisites"
    }
  },
  "_provenance": {
    "generated_by": "Kdesk-Catalog yaml-to-json",
    "generator_version": "1.0.0",
    "schema": "definition-v1",
    "source": "universal-agents/api/slo-sli-skill.yaml",
    "checksum": "523d063046eb7948a41ae05e7aab57cb7c769935517931b2ce430c13beedb3de"
  }
}
