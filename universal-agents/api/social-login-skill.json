{
  "name": "social-login",
  "display_name": "Social Login",
  "category": "api",
  "subcategory": "general",
  "description": "Hand-crafted social login skill: OAuth2 flows with GitHub and Google, access token exchange, userinfo retrieval, and callback wiring for social sign-in.",
  "version": "2.0.0",
  "tags": [
    "api"
  ],
  "author": "Kdesk-Catalog",
  "license": "MIT",
  "capabilities": [
    {
      "name": "oauth-social-login",
      "description": "Implement OAuth2 social login with GitHub and Google",
      "commands": [
        "curl -X POST https://github.com/login/oauth/access_token -H \"Accept: application/json\" -d \"client_id=$GITHUB_CLIENT_ID&client_secret=$GITHUB_CLIENT_SECRET&code=$CODE\" | jq -r .access_token",
        "curl -s -H \"Authorization: Bearer $ACCESS_TOKEN\" https://api.github.com/user | jq '{login, id, email}'",
        "curl -X POST https://oauth2.googleapis.com/token -d \"client_id=$GOOGLE_CLIENT_ID&client_secret=$GOOGLE_CLIENT_SECRET&code=$CODE&redirect_uri=http://localhost:3000/cb&grant_type=authorization_code\" | jq -r .access_token",
        "curl -s -H \"Authorization: Bearer $ACCESS_TOKEN\" https://www.googleapis.com/oauth2/v3/userinfo | jq '.sub, .email'"
      ],
      "examples": [
        "curl -X POST https://github.com/login/oauth/access_token -H \"Accept: application/json\" -d \"client_id=$GITHUB_CLIENT_ID&client_secret=$GITHUB_CLIENT_SECRET&code=$CODE\" | jq -r .access_token",
        "curl -s -H \"Authorization: Bearer $ACCESS_TOKEN\" https://www.googleapis.com/oauth2/v3/userinfo | jq '.email'",
        "curl -s -H \"Authorization: Bearer $ACCESS_TOKEN\" https://api.github.com/user | jq '{login, id}'"
      ],
      "parameters": [
        {
          "name": "provider",
          "type": "string",
          "description": "github or google"
        },
        {
          "name": "client_id",
          "type": "string",
          "description": "OAuth app client ID"
        },
        {
          "name": "redirect_uri",
          "type": "string",
          "description": "Registered callback URI"
        }
      ]
    }
  ],
  "knowledge": [
    {
      "title": "GitHub OAuth apps",
      "type": "documentation",
      "source": "https://docs.github.com/en/apps/oauth-apps/building-oauth-apps/authorizing-oauth-apps",
      "description": "GitHub authorization code flow"
    },
    {
      "title": "Google OAuth2 for web servers",
      "type": "documentation",
      "source": "https://developers.google.com/identity/protocols/oauth2/web-server",
      "description": "Google token endpoint and userinfo"
    }
  ],
  "instructions": "# Social Login\n\nHand-crafted skill for OAuth2 social sign-in with GitHub and Google.\n\n## What this skill does\n\n- Completes the authorization code exchange for GitHub and Google\n- Fetches profile data from userinfo endpoints\n- Maps provider identity to local accounts\n\n## When to use\n\n- Adding Sign in with GitHub/Google to an app\n- Debugging token exchange failures in CI\n- Verifying the claims your app receives\n\n## Real commands\n\n```bash\n# GitHub: exchange the code (from /login/oauth/authorize redirect)\ncurl -X POST https://github.com/login/oauth/access_token -H \"Accept: application/json\" -d \"client_id=$GITHUB_CLIENT_ID&client_secret=$GITHUB_CLIENT_SECRET&code=$CODE\" | jq -r .access_token\n\n# GitHub profile\ncurl -s -H \"Authorization: Bearer $ACCESS_TOKEN\" https://api.github.com/user | jq '{login, id, email}'\n\n# Google: exchange the code\ncurl -X POST https://oauth2.googleapis.com/token -d \"client_id=$GOOGLE_CLIENT_ID&client_secret=$GOOGLE_CLIENT_SECRET&code=$CODE&redirect_uri=http://localhost:3000/cb&grant_type=authorization_code\" | jq -r .access_token\n\n# Google userinfo\ncurl -s -H \"Authorization: Bearer $ACCESS_TOKEN\" https://www.googleapis.com/oauth2/v3/userinfo | jq '.sub, .email'\n```\n\n## Flow\n\n1. Redirect user to the provider's authorize URL\n2. Provider redirects back with ?code=\n3. Exchange the code for tokens (commands above)\n4. Fetch profile, upsert local user by provider id\n\n## Testing\n\n```bash\n# Local callback test\ncurl -X POST https://github.com/login/oauth/access_token -H \"Accept: application/json\" -d \"client_id=$GITHUB_CLIENT_ID&client_secret=$GITHUB_CLIENT_SECRET&code=test\" | jq\n```\n\n## Best practices\n\n- Match users by provider id, not email (emails change)\n- Store only the tokens you need; prefer short-lived access tokens\n- Verify the state parameter on the callback to block CSRF\n",
  "examples": [
    {
      "input": "Add Sign in with GitHub",
      "output": "Register an OAuth app, redirect to /login/oauth/authorize, exchange ?code= for a token, and fetch /user to create the local account keyed by id."
    },
    {
      "input": "Callback returned bad_verification_code",
      "output": "The code is single-use and expires fast; re-run the authorize flow to get a fresh code, or use the state/error fields from the callback."
    }
  ],
  "platforms": {
    "claude_code": {
      "tools": [
        "Bash",
        "Read",
        "Write",
        "Edit"
      ],
      "model": "inherit"
    },
    "cursor": {
      "rule_type": "auto"
    },
    "github_copilot": {
      "prompt_file": "social-login.md",
      "extension": "github.copilot"
    },
    "windsurf": {
      "model": "inherit",
      "tools": [
        "bash",
        "read",
        "write",
        "edit"
      ]
    },
    "opencode": {
      "plugin": "opencode-social-login"
    },
    "generic": {
      "system_prompt": "This is your domain: Social Login. You do it through oauth-social-login: Implement OAuth2 social login with GitHub and Google. The commands are real, e.g. `curl -X POST https://github.com/login/oauth/access_token -H Accept: application/json -d...`. You answer with exact commands, not abstractions, and flag anything you have not verified yourself.",
      "available_tools": [
        "bash",
        "read",
        "write",
        "edit"
      ]
    }
  },
  "created_at": "2024-01-01T00:00:00Z",
  "updated_at": "2024-01-01T00:00:00Z",
  "id": "social-login",
  "type": "skill",
  "skills": [],
  "tools": [
    "Bash",
    "Read",
    "Write",
    "Edit"
  ],
  "inputs": {
    "parameters": [
      {
        "capability": "oauth-social-login",
        "name": "provider",
        "type": "string",
        "description": "github or google"
      },
      {
        "capability": "oauth-social-login",
        "name": "client_id",
        "type": "string",
        "description": "OAuth app client ID"
      },
      {
        "capability": "oauth-social-login",
        "name": "redirect_uri",
        "type": "string",
        "description": "Registered callback URI"
      }
    ]
  },
  "outputs": {},
  "dependencies": [],
  "conversion": {
    "tool": "Kdesk-Catalog yaml-to-json",
    "schema": "definition-v1",
    "source_yaml": "api/social-login-skill.yaml",
    "derived": {
      "skills": "none",
      "tools": "platforms.claude_code.tools",
      "inputs": "capability.parameters",
      "outputs": "none (not present in source YAML)",
      "dependencies": "none"
    }
  },
  "_provenance": {
    "generated_by": "Kdesk-Catalog yaml-to-json",
    "generator_version": "1.0.0",
    "schema": "definition-v1",
    "source": "universal-agents/api/social-login-skill.yaml",
    "checksum": "96a3f87b800968e449e92c9b13876dc26aa1708337715e9170e97b1a0556091a"
  }
}
