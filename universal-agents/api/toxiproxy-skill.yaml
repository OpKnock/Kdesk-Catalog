name: toxiproxy
display_name: Toxiproxy
category: api
subcategory: general
description: Inject network faults into connections with it.
version: 2.0.0
tags:
- api
author: Kdesk
license: MIT
prerequisites:
- toxiproxy-cli
capabilities:
- name: toxiproxy-faults
  description: Inject network faults into connections with Toxiproxy
  commands:
  - toxiproxy-cli create -l localhost:26379 -u localhost:6379 redis
  - toxiproxy-cli list
  - toxiproxy-cli toxic add -t latency -a latency=1000 -a jitter=100 redis
  - toxiproxy-cli toxic add -t timeout -a timeout=5000 redis
  - toxiproxy-cli toxic list redis
  - toxiproxy-cli delete redis
  examples:
  - toxiproxy-cli create -l localhost:26379 -u localhost:6379 redis
  - toxiproxy-cli toxic add -t latency -a latency=1000 -a jitter=100 redis
  - toxiproxy-cli toxic add -t bandwidth -a rate=50 redis
  parameters:
  - name: proxy_name
    type: string
    description: Proxy name, e.g. redis
  - name: listen
    type: string
    description: Client-facing listen address
  - name: upstream
    type: string
    description: Real service address
knowledge:
- title: Toxiproxy repo
  type: documentation
  source: https://github.com/Shopify/toxiproxy
  description: CLI usage and toxic reference
- title: Toxiproxy toxic types
  type: documentation
  source: https://github.com/Shopify/toxiproxy#toxics
  description: latency, timeout, bandwidth, loss, corrupt
instructions: '# Toxiproxy


  Hand-crafted skill for fault injection with Shopify Toxiproxy.


  ## What this skill does


  - Proxies TCP connections to a target service

  - Injects latency, timeouts, bandwidth, and loss mid-stream

  - Toggles faults on and off without restarting anything


  ## When to use


  - Testing client retries and timeouts against real sockets

  - Chaos drills on databases and caches

  - Reproducing network bug reports deterministically


  ## Real commands


  ```bash

  # Proxy traffic: clients hit 26379, real redis at 6379

  toxiproxy-cli create -l localhost:26379 -u localhost:6379 redis

  toxiproxy-cli list


  # Add 1000ms latency with jitter

  toxiproxy-cli toxic add -t latency -a latency=1000 -a jitter=100 redis


  # Time out all traffic after 5s

  toxiproxy-cli toxic add -t timeout -a timeout=5000 redis


  # Limit bandwidth to 50 kbps

  toxiproxy-cli toxic add -t bandwidth -a rate=50 redis


  # Inspect and clean up

  toxiproxy-cli toxic list redis

  toxiproxy-cli delete redis

  ```


  ## Toxic types


  - latency: delay packets

  - timeout: stall responses for a duration

  - bandwidth: cap throughput

  - loss: drop a percentage of packets

  - corrupt: corrupt bytes


  ## Test recipe


  ```bash

  toxiproxy-cli create -l localhost:26379 -u localhost:6379 redis

  redis-cli -p 26379 ping          # baseline

  toxiproxy-cli toxic add -t latency -a latency=2000 redis

  redis-cli -p 26379 ping          # now slow: client must handle it

  toxiproxy-cli delete redis

  ```


  ## Best practices


  - Name proxies after the dependency they represent

  - Combine latency with client timeout settings to verify failures

  - Clean up proxies in CI teardown steps

  '
examples:
- input: Simulate a slow database
  output: toxiproxy-cli create -l localhost:26379 -u localhost:6379 redis; toxiproxy-cli toxic add -t
    latency -a latency=2000 redis; point the app at 26379.
- input: Simulate a dead connection
  output: toxiproxy-cli toxic add -t timeout -a timeout=30000 redis causes requests to hang until the
    client's own timeout.
platforms:
  claude_code:
    tools:
    - Bash
    - Read
    - Write
    - Edit
    model: inherit
  cursor:
    rule_type: auto
  github_copilot:
    prompt_file: toxiproxy.md
    extension: github.copilot
  windsurf:
    model: inherit
    tools:
    - bash
    - read
    - write
    - edit
  opencode:
    plugin: opencode-toxiproxy
  generic:
    system_prompt: 'This is your domain: Toxiproxy. You do it through toxiproxy-faults: Inject network
      faults into connections with Toxiproxy. The commands are real, e.g. `toxiproxy-cli create -l localhost:26379
      -u localhost:6379 redis`. You prefer the shortest safe path, and you reproduce failures before diagnosing
      them.'
    available_tools:
    - bash
    - read
    - write
    - edit
created_at: '2025-06-15T00:00:00Z'
updated_at: '2026-08-19T00:00:00Z'
