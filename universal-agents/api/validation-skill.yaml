name: validation
display_name: Validation
version: 2.0.0
type: skill
category: api
subcategory: general
description: "Validates JSON payloads against JSON Schema (draft 2020-12) using ajv-cli and Python jsonschema. Compiles schemas to catch errors before deployment, enforces contracts in CI pipelines, and performs quick shape checks with jq."
author: Mehul Wagde
created_at: '2025-06-15T00:00:00Z'
updated_at: '2026-08-19T00:00:00Z'
tags:
  - api
  - json-schema
  - validation
platforms:
  claude_code:
    tools: [Bash, Read, Write, Edit]
    model: inherit
  cursor:
    rule_type: auto
  github_copilot:
    prompt_file: validation.md
    extension: github.copilot
  windsurf:
    model: inherit
    tools: [bash, read, write, edit]
  opencode:
    plugin: opencode-validation
  generic:
    system_prompt: "You take the Validation seat. You do it through schema-validate: Validate JSON payloads against JSON Schema. The commands are real, e.g. `npx ajv-cli validate -s schema.json -d data.json --strict=false`. You answer with exact commands, not abstractions, and flag anything you have not verified yourself."
    available_tools: [bash, read, write, edit]
capabilities:
  - name: schema-validate
    description: Validate JSON payloads against JSON Schema
    commands:
      - 'npx ajv-cli validate -s schema.json -d data.json --strict=false'
      - 'python -m jsonschema -i data.json schema.json'
      - 'python -c "import json, jsonschema; jsonschema.validate(json.load(open(\"data.json\")), json.load(open(\"schema.json\"))); print(\"valid\")"'
      - 'npx ajv-cli compile -s schema.json'
      - 'jq "type == \"object\" and (.id | type == \"string\") and (.price | type == \"number\")" data.json'
    examples:
      - 'npx ajv-cli validate -s schema.json -d data.json --strict=false'
      - 'python -m jsonschema -i data.json schema.json'
      - 'jq "has(\"required_field\")" data.json'
    parameters:
      - name: schema
        type: string
        description: Path to JSON Schema file
      - name: data
        type: string
        description: Path to data file to validate
      - name: strict
        type: boolean
        description: Reject unknown keywords
prerequisites:
  - npx
  - python
  - jq
tools: [npx, python, jq]
examples:
  - input: Is my response shape correct?
    output: npx ajv-cli validate -s schema.json -d response.json --strict=false prints errors per failing keyword.
  - input: Is this schema itself valid?
    output: npx ajv-cli compile -s schema.json compiles or prints the meta-schema error.
instructions: |
  # Validation

  Hand-crafted skill for schema validation of JSON payloads.

  ## What this skill does

  - Validates payloads against JSON Schema (draft 2020-12)
  - Compiles schemas to check them before shipping
  - Guards pipelines and webhooks against bad shapes

  ## When to use

  - CI gates on API response shapes
  - Validating webhook payloads before processing
  - Enforcing contracts between producers and consumers

  ## Real commands

  ```bash
  # Validate a document
  npx ajv-cli validate -s schema.json -d data.json --strict=false

  # Python alternative
  python -m jsonschema -i data.json schema.json
  python -c "import json, jsonschema; jsonschema.validate(json.load(open('data.json')), json.load(open('schema.json'))); print('valid')"

  # Compile-only check (schema itself must be valid)
  npx ajv-cli compile -s schema.json

  # Quick jq shape checks
  jq 'type == "object" and (.id | type == "string") and (.price | type == "number")' data.json
  jq 'has("required_field")' data.json
  ```

  ## Example schema

  ```json
  {
    "$schema": "https://json-schema.org/draft/2020-12/schema",
    "type": "object",
    "required": ["id", "price"],
    "properties": {
      "id": {"type": "string"},
      "price": {"type": "number", "minimum": 0}
    }
  }
  ```

  ## Testing

  ```bash
  npx ajv-cli validate -s schema.json -d data.json --strict=false
  npx ajv-cli compile -s schema.json
  ```

  ## Best practices

  - Ship the schema with the contract, not after
  - Keep --strict off only when schemas use unknown extensions
  - Fail CI on invalid payloads, never warn
knowledge:
  - title: JSON Schema spec
    type: documentation
    source: https://json-schema.org/
    description: Draft 2020-12 keywords
  - title: Ajv CLI docs
    type: documentation
    source: https://ajv.js.org/guide/cli.html
    description: validate and compile commands
  - title: Python jsonschema
    type: documentation
    source: https://python-jsonschema.readthedocs.io/
    description: Python validation library reference