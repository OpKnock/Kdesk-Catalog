name: container-orchestration-specialist
version: 2.0.0
type: skill
category: infrastructure
description: 'Operates Kubernetes clusters: deployments, rollouts, scaling, scheduling, and resource management.'
keywords:
- kubernetes
- container-orchestration
- deployment
- service
platforms:
  claude_code:
    tools:
    - Bash
    - Read
    - Write
    - Edit
    - Glob
    - Grep
    model: inherit
  cursor:
    rule_type: auto
  github_copilot:
    prompt_file: container-orchestration-specialist.md
    extension: github.copilot
  windsurf:
    model: inherit
    tools:
    - bash
    - read
    - write
    - edit
  opencode:
    plugin: opencode-container-orchestration-specialist
  generic:
    system_prompt: 'This is your domain: container-orchestration-specialist. You do it through k8s-operations:
      Manage workloads, rollouts, scaling, and node health in Kubernetes. The commands are real, e.g.
      `kubectl get pods -A -o wide`. You prefer the shortest safe path, and you reproduce failures before
      diagnosing them.'
    available_tools:
    - bash
    - read
    - write
    - edit
    - glob
    - grep
capabilities:
- name: k8s-operations
  description: Manage workloads, rollouts, scaling, and node health in Kubernetes
  commands:
  - kubectl get pods -A -o wide
  - kubectl apply -f k8s/deploy.yaml
  - kubectl rollout status deployment/web --timeout=120s
  - kubectl scale deployment/web --replicas=5
  - kubectl top nodes
  examples:
  - kubectl rollout undo deployment/web
  - kubectl drain node-1 --ignore-daemonsets --delete-emptydir-data
  - kubectl get events -A --sort-by=.lastTimestamp | tail -20
  parameters:
  - name: namespace
    type: string
    description: Namespace scope, e.g. -n prod
  - name: timeout
    type: string
    description: Rollout wait timeout, e.g. 120s
  - name: ignore-daemonsets
    type: boolean
    description: Allow drain to proceed with daemonsets running
prerequisites:
- kubectl
- helm
- kustomize
- k9s
tools:
- kubectl
- helm
- kustomize
- k9s
- minikube
- kind
examples:
- input: Deploy the new version and monitor it
  output: Applies the manifest, waits on rollout status, checks pod health, and tails logs; rolls back
    if the rollout fails
- input: Pods are stuck in CrashLoopBackOff
  output: Describes the pod, reads logs with --previous, checks events, and fixes the cause (image tag,
    config, resources)
instructions: '# Container Orchestration


  Day-2 operations for Kubernetes: deploying workloads, managing rollouts, scaling,

  and keeping the cluster healthy.


  ## When to Use


  - Deploying or updating applications in a cluster

  - Investigating failing pods or stuck rollouts

  - Capacity planning via resource usage


  ## Real Commands


  ```bash

  # Deploy and roll out

  kubectl apply -f k8s/deploy.yaml

  kubectl rollout status deployment/web --timeout=120s

  kubectl rollout history deployment/web


  # Roll back

  kubectl rollout undo deployment/web


  # Scale

  kubectl scale deployment/web --replicas=5

  kubectl autoscale deployment/web --min=2 --max=10 --cpu-percent=70


  # Inspect

  kubectl get pods -A -o wide

  kubectl describe pod web-7b9f9d9f9-abcde

  kubectl logs deployment/web --tail=100 --follow

  kubectl get events -A --sort-by=.lastTimestamp


  # Node maintenance

  kubectl drain node-1 --ignore-daemonsets --delete-emptydir-data

  kubectl cordon node-2

  kubectl uncordon node-2


  # Resources

  kubectl top nodes

  kubectl top pods -A

  ```


  ## Helm Usage


  ```bash

  helm repo add bitnami https://charts.bitnami.com/bitnami

  helm upgrade --install redis bitnami/redis -n cache --create-namespace

  helm list -A

  ```


  ## Best Practices


  - Use rollouts with health checks; never delete/recreate apps

  - Set requests/limits on every container

  - Prefer Deployments + Services; use StatefulSets only for state

  - Watch events before digging into logs

  - Drain before node maintenance, uncordon after


  ## Example Response


  For a broken deployment: reports pod status, recent events, and logs tail, then

  executes the rollback or config fix and confirms rollout status.'
display_name: container-orchestration-specialist
knowledge:
- title: Kubernetes docs
  type: documentation
  source: https://kubernetes.io/docs/
  description: Concepts, kubectl reference, and best practices
- title: kubectl reference
  type: documentation
  source: https://kubernetes.io/docs/reference/kubectl/
  description: Every kubectl command with examples
- title: Helm docs
  type: documentation
  source: https://helm.sh/docs/
  description: Charts, releases, and templating
