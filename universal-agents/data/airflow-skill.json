{
  "name": "airflow",
  "display_name": "Airflow",
  "category": "data",
  "subcategory": "general",
  "description": "Builds and operates Airflow DAGs: local setup, DAG testing, task runs, and backfills.",
  "version": "2.0.0",
  "tags": [
    "data"
  ],
  "author": "Kdesk-Catalog",
  "license": "MIT",
  "prerequisites": [
    "airflow"
  ],
  "capabilities": [
    {
      "name": "airflow-operations",
      "description": "Manage Airflow metadata, users, DAGs, and task runs from the CLI",
      "commands": [
        "airflow db init",
        "airflow users create --username admin --firstname A --lastname A --role Admin --email admin@example.com",
        "airflow dags list",
        "airflow dags trigger my_dag --conf '{\"env\": \"prod\"}'",
        "airflow tasks test my_dag extract_task 2024-01-01"
      ],
      "examples": [
        "airflow dags list-import-errors",
        "airflow dags backfill -s 2024-01-01 -e 2024-01-10 my_dag",
        "airflow scheduler --num-runs 1"
      ],
      "parameters": [
        {
          "name": "conf",
          "type": "string",
          "description": "JSON config passed to the triggered run"
        },
        {
          "name": "log-level",
          "type": "string",
          "description": "Logging level for the command, e.g. DEBUG"
        },
        {
          "name": "subdir",
          "type": "string",
          "description": "Directory to search for DAG files"
        }
      ]
    }
  ],
  "knowledge": [
    {
      "title": "Airflow docs",
      "type": "documentation",
      "source": "https://airflow.apache.org/docs/",
      "description": "CLI reference, DAG authoring, and operator guides"
    },
    {
      "title": "Airflow best practices",
      "type": "documentation",
      "source": "https://airflow.apache.org/docs/apache-airflow/stable/best-practices.html",
      "description": "DAG structure, idempotency, and testing patterns"
    }
  ],
  "instructions": "# Airflow\n\nWorkflow orchestration: DAG authoring, scheduling, backfills, and troubleshooting\nvia the Airflow CLI.\n\n## When to Use\n\n- Scheduling ETL and ML pipelines\n- Re-running failed or historical ranges (backfill)\n- Testing individual tasks without running a full DAG\n\n## Real Commands\n\n```bash\n# Install (recommended via pipx/venv)\npip install apache-airflow\n\n# First-time database setup\nairflow db init\nairflow db upgrade\n\n# Create an admin user\nairflow users create --username admin --role Admin --email admin@example.com --firstname Admin --lastname User\n\n# Start services (two terminals)\nairflow scheduler\nairflow webserver --port 8080\n\n# DAG management\nairflow dags list\nairflow dags list-import-errors\nairflow dags show my_dag\n\n# Trigger a run\nairflow dags trigger my_dag --conf '{\"env\": \"staging\"}'\n\n# Test one task with a fixed execution date\nairflow tasks test my_dag extract 2024-01-01\n\n# Backfill a range\nairflow dags backfill -s 2024-01-01 -e 2024-01-10 my_dag --reset-dagruns\n```\n\n## Minimal DAG (dags/example.py)\n\n```python\nfrom datetime import datetime, timedelta\nfrom airflow import DAG\nfrom airflow.operators.bash import BashOperator\n\nwith DAG(\"example\", start_date=datetime(2024, 1, 1), schedule=\"@daily\", catchup=False) as dag:\n    extract = BashOperator(task_id=\"extract\", bash_command=\"python /app/extract.py\")\n    load = BashOperator(task_id=\"load\", bash_command=\"python /app/load.py\")\n    extract >> load\n```\n\n## Best Practices\n\n- Keep DAGs idempotent and deterministic\n- Test with `airflow tasks test` before scheduling\n- Set proper `start_date` and use catchup deliberately\n- Put secrets in Airflow Variables/Connections, not code\n- Monitor with `airflow dags list-runs -d my_dag`\n\n## Example Response\n\nFor a failed DAG run: identifies the failing task from logs, suggests the fix,\nthen re-runs the single task or backfills the range.",
  "examples": [
    {
      "input": "Set up Airflow locally",
      "output": "Runs airflow db init, creates the admin user, starts scheduler and webserver, and validates the DAG imports"
    },
    {
      "input": "Re-run last week's failed pipeline",
      "output": "Checks list-runs for failures, fixes the task code, and runs airflow dags backfill for the affected date range"
    }
  ],
  "platforms": {
    "claude_code": {
      "tools": [
        "Bash",
        "Read",
        "Write",
        "Edit"
      ],
      "model": "inherit"
    },
    "cursor": {
      "rule_type": "auto"
    },
    "github_copilot": {
      "prompt_file": "airflow.md",
      "extension": "github.copilot"
    },
    "windsurf": {
      "model": "inherit",
      "tools": [
        "bash",
        "read",
        "write",
        "edit"
      ]
    },
    "opencode": {
      "plugin": "opencode-airflow"
    },
    "generic": {
      "system_prompt": "Your lane is Airflow. You do it through airflow-operations: Manage Airflow metadata, users, DAGs, and task runs from the CLI. The commands are real, e.g. `airflow db init`. You run and verify the real CLI commands before you claim anything.",
      "available_tools": [
        "bash",
        "read",
        "write",
        "edit"
      ]
    }
  },
  "created_at": "2024-01-01T00:00:00Z",
  "updated_at": "2024-01-01T00:00:00Z",
  "id": "airflow",
  "type": "skill",
  "skills": [],
  "tools": [
    "Bash",
    "Read",
    "Write",
    "Edit"
  ],
  "inputs": {
    "parameters": [
      {
        "capability": "airflow-operations",
        "name": "conf",
        "type": "string",
        "description": "JSON config passed to the triggered run"
      },
      {
        "capability": "airflow-operations",
        "name": "log-level",
        "type": "string",
        "description": "Logging level for the command, e.g. DEBUG"
      },
      {
        "capability": "airflow-operations",
        "name": "subdir",
        "type": "string",
        "description": "Directory to search for DAG files"
      }
    ]
  },
  "outputs": {},
  "dependencies": [
    "airflow"
  ],
  "conversion": {
    "tool": "Kdesk-Catalog yaml-to-json",
    "schema": "definition-v1",
    "source_yaml": "data/airflow-skill.yaml",
    "derived": {
      "skills": "none",
      "tools": "platforms.claude_code.tools",
      "inputs": "capability.parameters",
      "outputs": "none (not present in source YAML)",
      "dependencies": "prerequisites"
    }
  },
  "_provenance": {
    "generated_by": "Kdesk-Catalog yaml-to-json",
    "generator_version": "1.0.0",
    "schema": "definition-v1",
    "source": "universal-agents/data/airflow-skill.yaml",
    "checksum": "aa837d6bfdf4eb60397a5e4ceb7bad4306b83dd3be981c1705935b6d19cbb2b7"
  }
}
