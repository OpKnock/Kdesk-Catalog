{
  "name": "kcat",
  "display_name": "kcat",
  "category": "database",
  "subcategory": "general",
  "description": "Kafka producer/consumer Swiss-army knife: kcat for producing, consuming, and inspecting Kafka topics.",
  "version": "2.0.0",
  "tags": [
    "database"
  ],
  "author": "Kdesk-Catalog",
  "license": "MIT",
  "prerequisites": [
    "kcat"
  ],
  "capabilities": [
    {
      "name": "kcat-commands",
      "description": "Produce, consume, and inspect Kafka topics with kcat",
      "commands": [
        "kcat -b localhost:9092 -t orders -P",
        "kcat -b localhost:9092 -t orders -C -o -10",
        "kcat -b localhost:9092 -L",
        "kcat -b localhost:9092 -t orders -C -f '%t %p %o %k %s\\n' -c 5",
        "kcat -b localhost:9092 -t orders -C -e -o beginning -c 10"
      ],
      "examples": [
        "echo '{\"id\":1}' | kcat -b localhost:9092 -t orders -P -l",
        "kcat -b localhost:9092 -t orders -C -q -o end -c 5",
        "kcat -b localhost:9092 -L -J | jq '.topics[0]'"
      ],
      "parameters": [
        {
          "name": "offset",
          "type": "string",
          "description": "Start offset: beginning, end, or -10"
        },
        {
          "name": "format",
          "type": "string",
          "description": "Message output format string (-f)"
        },
        {
          "name": "exit",
          "type": "boolean",
          "description": "Exit after consuming (-e) or print parsed exit codes"
        }
      ]
    }
  ],
  "knowledge": [
    {
      "title": "kcat GitHub",
      "type": "documentation",
      "source": "https://github.com/edenhill/kcat",
      "description": "Full CLI reference for produce/consume/metadata modes"
    },
    {
      "title": "kcat usage examples",
      "type": "documentation",
      "source": "https://github.com/edenhill/kcat#examples",
      "description": "Common workflows: piping, formats, headers"
    }
  ],
  "instructions": "# kcat\n\nLightweight CLI for Kafka: produce messages from stdin, consume to stdout, and\ninspect topics and brokers.\n\n## When to Use\n\n- Debugging message flow during development\n- One-off data injection for tests\n- Inspecting topic layout, offsets, and consumer info\n\n## Real Commands\n\n```bash\n# Produce from stdin (one message per line)\necho '{\"order_id\": 1, \"amount\": 99.5}' | kcat -b localhost:9092 -t orders -P\n\n# Produce from a file\nsudo kcat -b localhost:9092 -t orders -P -l messages.jsonl\n\n# Consume the last 10 messages\nsudo kcat -b localhost:9092 -t orders -C -o -10\n\n# Structured output with key/offset\nsudo kcat -b localhost:9092 -t orders -C -f '%t %p %o %k %s\\n' -c 5\n\n# Metadata listing\nsudo kcat -b localhost:9092 -L\nsudo kcat -b localhost:9092 -L -J | jq '.topics'\n```\n\n## Format Placeholders\n\n- `%t` topic, `%p` partition, `%o` offset, `%k` key, `%s` value\n- `%h` headers, `%T` timestamp\n\n## Best Practices\n\n- Use `-c N` to bound consumption in scripts\n- Pipe keys with `-K: ` delimiter when keys are needed\n- Use `-e` to exit when the end of a finite partition is reached\n- Prefer `-J` JSON metadata for scripting\n\n## Example Response\n\nProduces the test messages, consumes them back with offsets, and reports the\ntopic metadata layout.",
  "examples": [
    {
      "input": "Inject test orders into Kafka",
      "output": "Pipes the JSONL file into kcat -P and verifies by consuming -o -5"
    },
    {
      "input": "What's in this topic right now?",
      "output": "Consumes the last N messages with -C -o -N and prints formatted key/value pairs"
    }
  ],
  "platforms": {
    "claude_code": {
      "tools": [
        "Bash",
        "Read"
      ],
      "model": "inherit"
    },
    "cursor": {
      "rule_type": "auto"
    },
    "github_copilot": {
      "prompt_file": "kcat.md",
      "extension": "github.copilot"
    },
    "windsurf": {
      "model": "inherit",
      "tools": [
        "bash",
        "read",
        "write",
        "edit"
      ]
    },
    "opencode": {
      "plugin": "opencode-kcat"
    },
    "generic": {
      "system_prompt": "You take the kcat seat. You do it through kcat-commands: Produce, consume, and inspect Kafka topics with kcat. The commands are real, e.g. `kcat -b localhost:9092 -t orders -P`. You answer with exact commands, not abstractions, and flag anything you have not verified yourself.",
      "available_tools": [
        "bash",
        "read",
        "write",
        "edit"
      ]
    }
  },
  "created_at": "2024-01-01T00:00:00Z",
  "updated_at": "2024-01-01T00:00:00Z",
  "id": "kcat",
  "type": "skill",
  "skills": [],
  "tools": [
    "Bash",
    "Read"
  ],
  "inputs": {
    "parameters": [
      {
        "capability": "kcat-commands",
        "name": "offset",
        "type": "string",
        "description": "Start offset: beginning, end, or -10"
      },
      {
        "capability": "kcat-commands",
        "name": "format",
        "type": "string",
        "description": "Message output format string (-f)"
      },
      {
        "capability": "kcat-commands",
        "name": "exit",
        "type": "boolean",
        "description": "Exit after consuming (-e) or print parsed exit codes"
      }
    ]
  },
  "outputs": {},
  "dependencies": [
    "kcat"
  ],
  "conversion": {
    "tool": "Kdesk-Catalog yaml-to-json",
    "schema": "definition-v1",
    "source_yaml": "database/kcat-skill.yaml",
    "derived": {
      "skills": "none",
      "tools": "platforms.claude_code.tools",
      "inputs": "capability.parameters",
      "outputs": "none (not present in source YAML)",
      "dependencies": "prerequisites"
    }
  },
  "_provenance": {
    "generated_by": "Kdesk-Catalog yaml-to-json",
    "generator_version": "1.0.0",
    "schema": "definition-v1",
    "source": "universal-agents/database/kcat-skill.yaml",
    "checksum": "a55bcc479cf121563c04e0384c8977c028b695564f77a689d0867c406f8fc41a"
  }
}
