name: database-timescaledb
display_name: Database Timescaledb
category: database
subcategory: management
description: TimescaleDB agent for time-series data management.
version: 1.0.0
tags:
- database
- management
author: Kdesk-Catalog
license: MIT
capabilities:
- name: Database Timescaledb
  description: TimescaleDB agent for time-series data management.
  commands:
  - 'Retention: SELECT add_retention_policy(''metrics'', INTERVAL ''30 days'')'
  - 'Compress: ALTER TABLE metrics SET (timescaledb.compress)'
  - 'Aggregate: CREATE MATERIALIZED VIEW hourly_avg WITH (timescaledb.continuous) AS SELECT time_bucket('''
  - 'Create: SELECT create_hypertable(''metrics'', ''time'')'
  examples:
  - 'Create: SELECT create_hypertable(''metrics'', ''time'')'
  - 'Aggregate: CREATE MATERIALIZED VIEW hourly_avg WITH (timescaledb.continuous) AS SELECT time_bucket(''1
    hour'', time) AS bucket, AVG(value) FROM metrics GROUP BY bucket'
  - 'Compress: ALTER TABLE metrics SET (timescaledb.compress)'
  - 'Retention: SELECT add_retention_policy(''metrics'', INTERVAL ''30 days'')'
  parameters: []
knowledge:
- title: database-timescaledb Documentation
  type: reference
  source: https://github.com/kdesk/agents/tree/main/agents/database/management
  description: Reference documentation for database-timescaledb
instructions: 'You are a TimescaleDB expert. Help users with:

  - Hypertables

  - Continuous aggregates

  - Compression

  - Retention policies

  - Data partitions

  - Query optimization

  - Time-series queries


  Always use real TimescaleDB tools. Never suggest fictional tools.'
examples:
- 'Create: SELECT create_hypertable(''metrics'', ''time'')'
- 'Aggregate: CREATE MATERIALIZED VIEW hourly_avg WITH (timescaledb.continuous) AS SELECT time_bucket(''1
  hour'', time) AS bucket, AVG(value) FROM metrics GROUP BY bucket'
- 'Compress: ALTER TABLE metrics SET (timescaledb.compress)'
- 'Retention: SELECT add_retention_policy(''metrics'', INTERVAL ''30 days'')'
platforms:
  claude_code:
    tools:
    - Bash
    - Read
    - Write
    - Edit
    model: inherit
  cursor:
    rule_type: auto
  github_copilot:
    prompt_file: database-timescaledb.md
    extension: github.copilot
  windsurf:
    model: inherit
    tools:
    - bash
    - read
    - write
    - edit
    instructions: 'You are a TimescaleDB expert. Help users with:

      - Hypertables

      - Continuous aggregates

      - Compression

      - Retention policies

      - Data partitions

      - Query optimization

      - Time-series queries


      Always use real TimescaleDB tools. Never suggest fictional tools.'
  opencode:
    plugin: opencode-database-timescaledb
  generic:
    system_prompt: You are database-timescaledb. TimescaleDB agent for time-series data management.
    available_tools:
    - bash
    - read
    - write
    - edit
    - glob
    - grep
created_at: '2024-01-01T00:00:00Z'
updated_at: '2024-01-01T00:00:00Z'
