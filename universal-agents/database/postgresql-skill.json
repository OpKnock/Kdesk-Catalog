{
  "name": "postgresql",
  "display_name": "postgresql",
  "category": "database",
  "subcategory": "general",
  "description": "Operates PostgreSQL: psql queries, database lifecycle, dumps, and monitoring views.",
  "version": "2.0.0",
  "tags": [
    "database"
  ],
  "author": "Kdesk-Catalog",
  "license": "MIT",
  "prerequisites": [
    "createdb",
    "pg_dump",
    "psql"
  ],
  "capabilities": [
    {
      "name": "postgres-cli",
      "description": "Query, administer, and back up PostgreSQL databases",
      "commands": [
        "psql -U postgres -h localhost -d app",
        "createdb app -U postgres",
        "psql -U postgres -d app -f schema.sql",
        "pg_dump -U postgres -Fc app > app.dump",
        "psql -U postgres -d app -c \"SELECT pg_size_pretty(pg_database_size('app'));\""
      ],
      "examples": [
        "psql -U postgres -d app -c \"\\dt+\"",
        "psql -U postgres -d app -c \"SELECT * FROM pg_stat_activity WHERE state='active';\"",
        "pg_restore -U postgres -d app --no-owner -j4 app.dump"
      ],
      "parameters": [
        {
          "name": "host",
          "type": "string",
          "description": "Database server host (-h)"
        },
        {
          "name": "dbname",
          "type": "string",
          "description": "Database to connect to (-d)"
        },
        {
          "name": "file",
          "type": "string",
          "description": "Script file to execute (-f)"
        }
      ]
    }
  ],
  "knowledge": [
    {
      "title": "PostgreSQL docs",
      "type": "documentation",
      "source": "https://www.postgresql.org/docs/current/",
      "description": "SQL, psql, and administration reference"
    },
    {
      "title": "psql reference",
      "type": "documentation",
      "source": "https://www.postgresql.org/docs/current/app-psql.html",
      "description": "psql meta-commands and options"
    }
  ],
  "instructions": "# PostgreSQL\n\nRelational database operations: psql queries, database creation, schema scripts,\nand backups.\n\n## When to Use\n\n- Running queries and admin commands\n- Creating and managing databases\n- Backing up/restoring with pg_dump\n\n## Real Commands\n\n```bash\n# Connect\nsudo psql -U postgres -h localhost -d app\n\n# Database lifecycle\nsudo createdb app -U postgres\nsudo dropdb app -U postgres\n\n# Meta-commands\nsudo psql -U postgres -d app -c \"\\dt\"\nsudo psql -U postgres -d app -c \"\\d orders\"\n\n# Run a script\nsudo psql -U postgres -d app -f schema.sql\n\n# Backups\nsudo pg_dump -U postgres -Fc app > app.dump\nsudo pg_restore -U postgres -d app --no-owner -j4 app.dump\n\n# Monitoring\nsudo psql -U postgres -d app -c \"SELECT datname, pg_size_pretty(pg_database_size(datname)) FROM pg_database;\"\nsudo psql -U postgres -d app -c \"SELECT pid, state, query FROM pg_stat_activity;\"\n```\n\n## Best Practices\n\n- Use `-Fc` custom format for flexible restores\n- Test `pg_restore` on a staging database\n- Use pg_stat_activity to find blocking queries\n- Set `statement_timeout` for risky ad-hoc queries\n- Keep `VACUUM ANALYZE` healthy with autovacuum\n\n## Example Response\n\nFor a lock/blocking issue: lists pg_stat_activity, identifies the blocking pid,\nand cancels or terminates it after confirmation.",
  "examples": [
    {
      "input": "Create the app database and tables",
      "output": "Runs createdb, applies schema.sql, and verifies with \\dt"
    },
    {
      "input": "Backup before release",
      "output": "Dumps with pg_dump -Fc, verifies, and notes the restore command"
    }
  ],
  "platforms": {
    "claude_code": {
      "tools": [
        "Bash",
        "Read"
      ],
      "model": "inherit"
    },
    "cursor": {
      "rule_type": "auto"
    },
    "github_copilot": {
      "prompt_file": "postgresql.md",
      "extension": "github.copilot"
    },
    "windsurf": {
      "model": "inherit",
      "tools": [
        "bash",
        "read",
        "write",
        "edit"
      ]
    },
    "opencode": {
      "plugin": "opencode-postgresql"
    },
    "generic": {
      "system_prompt": "You work postgresql hands-on. You do it through postgres-cli: Query, administer, and back up PostgreSQL databases. The commands are real, e.g. `psql -U postgres -h localhost -d app`. You prefer the shortest safe path, and you reproduce failures before diagnosing them.",
      "available_tools": [
        "bash",
        "read",
        "write",
        "edit"
      ]
    }
  },
  "created_at": "2024-01-01T00:00:00Z",
  "updated_at": "2024-01-01T00:00:00Z",
  "id": "postgresql",
  "type": "skill",
  "skills": [],
  "tools": [
    "Bash",
    "Read"
  ],
  "inputs": {
    "parameters": [
      {
        "capability": "postgres-cli",
        "name": "host",
        "type": "string",
        "description": "Database server host (-h)"
      },
      {
        "capability": "postgres-cli",
        "name": "dbname",
        "type": "string",
        "description": "Database to connect to (-d)"
      },
      {
        "capability": "postgres-cli",
        "name": "file",
        "type": "string",
        "description": "Script file to execute (-f)"
      }
    ]
  },
  "outputs": {},
  "dependencies": [
    "createdb",
    "pg_dump",
    "psql"
  ],
  "conversion": {
    "tool": "Kdesk-Catalog yaml-to-json",
    "schema": "definition-v1",
    "source_yaml": "database/postgresql-skill.yaml",
    "derived": {
      "skills": "none",
      "tools": "platforms.claude_code.tools",
      "inputs": "capability.parameters",
      "outputs": "none (not present in source YAML)",
      "dependencies": "prerequisites"
    }
  },
  "_provenance": {
    "generated_by": "Kdesk-Catalog yaml-to-json",
    "generator_version": "1.0.0",
    "schema": "definition-v1",
    "source": "universal-agents/database/postgresql-skill.yaml",
    "checksum": "45704997bc595ae687a74a867d2a7e2f1063ab785f6f01b7e0604bfe06128901"
  }
}
