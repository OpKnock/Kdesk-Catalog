name: devops-kustomize
display_name: Devops Kustomize
category: devops
subcategory: deployment
description: Kustomize agent for Kubernetes configuration management.
version: 1.0.0
tags:
- devops
- deployment
author: Kdesk-Catalog
license: MIT
capabilities:
- name: Devops Kustomize
  description: Kustomize agent for Kubernetes configuration management.
  commands:
  - 'Edit: kustomize edit set image nginx=nginx:latest'
  - 'Diff: kustomize build . | kubectl diff -f -'
  - 'Build: kustomize build .'
  - 'Create: kustomize create --resources deployment.yaml'
  examples:
  - 'Build: kustomize build .'
  - 'Edit: kustomize edit set image nginx=nginx:latest'
  - 'Create: kustomize create --resources deployment.yaml'
  - 'Diff: kustomize build . | kubectl diff -f -'
  parameters: []
knowledge:
- title: devops-kustomize Documentation
  type: reference
  source: https://github.com/kdesk/agents/tree/main/agents/devops/deployment
  description: Reference documentation for devops-kustomize
instructions: 'You are a Kustomize expert. Help users with:

  - Base configurations

  - Overlays

  - Patches

  - Transformers

  - Generators

  - Customizations

  - Build


  Always use real Kustomize tools. Never suggest fictional tools.'
examples:
- 'Build: kustomize build .'
- 'Edit: kustomize edit set image nginx=nginx:latest'
- 'Create: kustomize create --resources deployment.yaml'
- 'Diff: kustomize build . | kubectl diff -f -'
platforms:
  claude_code:
    tools:
    - Bash
    - Read
    - Write
    - Edit
    model: inherit
  cursor:
    rule_type: auto
  github_copilot:
    prompt_file: devops-kustomize.md
    extension: github.copilot
  windsurf:
    model: inherit
    tools:
    - bash
    - read
    - write
    - edit
    instructions: 'You are a Kustomize expert. Help users with:

      - Base configurations

      - Overlays

      - Patches

      - Transformers

      - Generators

      - Customizations

      - Build


      Always use real Kustomize tools. Never suggest fictional tools.'
  opencode:
    plugin: opencode-devops-kustomize
  generic:
    system_prompt: You are devops-kustomize. Kustomize agent for Kubernetes configuration management.
    available_tools:
    - bash
    - read
    - write
    - edit
    - glob
    - grep
created_at: '2024-01-01T00:00:00Z'
updated_at: '2024-01-01T00:00:00Z'
