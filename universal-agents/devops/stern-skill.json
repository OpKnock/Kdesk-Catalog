{
  "name": "stern",
  "display_name": "stern",
  "category": "devops",
  "subcategory": "general",
  "description": "Tails and aggregates logs from multiple Kubernetes pods with regex matching using stern: multi-pod, multi-container, and namespace-wide views.",
  "version": "2.0.0",
  "tags": [
    "devops"
  ],
  "author": "Kdesk-Catalog",
  "license": "MIT",
  "prerequisites": [
    "stern"
  ],
  "capabilities": [
    {
      "name": "multi-pod-tailing",
      "description": "Tail logs from pod groups matched by regex across namespaces.",
      "commands": [
        "stern web-*",
        "stern '^api-.*' -n app",
        "stern web --all-namespaces",
        "stern web -A -e 'ERROR'",
        "stern web -c sidecar",
        "stern web --tail 200"
      ],
      "examples": [
        "stern web-*",
        "stern '^api-.*' -n app",
        "stern web -A -e 'ERROR'"
      ],
      "parameters": [
        {
          "name": "pattern",
          "type": "string",
          "description": "Pod name regex, e.g. web-*"
        },
        {
          "name": "namespace",
          "type": "string",
          "description": "Namespace scope"
        },
        {
          "name": "exclude",
          "type": "string",
          "description": "Regex to exclude lines"
        }
      ]
    },
    {
      "name": "filtering-and-format",
      "description": "Filter lines, colorize, and control timestamps and output.",
      "commands": [
        "stern web -i 'GET /health'",
        "stern web -n app --timestamps",
        "stern web --since 30m",
        "stern web --no-color",
        "stern web --template '{{.PodName}} {{.Message}}'",
        "stern web --max-log-requests 20"
      ],
      "examples": [
        "stern web -i 'GET /health'",
        "stern web --since 30m",
        "stern web --template '{{.PodName}} {{.Message}}'"
      ],
      "parameters": [
        {
          "name": "include",
          "type": "string",
          "description": "Regex to include lines"
        },
        {
          "name": "since",
          "type": "string",
          "description": "Look back window, e.g. 30m"
        },
        {
          "name": "template",
          "type": "string",
          "description": "Go template for output"
        }
      ]
    }
  ],
  "knowledge": [
    {
      "title": "stern GitHub",
      "type": "documentation",
      "source": "https://github.com/stern/stern",
      "description": "stern flags, templating, and configuration"
    },
    {
      "title": "stern Documentation",
      "type": "documentation",
      "source": "https://stern.io/",
      "description": "Usage guide and examples"
    }
  ],
  "instructions": "# stern Multi-Pod Logging\n\nTail logs across many pods at once with regex matching.\n\n## What This Skill Does\n\n- Matches multiple pods with a single regex\n- Aggregates logs chronologically with pod prefixes\n- Filters by include/exclude regexes\n- Supports multi-container pods and all-namespace scans\n- Customizes output with Go templates\n\n## When to Use\n\n- Debugging across a deployment's replicas\n- Correlation across API + worker pods\n- Live log monitoring during incidents\n\n## Real Commands\n\n```bash\n# Basic tailing\nstern web-*                      # all web pods\nstern '^api-.*' -n app\nstern web -A                     # all namespaces\nstern web -c sidecar             # specific container\nstern web --tail 200\n\n# Filtering\nstern web -i 'ERROR|WARN'        # include\nstern web -e 'health-check'      # exclude\nstern web --since 30m\n\n# Format\nstern web --timestamps\nstern web --no-color\nstern web --template '{{.PodName}} {{.Message}}'\nstern web --max-log-requests 20\n```\n\n## Templates\n\n- `{{.Namespace}} {{.PodName}} {{.ContainerName}} {{.Message}}`\n\n## Best Practices\n\n- Quote regexes containing special chars: `stern 'web-[0-9]+'`\n- Use -e to drop noisy health-check lines during incidents\n- Pin --max-log-requests to avoid API throttling on big deployments\n- Pair with --since for bounded time windows\n- Use --timestamps when correlating with metrics dashboards",
  "examples": [
    {
      "input": "Watch all web replicas for errors right now",
      "output": "Agent runs stern web-* -n app -i 'ERROR' and monitors the aggregated stream"
    },
    {
      "input": "Correlate api and worker logs during an incident",
      "output": "Agent tails stern '^api-|^worker-' --since 1h --timestamps and spots the ordering"
    }
  ],
  "platforms": {
    "claude_code": {
      "tools": [
        "Bash",
        "Read"
      ],
      "model": "inherit"
    },
    "cursor": {
      "rule_type": "auto"
    },
    "github_copilot": {
      "prompt_file": "stern.md",
      "extension": "github.copilot"
    },
    "windsurf": {
      "model": "inherit",
      "tools": [
        "bash",
        "read",
        "write",
        "edit"
      ]
    },
    "opencode": {
      "plugin": "opencode-stern"
    },
    "generic": {
      "system_prompt": "This is your domain: stern. You do it through multi-pod-tailing: Tail logs from pod groups matched by regex across namespaces. The commands are real, e.g. `stern web-*`. You verify each step you report and stay honest about what you have not run.",
      "available_tools": [
        "bash",
        "read",
        "write",
        "edit"
      ]
    }
  },
  "created_at": "2024-01-01T00:00:00Z",
  "updated_at": "2024-01-01T00:00:00Z",
  "id": "stern",
  "type": "skill",
  "skills": [],
  "tools": [
    "Bash",
    "Read"
  ],
  "inputs": {
    "parameters": [
      {
        "capability": "multi-pod-tailing",
        "name": "pattern",
        "type": "string",
        "description": "Pod name regex, e.g. web-*"
      },
      {
        "capability": "multi-pod-tailing",
        "name": "namespace",
        "type": "string",
        "description": "Namespace scope"
      },
      {
        "capability": "multi-pod-tailing",
        "name": "exclude",
        "type": "string",
        "description": "Regex to exclude lines"
      },
      {
        "capability": "filtering-and-format",
        "name": "include",
        "type": "string",
        "description": "Regex to include lines"
      },
      {
        "capability": "filtering-and-format",
        "name": "since",
        "type": "string",
        "description": "Look back window, e.g. 30m"
      },
      {
        "capability": "filtering-and-format",
        "name": "template",
        "type": "string",
        "description": "Go template for output"
      }
    ]
  },
  "outputs": {},
  "dependencies": [
    "stern"
  ],
  "conversion": {
    "tool": "Kdesk-Catalog yaml-to-json",
    "schema": "definition-v1",
    "source_yaml": "devops/stern-skill.yaml",
    "derived": {
      "skills": "none",
      "tools": "platforms.claude_code.tools",
      "inputs": "capability.parameters",
      "outputs": "none (not present in source YAML)",
      "dependencies": "prerequisites"
    }
  },
  "_provenance": {
    "generated_by": "Kdesk-Catalog yaml-to-json",
    "generator_version": "1.0.0",
    "schema": "definition-v1",
    "source": "universal-agents/devops/stern-skill.yaml",
    "checksum": "6162e23ed5d69702a9dd9ab784860243df746b12e07adba6983635d615c2d39c"
  }
}
