name: tilt
display_name: tilt
category: devops
subcategory: general
description: 'Develops Kubernetes apps with Tilt: resource definitions, live reload, Tiltfiles, CI mode,
  and dashboard workflows.'
version: 2.0.0
tags:
- devops
author: Mehul Wagde
license: MIT
prerequisites:
- tilt
capabilities:
- name: dev-session
  description: Start dev sessions, watch logs, and manage running resources.
  commands:
  - tilt up
  - tilt up --port 10350
  - tilt down
  - tilt args
  - tilt alpha tiltfile-result
  - tilt --watch=false up
  examples:
  - tilt up
  - tilt down
  - tilt args
  parameters:
  - name: port
    type: integer
    description: Dashboard port
  - name: watch
    type: boolean
    description: Watch file changes
- name: tiltfile-and-ci
  description: Author Tiltfiles and run sessions headlessly for CI.
  commands:
  - tilt ci
  - tilt dump
  - tilt dump image
  - tilt version
  - tilt alpha trigger demo-resource
  - tilt doctor
  examples:
  - tilt ci
  - tilt dump
  - tilt doctor
  parameters:
  - name: tiltfile
    type: string
    description: Tiltfile path
  - name: resource
    type: string
    description: Resource name for triggers
knowledge:
- title: Tilt Documentation
  type: documentation
  source: https://docs.tilt.dev/
  description: Tiltfile language, resources, and workflows
- title: Tiltfile Reference
  type: documentation
  source: https://docs.tilt.dev/api.html
  description: 'Tiltfile functions: docker_build, k8s_yaml, resource'
instructions: '# Tilt Development Sessions


  Spin up Kubernetes dev environments with live reload and a shared dashboard.


  ## What This Skill Does


  - Defines resources in a Tiltfile (build, deploy, probes)

  - Runs tilt up with live update (file change -> container update)

  - Streams logs and alerts in the dashboard

  - Runs headless sessions in CI with tilt ci

  - Manages resource triggers and arguments


  ## When to Use


  - Multi-service dev environments on Kubernetes

  - Teams wanting a shared dev dashboard

  - Live reload workflows (no rebuild for code-only changes)


  ## Real Commands


  ```bash

  # Dev session

  tilt up

  tilt up --port 10350

  tilt down

  tilt args

  tilt --watch=false up


  # Tiltfile

  tilt dump                      # view generated config

  tilt dump image                # image build graph

  tilt alpha tiltfile-result


  # CI and diagnostics

  tilt ci                        # run once, fail on errors

  tilt doctor

  tilt version

  tilt alpha trigger api

  ```


  ## Tiltfile Sketch


  ```python

  load(''ext://restart_process'', ''docker_build_with_restart'')


  docker_build_with_restart(''myapp'', ''.'', entrypoint=[''node'', ''server.js''])

  k8s_yaml(''k8s/deployment.yaml'')

  k8s_yaml(''k8s/service.yaml'')

  resource(''myapp'', port_forwards=[''8080:8080''], probes=[http_probe(''/health'')])

  ```


  ## Best Practices


  - Use live_update for interpreted languages to skip rebuilds

  - Add resource probes so tilt blocks on health, not just container start

  - Use tilt ci in PR pipelines for smoke tests

  - Keep Tiltfile reviewed like code; it is build config

  - Use `tilt args` for environment-specific overrides'
examples:
- input: Set up live-reload dev for the api service
  output: Agent writes a Tiltfile with docker_build, k8s_yaml, and port-forward, then starts tilt up
- input: Run the dev environment in CI
  output: Agent runs tilt ci headlessly and wires the exit code into the pipeline
platforms:
  claude_code:
    tools:
    - Bash
    - Read
    model: inherit
  cursor:
    rule_type: auto
  github_copilot:
    prompt_file: tilt.md
    extension: github.copilot
  windsurf:
    model: inherit
    tools:
    - bash
    - read
    - write
    - edit
  opencode:
    plugin: opencode-tilt
  generic:
    system_prompt: 'You take the tilt seat. You do it through dev-session: Start dev sessions, watch logs,
      and manage running resources. The commands are real, e.g. `tilt up`. You prefer the shortest safe
      path, and you reproduce failures before diagnosing them.'
    available_tools:
    - bash
    - read
    - write
    - edit
created_at: '2025-03-26T00:00:00Z'
updated_at: '2026-08-19T00:00:00Z'
