name: nvm
display_name: Nvm
category: devtools
subcategory: general
description: 'Installs and switches Node.js versions with nvm: version management, aliases, .nvmrc files,
  and per-project Node selection.'
version: 2.0.0
tags:
- devtools
author: Mehul Wagde
license: MIT
prerequisites:
- nvm
capabilities:
- name: version-management
  description: Install, list, and switch Node.js versions.
  commands:
  - nvm install 20
  - nvm install --lts
  - nvm ls
  - nvm ls-remote
  - nvm use 18.20.4
  - nvm current
  examples:
  - nvm install 20
  - nvm use 18.20.4
  - nvm ls-remote --lts
  parameters:
  - name: version
    type: string
    description: Node version or alias, e.g. 20, --lts
  - name: alias
    type: string
    description: Alias name
- name: aliases-and-autoload
  description: Set default versions, create aliases, and use .nvmrc.
  commands:
  - nvm alias default 20
  - nvm alias project-node 18.20.4
  - nvm unalias project-node
  - nvm use 20 >/dev/null 2>&1 || nvm install 20
  - nvm exec 18 node -v
  - nvm uninstall 16
  examples:
  - nvm alias default 20
  - nvm exec 18 node -v
  - nvm use 20 || nvm install 20
  parameters:
  - name: name
    type: string
    description: Alias or default name
  - name: version
    type: string
    description: Version the alias points to
knowledge:
- title: nvm-sh GitHub
  type: documentation
  source: https://github.com/nvm-sh/nvm
  description: Install, usage, and troubleshooting
- title: Node.js Releases
  type: documentation
  source: https://nodejs.org/en/about/previous-releases
  description: LTS schedule and supported versions
instructions: '# nvm Node Version Management


  Run multiple Node.js versions side by side.


  ## What This Skill Does


  - Installs Node versions (LTS, latest, exact)

  - Switches versions per shell/project

  - Sets default and named aliases

  - Uses .nvmrc for repo-pinned versions

  - Runs commands with a specific version (nvm exec)


  ## When to Use


  - Projects pinned to different Node majors

  - Testing against multiple Node versions

  - Avoiding global sudo installs


  ## Real Commands


  ```bash

  # Install and switch

  nvm install 20

  nvm install --lts

  nvm use 18.20.4

  nvm current

  nvm ls

  nvm ls-remote


  # Aliases and defaults

  nvm alias default 20

  nvm alias project-node 18.20.4

  nvm unalias project-node


  # Repo-pinned version (.nvmrc = "20")

  nvm use                      # reads .nvmrc

  nvm install                  # installs .nvmrc version


  # One-off

  nvm exec 18 node -v

  nvm run 18 --version


  # Cleanup

  nvm uninstall 16

  ```


  ## Best Practices


  - Commit `.nvmrc` with major.minor.patch to pin runtime

  - Add `nvm use` to your shell hook (nvm alias default + cd hook)

  - Use CI matrix to test against multiple LTS versions

  - Never sudo install Node globally; nvm keeps it in $HOME

  - Set default to an active LTS for new projects'
examples:
- input: Project A needs Node 18, Project B Node 20
  output: Agent creates .nvmrc files and demonstrates nvm use switching per directory
- input: Test our app on Node 18 and 20 in CI
  output: Agent sets up a CI matrix using nvm install and nvm exec per version
platforms:
  claude_code:
    tools:
    - Bash
    - Read
    - Write
    - Edit
    model: inherit
  cursor:
    rule_type: auto
  github_copilot:
    prompt_file: nvm.md
    extension: github.copilot
  windsurf:
    model: inherit
    tools:
    - bash
    - read
    - write
    - edit
  opencode:
    plugin: opencode-nvm
  generic:
    system_prompt: 'You own Nvm end to end. You do it through version-management: Install, list, and switch
      Node.js versions. The commands are real, e.g. `nvm install 20`. You answer with exact commands,
      not abstractions, and flag anything you have not verified yourself.'
    available_tools:
    - bash
    - read
    - write
    - edit
created_at: '2025-07-01T00:00:00Z'
updated_at: '2026-08-19T00:00:00Z'
