name: event-sourcing-event-sourcing
version: 2.0.0
type: skill
category: backend
description: Implements event sourcing with EventStoreDB, Kafka, projections, and CQRS read models using
  real CLI tooling.
keywords:
- event-sourcing
- cqrs
- projections
platforms:
  claude_code:
    tools:
    - Bash
    - Read
    - Write
    - Edit
    - Glob
    - Grep
    model: inherit
  cursor:
    rule_type: auto
  github_copilot:
    prompt_file: event-sourcing-event-sourcing.md
    extension: github.copilot
  windsurf:
    model: inherit
    tools:
    - bash
    - read
    - write
    - edit
  opencode:
    plugin: opencode-event-sourcing
  generic:
    system_prompt: 'You own event-sourcing-event-sourcing end to end. You do it through event-store-operations:
      Run and interact with EventStoreDB streams. The commands are real, e.g. `docker run -d -p 2113:2113
      -p 1113:1113 eventstore/eventstore:latest --insecure`. You run and verify the real CLI commands
      before you claim anything.'
    available_tools:
    - bash
    - read
    - write
    - edit
    - glob
    - grep
capabilities:
- name: event-store-operations
  description: Run and interact with EventStoreDB streams.
  commands:
  - docker run -d -p 2113:2113 -p 1113:1113 eventstore/eventstore:latest --insecure
  - curl -s http://localhost:2113/streams/orders | jq '.entries[0].title'
  - 'curl -s -X POST http://localhost:2113/streams/orders -H ''Content-Type: application/vnd.eventstore.events+json'''
  - 'curl -s http://localhost:2113/streams/orders/0 -H ''Accept: application/json'' | jq ''.data'''
  - 'curl -s http://localhost:2113/projections/any -H ''Accept: application/json'' | jq ''.projections[].name'''
  examples:
  - docker run -d -p 2113:2113 eventstore/eventstore:latest --insecure
  - curl -s http://localhost:2113/streams/orders | jq '.entries[0].title'
  - curl -s http://localhost:2113/streams/orders/0 | jq '.data'
  parameters:
  - name: stream
    type: string
    description: Stream name
  - name: event
    type: string
    description: Event type
- name: kafka-event-pipeline
  description: Produce, consume, and replay events through Kafka.
  commands:
  - kafka-topics.sh --bootstrap-server localhost:9092 --create --topic account.events --partitions 4
  - kafka-console-producer.sh --bootstrap-server localhost:9092 --topic account.events
  - kafka-console-consumer.sh --bootstrap-server localhost:9092 --topic account.events --from-beginning
  - kafka-consumer-groups.sh --bootstrap-server localhost:9092 --reset-offsets --group projection --to-earliest
    --topic account.events --execute
  - redis-cli GET account:42:balance
  examples:
  - kafka-topics.sh --bootstrap-server localhost:9092 --create --topic account.events --partitions 4
  - kafka-consumer-groups.sh --bootstrap-server localhost:9092 --reset-offsets --group projection --to-earliest
    --execute
  - redis-cli GET account:42:balance
  parameters:
  - name: topic
    type: string
    description: Event topic name
  - name: group
    type: string
    description: Consumer group for replay
prerequisites:
- node.js
- typescript
- postgresql
- redis
tools:
- node.js
- typescript
- postgresql
- redis
- eventstoredb
examples:
- input: Audit trail for a banking ledger
  output: Agent designs event types, runs EventStoreDB, writes and reads the ledger stream
- input: Rebuild our projections after a schema change
  output: Agent replays events from earliest offsets into a new projection version and verifies read model
instructions: '# Event Sourcing Implementation


  Build systems where the event log is the source of truth.


  ## What This Skill Does


  - Runs EventStoreDB and writes/reads streams

  - Implements projections and read models (CQRS)

  - Produces/replays events on Kafka

  - Rebuilds read models from scratch

  - Advises on event schema and versioning


  ## When to Use


  - An audit-required domain (finance, compliance)

  - Complex state transitions needing full history

  - Rebuilding projections after schema changes


  ## Real Commands


  ```bash

  # EventStoreDB

  docker run -d -p 2113:2113 -p 1113:1113 eventstore/eventstore:latest --insecure

  curl -s http://localhost:2113/streams/orders -H ''Accept: application/json'' | jq ''.entries[0].title''

  curl -s -X POST http://localhost:2113/streams/orders   -H ''Content-Type: application/vnd.eventstore.events+json''   -d
  ''[{"eventId":"...","eventType":"OrderCreated","data":{"id":"1","total":99}}]''

  curl -s http://localhost:2113/projections/any -H ''Accept: application/json'' | jq ''.projections[].name''


  # Kafka pipeline

  kafka-topics.sh --bootstrap-server localhost:9092 --create --topic account.events --partitions 4

  kafka-console-producer.sh --bootstrap-server localhost:9092 --topic account.events

  kafka-console-consumer.sh --bootstrap-server localhost:9092 --topic account.events --from-beginning


  # Rebuild projections

  kafka-consumer-groups.sh --bootstrap-server localhost:9092   --reset-offsets --group projection --to-earliest
  --topic account.events --execute

  redis-cli GET account:42:balance

  ```


  ## Core Rules


  - Events are immutable facts, never edited or deleted

  - Rebuild any read model by replaying the log

  - Commands validate, events record outcomes

  - Version events; consumers tolerate old versions

  - Projections are disposable: store only the log permanently


  ## Best Practices


  - Write the event schema before the code

  - Test replay determinism in CI

  - Idempotent projections for safe rebuilds

  - Snapshot long streams for performance

  - Use CDC (Debezium) to migrate legacy tables into events'
display_name: event-sourcing-event-sourcing
knowledge:
- title: EventStoreDB Documentation
  type: documentation
  source: https://developers.eventstore.com/
  description: Streams, projections, and HTTP API
- title: Debezium
  type: documentation
  source: https://debezium.io/documentation/
  description: CDC connectors producing events
- title: CQRS by Fowler
  type: documentation
  source: https://martinfowler.com/bliki/CQRS.html
  description: CQRS concept reference
