{
  "name": "fraud-detection",
  "version": "2.0.0",
  "type": "skill",
  "category": "security",
  "description": "Builds fraud detection pipelines with pandas, scikit-learn, and MLflow: feature engineering, model training, evaluation, and experiment tracking.",
  "keywords": [
    "fraud-detection"
  ],
  "platforms": {
    "claude_code": {
      "tools": [
        "Bash",
        "Read",
        "Write",
        "Edit",
        "Glob",
        "Grep"
      ],
      "model": "inherit"
    },
    "cursor": {
      "rule_type": "auto"
    },
    "github_copilot": {
      "prompt_file": "fraud-detection.md",
      "extension": "github.copilot"
    },
    "windsurf": {
      "model": "inherit",
      "tools": [
        "bash",
        "read",
        "write",
        "edit"
      ]
    },
    "opencode": {
      "plugin": "opencode-fraud-detection"
    },
    "generic": {
      "system_prompt": "This is your domain: fraud-detection. You do it through data-pipeline: Prepare and inspect transaction datasets with Python data tooling. The commands are real, e.g. `python -m venv .venv && .venv/Scripts/activate`. You run and verify the real CLI commands before you claim anything.",
      "available_tools": [
        "bash",
        "read",
        "write",
        "edit",
        "glob",
        "grep"
      ]
    }
  },
  "capabilities": [
    {
      "name": "data-pipeline",
      "description": "Prepare and inspect transaction datasets with Python data tooling.",
      "commands": [
        "python -m venv .venv && .venv/Scripts/activate",
        "pip install pandas scikit-learn imbalanced-learn mlflow pytest",
        "python -c \"import pandas as pd; df=pd.read_csv('transactions.csv'); print(df.isna().sum()); print(df['is_fraud'].value_counts(normalize=True))\"",
        "python -m pytest tests/ -k fraud -v",
        "jupyter nbconvert --to script notebooks/eda.ipynb --output eda"
      ],
      "examples": [
        "python -c \"import pandas as pd; df=pd.read_csv('tx.csv'); print(df.describe())\"",
        "python -c \"import pandas as pd; df=pd.read_csv('tx.csv'); print(df.groupby('channel')['amount'].sum())\"",
        "python -m pytest tests/test_features.py -v"
      ],
      "parameters": [
        {
          "name": "dataset",
          "type": "string",
          "description": "Path to transactions CSV"
        },
        {
          "name": "test-path",
          "type": "string",
          "description": "pytest path filter"
        },
        {
          "name": "notebook",
          "type": "string",
          "description": "Notebook path to export"
        }
      ]
    },
    {
      "name": "mlflow",
      "description": "Track experiments, log models, and compare fraud detection runs.",
      "commands": [
        "mlflow run . -e train --env-manager local",
        "mlflow experiments create --experiment-name fraud-v3",
        "mlflow runs list --experiment-id 42",
        "mlflow models serve -m models:/fraud-detector/Production -p 5001",
        "mlflow ui --host 0.0.0.0 --port 5000"
      ],
      "examples": [
        "mlflow run . -e train -P max_depth=7 -P scale_pos_weight=10",
        "mlflow models serve -m models:/fraud-detector/Production -p 5001",
        "mlflow runs list --experiment-id 42 --order-by 'metrics.auc desc'"
      ],
      "parameters": [
        {
          "name": "experiment-name",
          "type": "string",
          "description": "MLflow experiment name"
        },
        {
          "name": "model-uri",
          "type": "string",
          "description": "Registered model URI like models:/fraud-detector/Production"
        },
        {
          "name": "max_depth",
          "type": "number",
          "description": "Tree depth hyperparameter for training run"
        }
      ]
    }
  ],
  "prerequisites": [
    "python",
    "scikit-learn",
    "tensorflow",
    "kafka"
  ],
  "tools": [
    "python",
    "scikit-learn",
    "tensorflow",
    "kafka",
    "redis"
  ],
  "examples": [
    {
      "input": "Build a fraud model for credit card transactions with a 1% fraud rate.",
      "output": "Creates a pandas preprocessing pipeline, trains an XGBoost-style classifier with class weighting via MLflow, and reports precision/recall at the business threshold."
    },
    {
      "input": "Compare two fraud models and promote the better one.",
      "output": "Logs both runs to MLflow, compares AUC and cost-weighted loss, and promotes the winner to the Production stage in the model registry."
    }
  ],
  "instructions": "# Fraud Detection\n\nBuild, evaluate, and serve fraud models with reproducible data pipelines.\n\n## When to Use\n\n- Detecting transaction fraud in real-time or batch\n- Feature engineering on customer/device/velocity signals\n- Comparing model versions before production rollout\n\n## Setup\n\n```bash\npython -m venv .venv\npip install pandas scikit-learn imbalanced-learn mlflow pytest\n```\n\n## Data inspection\n\n```bash\npython -c \"import pandas as pd; df=pd.read_csv('transactions.csv'); print(df['is_fraud'].value_counts(normalize=True))\"\n```\n\nFraud is rare (often < 1%). Class imbalance will dominate model behavior - always report precision/recall, not accuracy.\n\n## Feature engineering\n\nCompute velocity features (count and sum of transactions per card in the last 1h/24h), device fingerprints, and normalized amounts. Test features first:\n\n```bash\npython -m pytest tests/test_features.py -v\n```\n\n## Training\n\nTrain with class weighting or SMOTE, then log to MLflow:\n\n```bash\nmlflow experiments create --experiment-name fraud-v3\nmlflow run . -e train -P max_depth=7 -P scale_pos_weight=10\n```\n\n## Evaluation\n\nTrack ROC AUC, precision at 0.1% FPR, and cost-weighted loss. Pick thresholds by business cost matrix, not the default 0.5.\n\n## Serving\n\n```bash\nmlflow models serve -m models:/fraud-detector/Production -p 5001\n```\n\n## Best practices\n\n- Keep train/test split temporal - never shuffle across time.\n- Monitor drift on score distribution and feature means daily.\n- Review false positives weekly with the risk team and relabel.\n- Shadow-deploy new models for two weeks before promotion.\n\n## Testing\n\n```bash\npython -m pytest tests/ -v\n```\n\nInclude golden-path unit tests for each engineered feature.",
  "display_name": "fraud-detection",
  "knowledge": [
    {
      "title": "scikit-learn",
      "type": "documentation",
      "source": "https://scikit-learn.org/stable/",
      "description": "Classifiers, metrics (ROC AUC, precision/recall), and pipelines"
    },
    {
      "title": "imbalanced-learn",
      "type": "documentation",
      "source": "https://imbalanced-learn.org/stable/",
      "description": "SMOTE and resampling for rare-class fraud data"
    },
    {
      "title": "MLflow Docs",
      "type": "documentation",
      "source": "https://mlflow.org/docs/latest/",
      "description": "Experiment tracking, model registry, and serving"
    }
  ],
  "id": "fraud-detection",
  "skills": [],
  "inputs": {
    "parameters": [
      {
        "capability": "data-pipeline",
        "name": "dataset",
        "type": "string",
        "description": "Path to transactions CSV"
      },
      {
        "capability": "data-pipeline",
        "name": "test-path",
        "type": "string",
        "description": "pytest path filter"
      },
      {
        "capability": "data-pipeline",
        "name": "notebook",
        "type": "string",
        "description": "Notebook path to export"
      },
      {
        "capability": "mlflow",
        "name": "experiment-name",
        "type": "string",
        "description": "MLflow experiment name"
      },
      {
        "capability": "mlflow",
        "name": "model-uri",
        "type": "string",
        "description": "Registered model URI like models:/fraud-detector/Production"
      },
      {
        "capability": "mlflow",
        "name": "max_depth",
        "type": "number",
        "description": "Tree depth hyperparameter for training run"
      }
    ]
  },
  "outputs": {},
  "dependencies": [
    "python",
    "scikit-learn",
    "tensorflow",
    "kafka"
  ],
  "conversion": {
    "tool": "Kdesk-Catalog yaml-to-json",
    "schema": "definition-v1",
    "source_yaml": "fraud-detection/skill/fraud-detection.yaml",
    "derived": {
      "skills": "none",
      "tools": "tools",
      "inputs": "capability.parameters",
      "outputs": "none (not present in source YAML)",
      "dependencies": "prerequisites"
    }
  },
  "_provenance": {
    "generated_by": "Kdesk-Catalog yaml-to-json",
    "generator_version": "1.0.0",
    "schema": "definition-v1",
    "source": "universal-agents/fraud-detection/skill/fraud-detection.yaml",
    "checksum": "37c123eb4c562c255397dfabeff843738f2f6d71b89507b2ce011af44708785d"
  }
}
