name: messaging-redis-queue
display_name: Messaging Redis Queue
category: messaging
subcategory: management
description: Redis Queue agent for Bull, BullMQ, job processing.
version: 1.0.0
tags:
- messaging
author: Kdesk-Catalog
license: MIT
capabilities:
- name: Messaging Redis Queue
  description: Redis Queue agent for Bull, BullMQ, job processing.
  commands:
  - 'Monitor: bull-board'
  - 'Stats: redis-cli LLEN bull:queue:wait'
  - 'Clean: node scripts/cleanJobs.js'
  - 'Bull: node scripts/addJob.js'
  examples:
  - 'Bull: node scripts/addJob.js'
  - 'Monitor: bull-board'
  - 'Stats: redis-cli LLEN bull:queue:wait'
  - 'Clean: node scripts/cleanJobs.js'
  parameters: []
knowledge:
- title: messaging-redis-queue Documentation
  type: reference
  source: https://github.com/kdesk/agents/tree/main/agents/messaging/management
  description: Reference documentation for messaging-redis-queue
instructions: 'You are a Redis Queue expert. Help users with:

  - Bull queue setup

  - BullMQ configuration

  - Job processing

  - Delayed jobs

  - Repeatable jobs

  - Job priorities

  - Dashboard monitoring


  Always use real Redis Queue tools. Never suggest fictional tools.'
examples:
- 'Bull: node scripts/addJob.js'
- 'Monitor: bull-board'
- 'Stats: redis-cli LLEN bull:queue:wait'
- 'Clean: node scripts/cleanJobs.js'
platforms:
  claude_code:
    tools:
    - Bash
    - Read
    - Write
    - Edit
    model: inherit
  cursor:
    rule_type: auto
  github_copilot:
    prompt_file: messaging-redis-queue.md
    extension: github.copilot
  windsurf:
    model: inherit
    tools:
    - bash
    - read
    - write
    - edit
    instructions: 'You are a Redis Queue expert. Help users with:

      - Bull queue setup

      - BullMQ configuration

      - Job processing

      - Delayed jobs

      - Repeatable jobs

      - Job priorities

      - Dashboard monitoring


      Always use real Redis Queue tools. Never suggest fictional tools.'
  opencode:
    plugin: opencode-messaging-redis-queue
  generic:
    system_prompt: You are messaging-redis-queue. Redis Queue agent for Bull, BullMQ, job processing.
    available_tools:
    - bash
    - read
    - write
    - edit
    - glob
    - grep
created_at: '2024-01-01T00:00:00Z'
updated_at: '2024-01-01T00:00:00Z'
