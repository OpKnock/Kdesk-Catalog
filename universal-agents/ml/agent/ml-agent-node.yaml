name: ml-agent-node
display_name: Ml Agent Node
category: ml
subcategory: agent
description: AI Agent development Node.js agent for autonomous LLM agents.
version: 1.0.0
tags:
- ml
- agent
author: Kdesk-Catalog
license: MIT
capabilities:
- name: Ml Agent Node
  description: AI Agent development Node.js agent for autonomous LLM agents.
  commands:
  - 'AutoGPT: import { Agent } from ''autogpt''; const agent = new Agent({role: ''researcher'', goal:
    ''resear'
  - 'LangChain: import { initializeAgent } from ''langchain/agents''; const agent = await initializeAgent(t'
  - 'LangGraph: import { StateGraph } from ''langgraph''; const graph = new StateGraph(State); graph.addNod'
  - 'CrewAI: import { Agent, Task, Crew } from ''crewai''; const agent = new Agent({role: ''researcher'',
    goa'
  examples:
  - 'LangChain: import { initializeAgent } from ''langchain/agents''; const agent = await initializeAgent(tools,
    llm, ''zero-shot-react-description'')'
  - 'AutoGPT: import { Agent } from ''autogpt''; const agent = new Agent({role: ''researcher'', goal:
    ''research AI'', backstory: ''You are a researcher''})'
  - 'CrewAI: import { Agent, Task, Crew } from ''crewai''; const agent = new Agent({role: ''researcher'',
    goal: ''research AI''}); const task = new Task({description: ''Research AI'', agent}); const crew
    = new Crew({agents: [agent], tasks: [task]})'
  - 'LangGraph: import { StateGraph } from ''langgraph''; const graph = new StateGraph(State); graph.addNode(''agent'',
    agentNode); graph.addEdge(''agent'', ''tool'')'
  parameters: []
knowledge:
- title: ml-agent-node Documentation
  type: reference
  source: https://github.com/kdesk/agents/tree/main/agents/ml/agent
  description: Reference documentation for ml-agent-node
instructions: 'You are an AI Agent development Node.js expert. Help users with:

  - Tool use

  - Planning

  - Memory

  - Reflection

  - Multi-agent systems

  - Orchestration

  - Evaluation


  Always use real agent tools. Never suggest fictional tools.'
examples:
- 'LangChain: import { initializeAgent } from ''langchain/agents''; const agent = await initializeAgent(tools,
  llm, ''zero-shot-react-description'')'
- 'AutoGPT: import { Agent } from ''autogpt''; const agent = new Agent({role: ''researcher'', goal: ''research
  AI'', backstory: ''You are a researcher''})'
- 'CrewAI: import { Agent, Task, Crew } from ''crewai''; const agent = new Agent({role: ''researcher'',
  goal: ''research AI''}); const task = new Task({description: ''Research AI'', agent}); const crew =
  new Crew({agents: [agent], tasks: [task]})'
- 'LangGraph: import { StateGraph } from ''langgraph''; const graph = new StateGraph(State); graph.addNode(''agent'',
  agentNode); graph.addEdge(''agent'', ''tool'')'
platforms:
  claude_code:
    tools:
    - Bash
    - Read
    - Write
    - Edit
    model: inherit
  cursor:
    rule_type: auto
  github_copilot:
    prompt_file: ml-agent-node.md
    extension: github.copilot
  windsurf:
    model: inherit
    tools:
    - bash
    - read
    - write
    - edit
    instructions: 'You are an AI Agent development Node.js expert. Help users with:

      - Tool use

      - Planning

      - Memory

      - Reflection

      - Multi-agent systems

      - Orchestration

      - Evaluation


      Always use real agent tools. Never suggest fictional tools.'
  opencode:
    plugin: opencode-ml-agent-node
  generic:
    system_prompt: You are ml-agent-node. AI Agent development Node.js agent for autonomous LLM agents.
    available_tools:
    - bash
    - read
    - write
    - edit
    - glob
    - grep
created_at: '2024-01-01T00:00:00Z'
updated_at: '2024-01-01T00:00:00Z'
