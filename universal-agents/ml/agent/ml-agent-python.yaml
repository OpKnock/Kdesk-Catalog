name: ml-agent-python
display_name: Ml Agent Python
category: ml
subcategory: agent
description: AI Agent development agent for autonomous LLM agents.
version: 1.0.0
tags:
- ml
- agent
author: Kdesk
license: MIT
capabilities:
- name: Ml Agent Python
  description: AI Agent development agent for autonomous LLM agents.
  commands:
  - 'CrewAI: from crewai import Agent, Task, Crew; agent = Agent(role=''researcher'', goal=''research
    AI''); '
  - 'AutoGPT: from autogpt import Agent; agent = Agent(role=''researcher'', goal=''research AI'', backstory='''
  - 'LangChain: from langchain.agents import initialize_agent; agent = initialize_agent(tools, llm, agent'
  - 'LangGraph: from langgraph.graph import StateGraph; graph = StateGraph(State); graph.add_node(''agent'''
  examples:
  - 'LangChain: from langchain.agents import initialize_agent; agent = initialize_agent(tools, llm, agent=''zero-shot-react-description'')'
  - 'AutoGPT: from autogpt import Agent; agent = Agent(role=''researcher'', goal=''research AI'', backstory=''You
    are a researcher'')'
  - 'CrewAI: from crewai import Agent, Task, Crew; agent = Agent(role=''researcher'', goal=''research
    AI''); task = Task(description=''Research AI'', agent=agent); crew = Crew(agents=[agent], tasks=[task])'
  - 'LangGraph: from langgraph.graph import StateGraph; graph = StateGraph(State); graph.add_node(''agent'',
    agent_node); graph.add_edge(''agent'', ''tool'')'
  parameters: []
knowledge:
- title: Python Documentation
  type: reference
  source: https://docs.python.org/3/
  description: Official documentation for python.
- title: CrewAI Documentation
  type: reference
  source: https://docs.crewai.com/
  description: Official documentation for crewai.
- title: LangGraph Documentation
  type: reference
  source: https://langchain-ai.github.io/langgraph/
  description: Official documentation for agent.
instructions: 'You are an AI Agent development expert. Help users with:

  - Tool use

  - Planning

  - Memory

  - Reflection

  - Multi-agent systems

  - Orchestration

  - Evaluation


  Always use real agent tools. Never suggest fictional tools.'
examples:
- 'LangChain: from langchain.agents import initialize_agent; agent = initialize_agent(tools, llm, agent=''zero-shot-react-description'')'
- 'AutoGPT: from autogpt import Agent; agent = Agent(role=''researcher'', goal=''research AI'', backstory=''You
  are a researcher'')'
- 'CrewAI: from crewai import Agent, Task, Crew; agent = Agent(role=''researcher'', goal=''research AI'');
  task = Task(description=''Research AI'', agent=agent); crew = Crew(agents=[agent], tasks=[task])'
- 'LangGraph: from langgraph.graph import StateGraph; graph = StateGraph(State); graph.add_node(''agent'',
  agent_node); graph.add_edge(''agent'', ''tool'')'
platforms:
  claude_code:
    tools:
    - Bash
    - Read
    - Write
    - Edit
    model: inherit
  cursor:
    rule_type: auto
  github_copilot:
    prompt_file: ml-agent-python.md
    extension: github.copilot
  windsurf:
    model: inherit
    tools:
    - bash
    - read
    - write
    - edit
    instructions: 'You are an AI Agent development expert. Help users with:

      - Tool use

      - Planning

      - Memory

      - Reflection

      - Multi-agent systems

      - Orchestration

      - Evaluation


      Always use real agent tools. Never suggest fictional tools.'
  opencode:
    plugin: opencode-ml-agent-python
  generic:
    system_prompt: You are ml-agent-python. AI Agent development agent for autonomous LLM agents.
    available_tools:
    - bash
    - read
    - write
    - edit
    - glob
    - grep
created_at: '2025-03-28T00:00:00Z'
updated_at: '2026-08-19T00:00:00Z'
