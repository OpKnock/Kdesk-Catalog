name: ml-privacy-python-agent
display_name: Ml Privacy Python Agent
category: ml
subcategory: agent
description: ML Privacy Python agent for differential privacy.
version: 1.0.0
tags:
- ml
- agent
author: Kdesk-Catalog
license: MIT
capabilities:
- name: Ml Privacy Python Agent
  description: ML Privacy Python agent for differential privacy.
  commands:
  - 'ARX: python -c ''import arx; print(arx.anonymize_dataset("data.csv", ["name", "email"], ["k-anonymity'
  - 'Opacus: python -c ''from opacus import PrivacyEngine; pe = PrivacyEngine(); model, optimizer, data_lo'
  - 'PySyft: python -c ''import syft as sy; node = sy.Node(name="alice"); print(node)'''
  examples:
  - 'Opacus: python -c ''from opacus import PrivacyEngine; pe = PrivacyEngine(); model, optimizer, data_loader
    = pe.make_private(model, optimizer, data_loader, noise_multiplier=1.0, max_grad_norm=1.0)'''
  - 'PySyft: python -c ''import syft as sy; node = sy.Node(name="alice"); print(node)'''
  - 'ARX: python -c ''import arx; print(arx.anonymize_dataset("data.csv", ["name", "email"], ["k-anonymity",
    "l-diversity"]))'''
  parameters: []
knowledge:
- title: ml-privacy-python-agent Documentation
  type: reference
  source: https://github.com/kdesk/agents/tree/main/agents/ml/agent
  description: Reference documentation for ml-privacy-python-agent
instructions: 'You are a Python ML privacy expert. Help users with:

  - Differential privacy

  - Federated learning

  - Data anonymization

  - Privacy budget management


  Always use real Python privacy tools and best practices.'
examples:
- 'Opacus: python -c ''from opacus import PrivacyEngine; pe = PrivacyEngine(); model, optimizer, data_loader
  = pe.make_private(model, optimizer, data_loader, noise_multiplier=1.0, max_grad_norm=1.0)'''
- 'PySyft: python -c ''import syft as sy; node = sy.Node(name="alice"); print(node)'''
- 'ARX: python -c ''import arx; print(arx.anonymize_dataset("data.csv", ["name", "email"], ["k-anonymity",
  "l-diversity"]))'''
platforms:
  claude_code:
    tools:
    - Bash
    - Read
    - Write
    - Edit
    model: inherit
  cursor:
    rule_type: auto
  github_copilot:
    prompt_file: ml-privacy-python-agent.md
    extension: github.copilot
  windsurf:
    model: inherit
    tools:
    - bash
    - read
    - write
    - edit
    instructions: 'You are a Python ML privacy expert. Help users with:

      - Differential privacy

      - Federated learning

      - Data anonymization

      - Privacy budget management


      Always use real Python privacy tools and best practices.'
  opencode:
    plugin: opencode-ml-privacy-python-agent
  generic:
    system_prompt: You are ml-privacy-python-agent. ML Privacy Python agent for differential privacy.
    available_tools:
    - bash
    - read
    - write
    - edit
    - glob
    - grep
created_at: '2024-01-01T00:00:00Z'
updated_at: '2024-01-01T00:00:00Z'
