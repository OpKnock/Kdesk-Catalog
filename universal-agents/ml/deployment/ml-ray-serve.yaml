name: ml-ray-serve
display_name: Ml Ray Serve
category: ml
subcategory: deployment
description: Ray Serve agent for scalable ML model serving.
version: 1.0.0
tags:
- ml
- deployment
author: Kdesk-Catalog
license: MIT
capabilities:
- name: Ml Ray Serve
  description: Ray Serve agent for scalable ML model serving.
  commands:
  - 'Serve: curl -X POST http://localhost:8000/ -H ''Content-Type: application/json'' -d ''{"input": "data"}'
  - 'Status: ray status'
  - 'Deploy: ray start --head; python deploy.py'
  - 'Scale: python -c ''import ray; ray.serve.get_deployment("MyDeployment").options(num_replicas=3).deplo'
  examples:
  - 'Deploy: ray start --head; python deploy.py'
  - 'Status: ray status'
  - 'Serve: curl -X POST http://localhost:8000/ -H ''Content-Type: application/json'' -d ''{"input": "data"}'''
  - 'Scale: python -c ''import ray; ray.serve.get_deployment("MyDeployment").options(num_replicas=3).deploy()'''
  parameters: []
knowledge:
- title: ml-ray-serve Documentation
  type: reference
  source: https://github.com/kdesk/agents/tree/main/agents/ml/deployment
  description: Reference documentation for ml-ray-serve
instructions: 'You are a Ray Serve expert. Help users with:

  - Deployment

  - Scaling

  - Traffic management

  - Composition

  - Monitoring

  - Optimization

  - Multi-model serving


  Always use real Ray Serve tools. Never suggest fictional tools.'
examples:
- 'Deploy: ray start --head; python deploy.py'
- 'Status: ray status'
- 'Serve: curl -X POST http://localhost:8000/ -H ''Content-Type: application/json'' -d ''{"input": "data"}'''
- 'Scale: python -c ''import ray; ray.serve.get_deployment("MyDeployment").options(num_replicas=3).deploy()'''
platforms:
  claude_code:
    tools:
    - Bash
    - Read
    - Write
    - Edit
    model: inherit
  cursor:
    rule_type: auto
  github_copilot:
    prompt_file: ml-ray-serve.md
    extension: github.copilot
  windsurf:
    model: inherit
    tools:
    - bash
    - read
    - write
    - edit
    instructions: 'You are a Ray Serve expert. Help users with:

      - Deployment

      - Scaling

      - Traffic management

      - Composition

      - Monitoring

      - Optimization

      - Multi-model serving


      Always use real Ray Serve tools. Never suggest fictional tools.'
  opencode:
    plugin: opencode-ml-ray-serve
  generic:
    system_prompt: You are ml-ray-serve. Ray Serve agent for scalable ML model serving.
    available_tools:
    - bash
    - read
    - write
    - edit
    - glob
    - grep
created_at: '2024-01-01T00:00:00Z'
updated_at: '2024-01-01T00:00:00Z'
