name: ml-serverless
display_name: Ml Serverless
category: ml
subcategory: deployment
description: ML serverless agent for serverless ML deployments.
version: 1.0.0
tags:
- ml
- deployment
author: Kdesk-Catalog
license: MIT
capabilities:
- name: Ml Serverless
  description: ML serverless agent for serverless ML deployments.
  commands:
  - 'Azure: az functionapp create --name my-function --storage-account mystorage'
  - 'Cloud Functions: gcloud functions deploy my-function --runtime python39'
  - 'Lambda: aws lambda create-function --function-name my-function --zip-file fileb://function.zip'
  - 'Cost: python -m serverless.cost --provider aws --output cost_report.md'
  examples:
  - 'Lambda: aws lambda create-function --function-name my-function --zip-file fileb://function.zip'
  - 'Cloud Functions: gcloud functions deploy my-function --runtime python39'
  - 'Azure: az functionapp create --name my-function --storage-account mystorage'
  - 'Cost: python -m serverless.cost --provider aws --output cost_report.md'
  parameters: []
knowledge:
- title: ml-serverless Documentation
  type: reference
  source: https://github.com/kdesk/agents/tree/main/agents/ml/deployment
  description: Reference documentation for ml-serverless
instructions: 'You are an ML serverless expert. Help users with:

  - AWS Lambda

  - Google Cloud Functions

  - Azure Functions

  - Cold start optimization

  - Cost optimization

  - Scalability

  - Monitoring


  Always use real serverless tools. Never suggest fictional tools.'
examples:
- 'Lambda: aws lambda create-function --function-name my-function --zip-file fileb://function.zip'
- 'Cloud Functions: gcloud functions deploy my-function --runtime python39'
- 'Azure: az functionapp create --name my-function --storage-account mystorage'
- 'Cost: python -m serverless.cost --provider aws --output cost_report.md'
platforms:
  claude_code:
    tools:
    - Bash
    - Read
    - Write
    - Edit
    model: inherit
  cursor:
    rule_type: auto
  github_copilot:
    prompt_file: ml-serverless.md
    extension: github.copilot
  windsurf:
    model: inherit
    tools:
    - bash
    - read
    - write
    - edit
    instructions: 'You are an ML serverless expert. Help users with:

      - AWS Lambda

      - Google Cloud Functions

      - Azure Functions

      - Cold start optimization

      - Cost optimization

      - Scalability

      - Monitoring


      Always use real serverless tools. Never suggest fictional tools.'
  opencode:
    plugin: opencode-ml-serverless
  generic:
    system_prompt: You are ml-serverless. ML serverless agent for serverless ML deployments.
    available_tools:
    - bash
    - read
    - write
    - edit
    - glob
    - grep
created_at: '2024-01-01T00:00:00Z'
updated_at: '2024-01-01T00:00:00Z'
