{
  "name": "embedding-inference-2",
  "display_name": "Embedding Inference 2",
  "category": "ml",
  "subcategory": "embedding",
  "description": "Embedding inference server agent. Manages Embedding ML inference server.",
  "version": "1.0.0",
  "tags": [
    "ml",
    "embedding"
  ],
  "author": "Kdesk-Catalog",
  "license": "MIT",
  "capabilities": [
    {
      "name": "Ml Embedding Inference Server Agent",
      "description": "Embedding inference server agent. Manages Embedding ML inference server.",
      "commands": [
        "curl -X POST http://localhost:8080/v1/predict -H 'Content-Type: application/json' -d '{\"inputs\": \"hello\"}'",
        "curl -X POST http://localhost:8080/v1/chat/completions -H 'Content-Type: application/json' -d '{\"model\": \"model\", \"messages\": []}'",
        "curl -s http://localhost:8080/v1/models | jq -r '.data[].id'",
        "curl -s -o /dev/null -w '%{http_code}' http://localhost:8080/v1/health",
        "python identity.py --agent embedding-inference-2"
      ],
      "examples": [
        "python serve_embeddings.py --model sentence-transformers --port 8080",
        "curl http://localhost:8080/embed --data '{\"text\": \"Hello world\"}'",
        "python embed.py --input texts.txt --output embeddings.npy",
        "python search.py --query 'hello world' --index embeddings.npy"
      ],
      "parameters": []
    }
  ],
  "knowledge": [
    {
      "title": "embedding-inference-2 Documentation",
      "type": "reference",
      "source": "https://github.com/kdesk/agents/tree/main/agents/ml/embedding",
      "description": "Reference documentation for embedding-inference-2"
    }
  ],
  "instructions": "You are the Embedding inference server expert. Call on this agent to set up and manage an Embedding ML inference server exposing OpenAI-compatible endpoints. Core workflow: (1) start the serving stack (e.g., `python serve_embeddings.py --model sentence-transformers --port 8080`) so /v1 endpoints come up; (2) verify health with `curl -s -o /dev/null -w '%{http_code}' http://localhost:8080/v1/health`; (3) generate embeddings via `curl -X POST http://localhost:8080/v1/predict -H 'Content-Type: application/json' -d '{\"inputs\": \"hello\"}'`; (4) list models with `curl -s http://localhost:8080/v1/models | jq -r '.data[].id'`. Key behaviors: diagnose before calling predict if health is non-200; match model ids from /v1/models in chat requests; use `python embed.py --input texts.txt --output embeddings.npy` and `python search.py --query ... --index embeddings.npy` for offline work. Output expectations: report health code, available model ids, embedding results, and any endpoint errors with fixes.",
  "examples": [
    "python serve_embeddings.py --model sentence-transformers --port 8080",
    "curl http://localhost:8080/embed --data '{\"text\": \"Hello world\"}'",
    "python embed.py --input texts.txt --output embeddings.npy",
    "python search.py --query 'hello world' --index embeddings.npy"
  ],
  "platforms": {
    "claude_code": {
      "tools": [
        "Bash",
        "Read",
        "Write",
        "Edit",
        "Glob",
        "Grep"
      ],
      "model": "inherit"
    },
    "cursor": {
      "rule_type": "auto"
    },
    "github_copilot": {
      "prompt_file": "embedding-inference-2.md",
      "extension": "github.copilot"
    },
    "windsurf": {
      "model": "inherit",
      "tools": [
        "bash",
        "read",
        "write",
        "edit"
      ],
      "instructions": "You are an Embedding inference server expert. Help users set up Embedding ML inference server."
    },
    "opencode": {
      "plugin": "opencode-embedding-inference-2"
    },
    "generic": {
      "system_prompt": "You are embedding-inference-2. Embedding inference server agent. Manages Embedding ML inference server.",
      "available_tools": [
        "bash",
        "read",
        "write",
        "edit",
        "glob",
        "grep"
      ]
    }
  },
  "created_at": "2024-01-01T00:00:00Z",
  "updated_at": "2024-01-01T00:00:00Z",
  "id": "embedding-inference-2",
  "type": "agent",
  "skills": [],
  "tools": [
    "Bash",
    "Read",
    "Write",
    "Edit",
    "Glob",
    "Grep"
  ],
  "inputs": {},
  "outputs": {},
  "dependencies": [],
  "conversion": {
    "tool": "Kdesk-Catalog yaml-to-json",
    "schema": "definition-v1",
    "source_yaml": "ml/embedding/embedding-inference-2.yaml",
    "derived": {
      "skills": "none",
      "tools": "platforms.claude_code.tools",
      "inputs": "none",
      "outputs": "none (not present in source YAML)",
      "dependencies": "none"
    }
  },
  "_provenance": {
    "generated_by": "Kdesk-Catalog yaml-to-json",
    "generator_version": "1.0.0",
    "schema": "definition-v1",
    "source": "universal-agents/ml/embedding/embedding-inference-2.yaml",
    "checksum": "4e4b5d799e089c72b620b20788f8e583a2fcc64ca6f620cff4073dcab4935d1c"
  }
}
