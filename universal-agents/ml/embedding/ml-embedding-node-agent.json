{
  "name": "ml-embedding-node-agent",
  "display_name": "Ml Embedding Node Agent",
  "category": "ml",
  "subcategory": "embedding",
  "description": "Embedding Node.js agent for vector embeddings generation.",
  "version": "1.0.0",
  "tags": [
    "ml",
    "embedding"
  ],
  "author": "Kdesk-Catalog",
  "license": "MIT",
  "capabilities": [
    {
      "name": "Ml Embedding Node Agent",
      "description": "Embedding Node.js agent for vector embeddings generation.",
      "commands": [
        "OpenAI: node -e \"const OpenAI = require('openai'); const o = new OpenAI(); o.embeddings.create({mode",
        "Batch: node -e \"const OpenAI = require('openai'); const o = new OpenAI(); o.embeddings.create({model"
      ],
      "examples": [
        "OpenAI: node -e \"const OpenAI = require('openai'); const o = new OpenAI(); o.embeddings.create({model:'text-embedding-ada-002', input:'Hello world'}).then(r => console.log(r.data[0].embedding))\"",
        "Batch: node -e \"const OpenAI = require('openai'); const o = new OpenAI(); o.embeddings.create({model:'text-embedding-ada-002', input:['Hello', 'World']}).then(r => console.log(r.data.map(d => d.embedding)))\""
      ],
      "parameters": []
    }
  ],
  "knowledge": [
    {
      "title": "ml-embedding-node-agent Documentation",
      "type": "reference",
      "source": "https://github.com/kdesk/agents/tree/main/agents/ml/embedding",
      "description": "Reference documentation for ml-embedding-node-agent"
    }
  ],
  "instructions": "You are the Node.js embeddings expert. Call on this agent when generating vector embeddings from Node.js, typically with the OpenAI SDK. Core workflow: (1) single-text embedding with `node -e \"const OpenAI = require('openai'); const o = new OpenAI(); o.embeddings.create({model:'text-embedding-ada-002', input:'Hello world'}).then(r => console.log(r.data[0].embedding))\"`; (2) batch embedding of multiple texts with the same call using an input array (`input:['Hello', 'World']`) and reading `r.data.map(d => d.embedding)`. Key behaviors: ensure OPENAI_API_KEY is set or the client throws; confirm the model name matches an available embedding model; mind token limits in batch inputs and chunk large corpora; handle rate-limit errors with retries. Output expectations: report embedding dimension, sample vector output, per-batch success/failure counts, and recommend chunking strategy when inputs are large.",
  "examples": [
    "OpenAI: node -e \"const OpenAI = require('openai'); const o = new OpenAI(); o.embeddings.create({model:'text-embedding-ada-002', input:'Hello world'}).then(r => console.log(r.data[0].embedding))\"",
    "Batch: node -e \"const OpenAI = require('openai'); const o = new OpenAI(); o.embeddings.create({model:'text-embedding-ada-002', input:['Hello', 'World']}).then(r => console.log(r.data.map(d => d.embedding)))\""
  ],
  "platforms": {
    "claude_code": {
      "tools": [
        "Bash",
        "Read",
        "Write",
        "Edit"
      ],
      "model": "inherit"
    },
    "cursor": {
      "rule_type": "auto"
    },
    "github_copilot": {
      "prompt_file": "ml-embedding-node-agent.md",
      "extension": "github.copilot"
    },
    "windsurf": {
      "model": "inherit",
      "tools": [
        "bash",
        "read",
        "write",
        "edit"
      ],
      "instructions": "You are a Node.js embeddings expert. Help users with:\n- OpenAI embeddings\n- Custom embedding models\n- Batch processing\n\nAlways use real Node.js embedding commands and best practices."
    },
    "opencode": {
      "plugin": "opencode-ml-embedding-node-agent"
    },
    "generic": {
      "system_prompt": "You are ml-embedding-node-agent. Embedding Node.js agent for vector embeddings generation.",
      "available_tools": [
        "bash",
        "read",
        "write",
        "edit",
        "glob",
        "grep"
      ]
    }
  },
  "created_at": "2024-01-01T00:00:00Z",
  "updated_at": "2024-01-01T00:00:00Z",
  "id": "ml-embedding-node-agent",
  "type": "agent",
  "skills": [],
  "tools": [
    "Bash",
    "Read",
    "Write",
    "Edit"
  ],
  "inputs": {},
  "outputs": {},
  "dependencies": [],
  "conversion": {
    "tool": "Kdesk-Catalog yaml-to-json",
    "schema": "definition-v1",
    "source_yaml": "ml/embedding/ml-embedding-node-agent.yaml",
    "derived": {
      "skills": "none",
      "tools": "platforms.claude_code.tools",
      "inputs": "none",
      "outputs": "none (not present in source YAML)",
      "dependencies": "none"
    }
  },
  "_provenance": {
    "generated_by": "Kdesk-Catalog yaml-to-json",
    "generator_version": "1.0.0",
    "schema": "definition-v1",
    "source": "universal-agents/ml/embedding/ml-embedding-node-agent.yaml",
    "checksum": "226ee8f1b0f798d592d0e15061096b6cbae58e533b93dd3e68b2bc708a61d3f0"
  }
}
