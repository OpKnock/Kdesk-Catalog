name: ml-embedding-node
display_name: Ml Embedding Node
category: ml
subcategory: embedding
description: Embedding generation Node.js agent for text embeddings.
version: 1.0.0
tags:
- ml
- embedding
author: Mehul Wagde
license: MIT
capabilities:
- name: Ml Embedding Node
  description: Embedding generation Node.js agent for text embeddings.
  commands:
  - 'Similarity: const similarity = (a, b) => a.reduce((sum, val, i) => sum + val * b[i], 0) / (Math.sqrt'
  - 'Cohere: import CohereClient from ''cohere-ai''; const client = new CohereClient({token: ''API_KEY''});
    c'
  - 'OpenAI: import OpenAI from ''openai''; const client = new OpenAI(); const response = await client.embe'
  - 'Hugging Face: import { pipeline } from ''@huggingface/inference''; const featureExtraction = pipeline('
  examples:
  - 'OpenAI: import OpenAI from ''openai''; const client = new OpenAI(); const response = await client.embeddings.create({model:
    ''text-embedding-3-small'', input: ''Hello''})'
  - 'Hugging Face: import { pipeline } from ''@huggingface/inference''; const featureExtraction = pipeline(''feature-extraction'',
    ''Xenova/all-MiniLM-L6-v2''); const output = await featureExtraction(''Hello'')'
  - 'Cohere: import CohereClient from ''cohere-ai''; const client = new CohereClient({token: ''API_KEY''});
    const response = await client.embed({model: ''embed-english-v3.0'', texts: [''Hello'']})'
  - 'Similarity: const similarity = (a, b) => a.reduce((sum, val, i) => sum + val * b[i], 0) / (Math.sqrt(a.reduce((sum,
    val) => sum + val * val, 0)) * Math.sqrt(b.reduce((sum, val) => sum + val * val, 0)))'
  parameters: []
knowledge:
- title: OpenAI Embeddings Guide
  type: reference
  source: https://platform.openai.com/docs/guides/embeddings
  description: Official documentation for embedding.
- title: OpenAI API Documentation
  type: reference
  source: https://platform.openai.com/docs/
  description: Official documentation for openai.
- title: TensorFlow Serving
  type: reference
  source: https://www.tensorflow.org/serving
  description: Official documentation for inference.
instructions: 'You are an embedding generation Node.js expert. Help users with:

  - OpenAI embeddings

  - Cohere embeddings

  - Hugging Face embeddings

  - Sentence transformers

  - Custom embeddings

  - Batch processing

  - Similarity search


  Always use real embedding tools. Never suggest fictional tools.'
examples:
- 'OpenAI: import OpenAI from ''openai''; const client = new OpenAI(); const response = await client.embeddings.create({model:
  ''text-embedding-3-small'', input: ''Hello''})'
- 'Hugging Face: import { pipeline } from ''@huggingface/inference''; const featureExtraction = pipeline(''feature-extraction'',
  ''Xenova/all-MiniLM-L6-v2''); const output = await featureExtraction(''Hello'')'
- 'Cohere: import CohereClient from ''cohere-ai''; const client = new CohereClient({token: ''API_KEY''});
  const response = await client.embed({model: ''embed-english-v3.0'', texts: [''Hello'']})'
- 'Similarity: const similarity = (a, b) => a.reduce((sum, val, i) => sum + val * b[i], 0) / (Math.sqrt(a.reduce((sum,
  val) => sum + val * val, 0)) * Math.sqrt(b.reduce((sum, val) => sum + val * val, 0)))'
platforms:
  claude_code:
    tools:
    - Bash
    - Read
    - Write
    - Edit
    model: inherit
  cursor:
    rule_type: auto
  github_copilot:
    prompt_file: ml-embedding-node.md
    extension: github.copilot
  windsurf:
    model: inherit
    tools:
    - bash
    - read
    - write
    - edit
    instructions: 'You are an embedding generation Node.js expert. Help users with:

      - OpenAI embeddings

      - Cohere embeddings

      - Hugging Face embeddings

      - Sentence transformers

      - Custom embeddings

      - Batch processing

      - Similarity search


      Always use real embedding tools. Never suggest fictional tools.'
  opencode:
    plugin: opencode-ml-embedding-node
  generic:
    system_prompt: You are ml-embedding-node. Embedding generation Node.js agent for text embeddings.
    available_tools:
    - bash
    - read
    - write
    - edit
    - glob
    - grep
created_at: '2025-09-30T00:00:00Z'
updated_at: '2026-08-19T00:00:00Z'
