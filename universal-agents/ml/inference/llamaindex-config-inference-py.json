{
  "name": "llamaindex-config-inference-py",
  "display_name": "Llamaindex Config Inference Py",
  "category": "ml",
  "subcategory": "inference",
  "description": "LlamaIndex inference server agent Manages LlamaIndex inference server.",
  "version": "1.0.0",
  "tags": [
    "ml",
    "inference"
  ],
  "author": "Kdesk-Catalog",
  "license": "MIT",
  "capabilities": [
    {
      "name": "Ml Llamaindex Inference Server Agent V2",
      "description": "LlamaIndex inference server agent. Manages LlamaIndex inference server.",
      "commands": [
        "python config_inference.py --index index.json",
        "python test_inference_server.py --endpoint http://localhost:8080",
        "curl http://localhost:8080/query --data '{\"query\": \"What is in the documents?\"}'",
        "python inference_server.py --index index.json --port 8080"
      ],
      "examples": [
        "python inference_server.py --index index.json --port 8080",
        "curl http://localhost:8080/query --data '{\"query\": \"What is in the documents?\"}'",
        "python test_inference_server.py --endpoint http://localhost:8080",
        "python config_inference.py --index index.json"
      ],
      "parameters": [
        {
          "name": "index",
          "type": "string",
          "description": "CLI flag --index observed in capability commands"
        }
      ]
    }
  ],
  "knowledge": [
    {
      "title": "llamaindex-config-inference-py Documentation",
      "type": "reference",
      "source": "https://github.com/kdesk/agents/tree/main/agents/ml/inference",
      "description": "Reference documentation for llamaindex-config-inference-py"
    }
  ],
  "instructions": "You are the LlamaIndex inference server expert. Call on this agent to set up and operate a LlamaIndex-based ML inference server exposing OpenAI-compatible endpoints. Core workflow: (1) start the server with `python -m llamaindex.inference_server --port 8080 --workers 4`; (2) verify health with `curl -s -o /dev/null -w '%{http_code}' http://localhost:8080/v1/health` and list models with `curl -s http://localhost:8080/v1/models | jq -r '.data[].id'`; (3) predict with `curl -X POST http://localhost:8080/v1/predict -H 'Content-Type: application/json' -d '{\"inputs\": \"hello\"}'` and chat with `curl -X POST http://localhost:8080/v1/chat/completions -H 'Content-Type: application/json' -d '{\"model\": \"llamaindex\", \"messages\": []}'`. Key behaviors: treat non-200 health as a down server; confirm the index/data files exist before starting; on startup failure inspect logs and port binding. Output expectations: report health status, served model ids, sample outputs, and any errors encountered.",
  "examples": [
    "python inference_server.py --index index.json --port 8080",
    "curl http://localhost:8080/query --data '{\"query\": \"What is in the documents?\"}'",
    "python test_inference_server.py --endpoint http://localhost:8080",
    "python config_inference.py --index index.json"
  ],
  "platforms": {
    "claude_code": {
      "tools": [
        "Bash",
        "Read",
        "Write",
        "Edit",
        "Glob",
        "Grep"
      ],
      "model": "inherit"
    },
    "cursor": {
      "rule_type": "auto"
    },
    "github_copilot": {
      "prompt_file": "llamaindex-config-inference-py.md",
      "extension": "github.copilot"
    },
    "windsurf": {
      "model": "inherit",
      "tools": [
        "bash",
        "read",
        "write",
        "edit"
      ],
      "instructions": "You are a LlamaIndex inference server expert. Help users set up LlamaIndex inference server."
    },
    "opencode": {
      "plugin": "opencode-llamaindex-config-inference-py"
    },
    "generic": {
      "system_prompt": "You are llamaindex-config-inference-py. LlamaIndex inference server agent. Manages LlamaIndex inference server.",
      "available_tools": [
        "bash",
        "read",
        "write",
        "edit",
        "glob",
        "grep"
      ]
    }
  },
  "created_at": "2024-01-01T00:00:00Z",
  "updated_at": "2024-01-01T00:00:00Z",
  "id": "llamaindex-config-inference-py",
  "type": "agent",
  "skills": [],
  "tools": [
    "Bash",
    "Read",
    "Write",
    "Edit",
    "Glob",
    "Grep"
  ],
  "inputs": {
    "parameters": [
      {
        "capability": "Ml Llamaindex Inference Server Agent V2",
        "name": "index",
        "type": "string",
        "description": "CLI flag --index observed in capability commands"
      }
    ]
  },
  "outputs": {},
  "dependencies": [],
  "conversion": {
    "tool": "Kdesk-Catalog yaml-to-json",
    "schema": "definition-v1",
    "source_yaml": "ml/inference/llamaindex-config-inference-py.yaml",
    "derived": {
      "skills": "none",
      "tools": "platforms.claude_code.tools",
      "inputs": "capability.parameters",
      "outputs": "none (not present in source YAML)",
      "dependencies": "none"
    }
  },
  "_provenance": {
    "generated_by": "Kdesk-Catalog yaml-to-json",
    "generator_version": "1.0.0",
    "schema": "definition-v1",
    "source": "universal-agents/ml/inference/llamaindex-config-inference-py.yaml",
    "checksum": "699a43991d1283739ee303051f5b9eb9a7c39c03a26b064e4f2cae2057b21a79"
  }
}
