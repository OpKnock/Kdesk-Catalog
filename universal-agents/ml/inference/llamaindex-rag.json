{
  "name": "llamaindex-rag",
  "display_name": "Llamaindex Rag",
  "category": "ml",
  "subcategory": "inference",
  "description": "LlamaIndex RAG agent. Manages retrieval-augmented generation with LlamaIndex.",
  "version": "1.0.0",
  "tags": [
    "ml",
    "inference"
  ],
  "author": "Kdesk-Catalog",
  "license": "MIT",
  "capabilities": [
    {
      "name": "Ml Llamaindex Rag Agent",
      "description": "LlamaIndex RAG agent. Manages retrieval-augmented generation with LlamaIndex.",
      "commands": [
        "python build_rag_index.py --data ./docs --collection llamaindex-rag --chunk 512",
        "python query_rag.py --collection llamaindex-rag --question 'What is covered?' --top-k 5",
        "python update_rag.py --collection llamaindex-rag --upsert docs/update.json",
        "curl -X POST http://localhost:8080/v1/rag -d '{\"question\": \"test\"}'"
      ],
      "examples": [
        "python build_index.py --data ./data --output index.json",
        "python query.py --index index.json --query 'What is in the documents?'",
        "python serve.py --index index.json --port 8080",
        "python test_index.py --index index.json"
      ],
      "parameters": [
        {
          "name": "collection",
          "type": "string",
          "description": "CLI flag --collection observed in capability commands"
        }
      ]
    }
  ],
  "knowledge": [
    {
      "title": "llamaindex-rag Documentation",
      "type": "reference",
      "source": "https://github.com/kdesk/agents/tree/main/agents/ml/inference",
      "description": "Reference documentation for llamaindex-rag"
    }
  ],
  "instructions": "You are the LlamaIndex RAG expert. Call on this agent to build retrieval-augmented generation pipelines over user documents. Core workflow: (1) build the index with `python build_index.py --data ./data --output index.json`; (2) query it with `python query.py --index index.json --query 'What is in the documents?'`; (3) run the RAG flow with `python query_rag.py --index index.json --query 'Summarize the key points'`. Key behaviors: build the index before querying; confirm the --index path matches the build output; check the LLM provider API key is configured; if queries return empty results verify the data directory is not empty. Output expectations: report the built index location, retrieved-context answers with citations where available, and any pipeline errors.",
  "examples": [
    "python build_index.py --data ./data --output index.json",
    "python query.py --index index.json --query 'What is in the documents?'",
    "python serve.py --index index.json --port 8080",
    "python test_index.py --index index.json"
  ],
  "platforms": {
    "claude_code": {
      "tools": [
        "Bash",
        "Read",
        "Write",
        "Edit",
        "Glob",
        "Grep"
      ],
      "model": "inherit"
    },
    "cursor": {
      "rule_type": "auto"
    },
    "github_copilot": {
      "prompt_file": "llamaindex-rag.md",
      "extension": "github.copilot"
    },
    "windsurf": {
      "model": "inherit",
      "tools": [
        "bash",
        "read",
        "write",
        "edit"
      ],
      "instructions": "You are a LlamaIndex RAG expert. Help users build RAG applications with LlamaIndex."
    },
    "opencode": {
      "plugin": "opencode-llamaindex-rag"
    },
    "generic": {
      "system_prompt": "You are llamaindex-rag. LlamaIndex RAG agent. Manages retrieval-augmented generation with LlamaIndex.",
      "available_tools": [
        "bash",
        "read",
        "write",
        "edit",
        "glob",
        "grep"
      ]
    }
  },
  "created_at": "2024-01-01T00:00:00Z",
  "updated_at": "2024-01-01T00:00:00Z",
  "id": "llamaindex-rag",
  "type": "agent",
  "skills": [],
  "tools": [
    "Bash",
    "Read",
    "Write",
    "Edit",
    "Glob",
    "Grep"
  ],
  "inputs": {
    "parameters": [
      {
        "capability": "Ml Llamaindex Rag Agent",
        "name": "collection",
        "type": "string",
        "description": "CLI flag --collection observed in capability commands"
      }
    ]
  },
  "outputs": {},
  "dependencies": [],
  "conversion": {
    "tool": "Kdesk-Catalog yaml-to-json",
    "schema": "definition-v1",
    "source_yaml": "ml/inference/llamaindex-rag.yaml",
    "derived": {
      "skills": "none",
      "tools": "platforms.claude_code.tools",
      "inputs": "capability.parameters",
      "outputs": "none (not present in source YAML)",
      "dependencies": "none"
    }
  },
  "_provenance": {
    "generated_by": "Kdesk-Catalog yaml-to-json",
    "generator_version": "1.0.0",
    "schema": "definition-v1",
    "source": "universal-agents/ml/inference/llamaindex-rag.yaml",
    "checksum": "6f6c57da8e848bed7f439bea38383eed79ad9c1226111f300f2fbd4ed1da160e"
  }
}
