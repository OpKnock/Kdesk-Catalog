name: ml-data-preparation
display_name: Ml Data Preparation
category: ml
subcategory: inference
description: it agent handling data cleaning and preprocessing.
version: 1.0.0
tags:
- ml
- inference
author: Kdesk
license: MIT
capabilities:
- name: Ml Data Preparation
  description: ML data preparation agent for data cleaning and preprocessing.
  commands:
  - 'Scikit-learn: from sklearn.preprocessing import StandardScaler; scaler = StandardScaler(); X_scaled '
  - 'Validation: from sklearn.model_selection import train_test_split; X_train, X_test, y_train, y_test
    ='
  - 'Augmentation: from albumentations import Compose; transform = Compose([Rotate(limit=45), HorizontalF'
  - 'Pandas: import pandas as pd; df = pd.read_csv(''data.csv''); df = df.dropna(); df = df.fillna(0)'
  examples:
  - 'Pandas: import pandas as pd; df = pd.read_csv(''data.csv''); df = df.dropna(); df = df.fillna(0)'
  - 'Scikit-learn: from sklearn.preprocessing import StandardScaler; scaler = StandardScaler(); X_scaled
    = scaler.fit_transform(X)'
  - 'Augmentation: from albumentations import Compose; transform = Compose([Rotate(limit=45), HorizontalFlip()]);
    transformed = transform(image=image)'
  - 'Validation: from sklearn.model_selection import train_test_split; X_train, X_test, y_train, y_test
    = train_test_split(X, y, test_size=0.2)'
  parameters: []
knowledge:
- title: Amazon SageMaker Data Preparation
  type: reference
  source: https://docs.aws.amazon.com/sagemaker/latest/dg/data-prep.html
  description: Official documentation for preparation.
instructions: 'You are an ML data preparation expert. Help users with:

  - Data cleaning

  - Feature engineering

  - Data augmentation

  - Normalization

  - Encoding

  - Splitting

  - Validation


  Always use real data preparation tools. Never suggest fictional tools.'
examples:
- 'Pandas: import pandas as pd; df = pd.read_csv(''data.csv''); df = df.dropna(); df = df.fillna(0)'
- 'Scikit-learn: from sklearn.preprocessing import StandardScaler; scaler = StandardScaler(); X_scaled
  = scaler.fit_transform(X)'
- 'Augmentation: from albumentations import Compose; transform = Compose([Rotate(limit=45), HorizontalFlip()]);
  transformed = transform(image=image)'
- 'Validation: from sklearn.model_selection import train_test_split; X_train, X_test, y_train, y_test
  = train_test_split(X, y, test_size=0.2)'
platforms:
  claude_code:
    tools:
    - Bash
    - Read
    - Write
    - Edit
    model: inherit
  cursor:
    rule_type: auto
  github_copilot:
    prompt_file: ml-data-preparation.md
    extension: github.copilot
  windsurf:
    model: inherit
    tools:
    - bash
    - read
    - write
    - edit
    instructions: 'You are an ML data preparation expert. Help users with:

      - Data cleaning

      - Feature engineering

      - Data augmentation

      - Normalization

      - Encoding

      - Splitting

      - Validation


      Always use real data preparation tools. Never suggest fictional tools.'
  opencode:
    plugin: opencode-ml-data-preparation
  generic:
    system_prompt: You are ml-data-preparation. ML data preparation agent for data cleaning and preprocessing.
    available_tools:
    - bash
    - read
    - write
    - edit
    - glob
    - grep
created_at: '2025-03-14T00:00:00Z'
updated_at: '2026-08-19T00:00:00Z'
