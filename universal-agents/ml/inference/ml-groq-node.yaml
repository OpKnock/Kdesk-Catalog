name: ml-groq-node
display_name: Ml Groq Node
category: ml
subcategory: inference
description: Groq Node.js SDK agent for fast LLM inference.
version: 1.0.0
tags:
- ml
- inference
author: Kdesk-Catalog
license: MIT
capabilities:
- name: Ml Groq Node
  description: Groq Node.js SDK agent for fast LLM inference.
  commands:
  - 'Client: import Groq from ''groq-sdk''; const client = new Groq()'
  - 'Chat: const completion = await client.chat.completions.create({model: ''llama-3.3-70b-versatile'',
    mes'
  - 'Stream: const stream = await client.chat.completions.create({model: ''llama-3.3-70b-versatile'',
    messa'
  - 'Install: npm install groq-sdk'
  examples:
  - 'Install: npm install groq-sdk'
  - 'Client: import Groq from ''groq-sdk''; const client = new Groq()'
  - 'Chat: const completion = await client.chat.completions.create({model: ''llama-3.3-70b-versatile'',
    messages: [{role: ''user'', content: ''Hello''}]})'
  - 'Stream: const stream = await client.chat.completions.create({model: ''llama-3.3-70b-versatile'',
    messages: [...], stream: true})'
  parameters: []
knowledge:
- title: ml-groq-node Documentation
  type: reference
  source: https://github.com/kdesk/agents/tree/main/agents/ml/inference
  description: Reference documentation for ml-groq-node
instructions: 'You are a Groq Node.js SDK expert. Help users with:

  - Client initialization

  - Chat completions

  - Text completions

  - Embeddings

  - Model selection

  - Rate limiting

  - Streaming


  Always use real Groq Node.js SDK tools. Never suggest fictional tools.'
examples:
- 'Install: npm install groq-sdk'
- 'Client: import Groq from ''groq-sdk''; const client = new Groq()'
- 'Chat: const completion = await client.chat.completions.create({model: ''llama-3.3-70b-versatile'',
  messages: [{role: ''user'', content: ''Hello''}]})'
- 'Stream: const stream = await client.chat.completions.create({model: ''llama-3.3-70b-versatile'', messages:
  [...], stream: true})'
platforms:
  claude_code:
    tools:
    - Bash
    - Read
    - Write
    - Edit
    model: inherit
  cursor:
    rule_type: auto
  github_copilot:
    prompt_file: ml-groq-node.md
    extension: github.copilot
  windsurf:
    model: inherit
    tools:
    - bash
    - read
    - write
    - edit
    instructions: 'You are a Groq Node.js SDK expert. Help users with:

      - Client initialization

      - Chat completions

      - Text completions

      - Embeddings

      - Model selection

      - Rate limiting

      - Streaming


      Always use real Groq Node.js SDK tools. Never suggest fictional tools.'
  opencode:
    plugin: opencode-ml-groq-node
  generic:
    system_prompt: You are ml-groq-node. Groq Node.js SDK agent for fast LLM inference.
    available_tools:
    - bash
    - read
    - write
    - edit
    - glob
    - grep
created_at: '2024-01-01T00:00:00Z'
updated_at: '2024-01-01T00:00:00Z'
