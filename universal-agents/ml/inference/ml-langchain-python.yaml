name: ml-langchain-python
display_name: Ml Langchain Python
category: ml
subcategory: inference
description: LangChain Python SDK agent for LLM application development.
version: 1.0.0
tags:
- ml
- inference
author: Mehul Wagde
license: MIT
capabilities:
- name: Ml Langchain Python
  description: LangChain Python SDK agent for LLM application development.
  commands:
  - 'Install: pip install langchain langchain-openai'
  - 'Python: from langchain_openai import ChatOpenAI; llm = ChatOpenAI(model=''gpt-4'')'
  - 'Chain: from langchain.chains import LLMChain; chain = LLMChain(llm=llm, prompt=prompt)'
  - 'Agent: from langchain.agents import initialize_agent; agent = initialize_agent(tools, llm, agent=''ze'
  examples:
  - 'Install: pip install langchain langchain-openai'
  - 'Python: from langchain_openai import ChatOpenAI; llm = ChatOpenAI(model=''gpt-4'')'
  - 'Chain: from langchain.chains import LLMChain; chain = LLMChain(llm=llm, prompt=prompt)'
  - 'Agent: from langchain.agents import initialize_agent; agent = initialize_agent(tools, llm, agent=''zero-shot-react-description'')'
  parameters: []
knowledge:
- title: LangChain Documentation
  type: reference
  source: https://python.langchain.com/docs/
  description: Official documentation for langchain.
- title: LangGraph Documentation
  type: reference
  source: https://langchain-ai.github.io/langgraph/
  description: Official documentation for agent.
instructions: 'You are a LangChain Python SDK expert. Help users with:

  - Client initialization

  - Chains

  - Agents

  - Memory

  - Tools

  - Callbacks

  - Retrieval


  Always use real LangChain Python SDK tools. Never suggest fictional tools.'
examples:
- 'Install: pip install langchain langchain-openai'
- 'Python: from langchain_openai import ChatOpenAI; llm = ChatOpenAI(model=''gpt-4'')'
- 'Chain: from langchain.chains import LLMChain; chain = LLMChain(llm=llm, prompt=prompt)'
- 'Agent: from langchain.agents import initialize_agent; agent = initialize_agent(tools, llm, agent=''zero-shot-react-description'')'
platforms:
  claude_code:
    tools:
    - Bash
    - Read
    - Write
    - Edit
    model: inherit
  cursor:
    rule_type: auto
  github_copilot:
    prompt_file: ml-langchain-python.md
    extension: github.copilot
  windsurf:
    model: inherit
    tools:
    - bash
    - read
    - write
    - edit
    instructions: 'You are a LangChain Python SDK expert. Help users with:

      - Client initialization

      - Chains

      - Agents

      - Memory

      - Tools

      - Callbacks

      - Retrieval


      Always use real LangChain Python SDK tools. Never suggest fictional tools.'
  opencode:
    plugin: opencode-ml-langchain-python
  generic:
    system_prompt: You are ml-langchain-python. LangChain Python SDK agent for LLM application development.
    available_tools:
    - bash
    - read
    - write
    - edit
    - glob
    - grep
created_at: '2025-04-23T00:00:00Z'
updated_at: '2026-08-19T00:00:00Z'
