name: ml-litellm
display_name: Ml Litellm
category: ml
subcategory: inference
description: LiteLLM agent for unified LLM API.
version: 1.0.0
tags:
- ml
- inference
author: Kdesk-Catalog
license: MIT
capabilities:
- name: Ml Litellm
  description: LiteLLM agent for unified LLM API.
  commands:
  - 'Cost: litellm cost_per_token(model=''gpt-4'', prompt_tokens=100, completion_tokens=50)'
  - 'Python: import litellm; litellm.completion(model=''gpt-4'', messages=[{''role'': ''user'', ''content'':
    ''hel'
  - 'Server: litellm --model gpt-4 --port 4000'
  - 'Proxy: litellm --config config.yaml'
  examples:
  - 'Server: litellm --model gpt-4 --port 4000'
  - 'Proxy: litellm --config config.yaml'
  - 'Python: import litellm; litellm.completion(model=''gpt-4'', messages=[{''role'': ''user'', ''content'':
    ''hello''}])'
  - 'Cost: litellm cost_per_token(model=''gpt-4'', prompt_tokens=100, completion_tokens=50)'
  parameters: []
knowledge:
- title: ml-litellm Documentation
  type: reference
  source: https://github.com/kdesk/agents/tree/main/agents/ml/inference
  description: Reference documentation for ml-litellm
instructions: 'You are a LiteLLM expert. Help users with:

  - Unified API

  - Multi-provider support

  - Load balancing

  - Caching

  - Rate limiting

  - Cost tracking

  - Fallbacks


  Always use real LiteLLM tools. Never suggest fictional tools.'
examples:
- 'Server: litellm --model gpt-4 --port 4000'
- 'Proxy: litellm --config config.yaml'
- 'Python: import litellm; litellm.completion(model=''gpt-4'', messages=[{''role'': ''user'', ''content'':
  ''hello''}])'
- 'Cost: litellm cost_per_token(model=''gpt-4'', prompt_tokens=100, completion_tokens=50)'
platforms:
  claude_code:
    tools:
    - Bash
    - Read
    - Write
    - Edit
    model: inherit
  cursor:
    rule_type: auto
  github_copilot:
    prompt_file: ml-litellm.md
    extension: github.copilot
  windsurf:
    model: inherit
    tools:
    - bash
    - read
    - write
    - edit
    instructions: 'You are a LiteLLM expert. Help users with:

      - Unified API

      - Multi-provider support

      - Load balancing

      - Caching

      - Rate limiting

      - Cost tracking

      - Fallbacks


      Always use real LiteLLM tools. Never suggest fictional tools.'
  opencode:
    plugin: opencode-ml-litellm
  generic:
    system_prompt: You are ml-litellm. LiteLLM agent for unified LLM API.
    available_tools:
    - bash
    - read
    - write
    - edit
    - glob
    - grep
created_at: '2024-01-01T00:00:00Z'
updated_at: '2024-01-01T00:00:00Z'
