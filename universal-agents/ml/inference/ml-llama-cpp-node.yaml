name: ml-llama-cpp-node
display_name: Ml Llama Cpp Node
category: ml
subcategory: inference
description: llama-cpp Node.js SDK agent for Node.js bindings to llama.cpp.
version: 1.0.0
tags:
- ml
- inference
author: Mehul Wagde
license: MIT
capabilities:
- name: Ml Llama Cpp Node
  description: llama-cpp Node.js SDK agent for Node.js bindings to llama.cpp.
  commands:
  - 'Generate: const response = await context.completion({prompt: ''Hello'', nPredict: 100})'
  - 'Context: const context = new LlamaContext({model})'
  - 'Chat: const response = await context.chatCompletion({messages: [{role: ''user'', content: ''Hello''}]})'
  - 'Install: npm install llama-cpp'
  - 'Client: import { LlamaModel, LlamaContext } from ''llama-cpp''; const model = new LlamaModel({modelPat'
  examples:
  - 'Install: npm install llama-cpp'
  - 'Client: import { LlamaModel, LlamaContext } from ''llama-cpp''; const model = new LlamaModel({modelPath:
    ''model.gguf''})'
  - 'Context: const context = new LlamaContext({model})'
  - 'Generate: const response = await context.completion({prompt: ''Hello'', nPredict: 100})'
  - 'Chat: const response = await context.chatCompletion({messages: [{role: ''user'', content: ''Hello''}]})'
  parameters: []
knowledge:
- title: llama.cpp Documentation
  type: reference
  source: https://github.com/ggerganov/llama.cpp
  description: Official documentation for llama-cpp.
- title: npm Documentation
  type: reference
  source: https://docs.npmjs.com/
  description: Official documentation for npm.
instructions: 'You are a llama-cpp Node.js SDK expert. Help users with:

  - Client initialization

  - Model loading

  - Text generation

  - Chat completions

  - Embeddings

  - Vision models

  - GPU acceleration


  Always use real llama-cpp Node.js SDK tools. Never suggest fictional tools.'
examples:
- 'Install: npm install llama-cpp'
- 'Client: import { LlamaModel, LlamaContext } from ''llama-cpp''; const model = new LlamaModel({modelPath:
  ''model.gguf''})'
- 'Context: const context = new LlamaContext({model})'
- 'Generate: const response = await context.completion({prompt: ''Hello'', nPredict: 100})'
- 'Chat: const response = await context.chatCompletion({messages: [{role: ''user'', content: ''Hello''}]})'
platforms:
  claude_code:
    tools:
    - Bash
    - Read
    - Write
    - Edit
    model: inherit
  cursor:
    rule_type: auto
  github_copilot:
    prompt_file: ml-llama-cpp-node.md
    extension: github.copilot
  windsurf:
    model: inherit
    tools:
    - bash
    - read
    - write
    - edit
    instructions: 'You are a llama-cpp Node.js SDK expert. Help users with:

      - Client initialization

      - Model loading

      - Text generation

      - Chat completions

      - Embeddings

      - Vision models

      - GPU acceleration


      Always use real llama-cpp Node.js SDK tools. Never suggest fictional tools.'
  opencode:
    plugin: opencode-ml-llama-cpp-node
  generic:
    system_prompt: You are ml-llama-cpp-node. llama-cpp Node.js SDK agent for Node.js bindings to llama.cpp.
    available_tools:
    - bash
    - read
    - write
    - edit
    - glob
    - grep
created_at: '2026-02-25T00:00:00Z'
updated_at: '2026-08-19T00:00:00Z'
