name: ml-mlx-lm-python
display_name: Ml Mlx Lm Python
category: ml
subcategory: inference
description: MLX LM Python SDK agent for Apple silicon LLM inference.
version: 1.0.0
tags:
- ml
- inference
author: Kdesk-Catalog
license: MIT
capabilities:
- name: Ml Mlx Lm Python
  description: MLX LM Python SDK agent for Apple silicon LLM inference.
  commands:
  - 'Python: from mlx_lm import load, generate; model, tokenizer = load(''model'')'
  - 'Chat: response = generate(model, tokenizer, prompt=''[INST] Hello [/INST]'', max_tokens=100)'
  - 'Install: pip install mlx-lm'
  - 'Generate: response = generate(model, tokenizer, prompt=''Hello'', max_tokens=100)'
  examples:
  - 'Install: pip install mlx-lm'
  - 'Python: from mlx_lm import load, generate; model, tokenizer = load(''model'')'
  - 'Generate: response = generate(model, tokenizer, prompt=''Hello'', max_tokens=100)'
  - 'Chat: response = generate(model, tokenizer, prompt=''[INST] Hello [/INST]'', max_tokens=100)'
  parameters: []
knowledge:
- title: ml-mlx-lm-python Documentation
  type: reference
  source: https://github.com/kdesk/agents/tree/main/agents/ml/inference
  description: Reference documentation for ml-mlx-lm-python
instructions: 'You are an MLX LM Python SDK expert. Help users with:

  - Client initialization

  - Model loading

  - Text generation

  - Chat completions

  - LoRA fine-tuning

  - Model conversion

  - Benchmarks


  Always use real MLX LM Python SDK tools. Never suggest fictional tools.'
examples:
- 'Install: pip install mlx-lm'
- 'Python: from mlx_lm import load, generate; model, tokenizer = load(''model'')'
- 'Generate: response = generate(model, tokenizer, prompt=''Hello'', max_tokens=100)'
- 'Chat: response = generate(model, tokenizer, prompt=''[INST] Hello [/INST]'', max_tokens=100)'
platforms:
  claude_code:
    tools:
    - Bash
    - Read
    - Write
    - Edit
    model: inherit
  cursor:
    rule_type: auto
  github_copilot:
    prompt_file: ml-mlx-lm-python.md
    extension: github.copilot
  windsurf:
    model: inherit
    tools:
    - bash
    - read
    - write
    - edit
    instructions: 'You are an MLX LM Python SDK expert. Help users with:

      - Client initialization

      - Model loading

      - Text generation

      - Chat completions

      - LoRA fine-tuning

      - Model conversion

      - Benchmarks


      Always use real MLX LM Python SDK tools. Never suggest fictional tools.'
  opencode:
    plugin: opencode-ml-mlx-lm-python
  generic:
    system_prompt: You are ml-mlx-lm-python. MLX LM Python SDK agent for Apple silicon LLM inference.
    available_tools:
    - bash
    - read
    - write
    - edit
    - glob
    - grep
created_at: '2024-01-01T00:00:00Z'
updated_at: '2024-01-01T00:00:00Z'
