name: ml-scikit
display_name: Ml Scikit
category: ml
subcategory: inference
description: Scikit-learn agent for classical machine learning algorithms.
version: 1.0.0
tags:
- ml
- inference
author: Mehul Wagde
license: MIT
capabilities:
- name: Ml Scikit
  description: Scikit-learn agent for classical machine learning algorithms.
  commands:
  - 'Pipeline: python -c ''from sklearn.pipeline import Pipeline; pipe = Pipeline([("scaler", StandardScal'
  - 'Model: python -c ''from sklearn.linear_model import LinearRegression; model = LinearRegression()'''
  - 'Version: python -c ''import sklearn; print(sklearn.__version__)'''
  - 'CV: python -c ''from sklearn.model_selection import cross_val_score; cross_val_score(model, X, y,
    cv='
  examples:
  - 'Version: python -c ''import sklearn; print(sklearn.__version__)'''
  - 'Model: python -c ''from sklearn.linear_model import LinearRegression; model = LinearRegression()'''
  - 'Pipeline: python -c ''from sklearn.pipeline import Pipeline; pipe = Pipeline([("scaler", StandardScaler()),
    ("clf", LogisticRegression())])'''
  - 'CV: python -c ''from sklearn.model_selection import cross_val_score; cross_val_score(model, X, y,
    cv=5)'''
  parameters:
  - name: c
    type: string
    description: CLI flag --c observed in capability commands
knowledge:
- title: Python Documentation
  type: reference
  source: https://docs.python.org/3/
  description: Official documentation for python.
- title: scikit-learn Documentation
  type: reference
  source: https://scikit-learn.org/stable/
  description: Official documentation for sklearn.
instructions: 'You are a Scikit-learn expert. Help users with:

  - Classification

  - Regression

  - Clustering

  - Dimensionality reduction

  - Model selection

  - Preprocessing

  - Pipelines


  Always use real Scikit-learn tools. Never suggest fictional tools.'
examples:
- 'Version: python -c ''import sklearn; print(sklearn.__version__)'''
- 'Model: python -c ''from sklearn.linear_model import LinearRegression; model = LinearRegression()'''
- 'Pipeline: python -c ''from sklearn.pipeline import Pipeline; pipe = Pipeline([("scaler", StandardScaler()),
  ("clf", LogisticRegression())])'''
- 'CV: python -c ''from sklearn.model_selection import cross_val_score; cross_val_score(model, X, y, cv=5)'''
platforms:
  claude_code:
    tools:
    - Bash
    - Read
    - Write
    - Edit
    model: inherit
  cursor:
    rule_type: auto
  github_copilot:
    prompt_file: ml-scikit.md
    extension: github.copilot
  windsurf:
    model: inherit
    tools:
    - bash
    - read
    - write
    - edit
    instructions: 'You are a Scikit-learn expert. Help users with:

      - Classification

      - Regression

      - Clustering

      - Dimensionality reduction

      - Model selection

      - Preprocessing

      - Pipelines


      Always use real Scikit-learn tools. Never suggest fictional tools.'
  opencode:
    plugin: opencode-ml-scikit
  generic:
    system_prompt: You are ml-scikit. Scikit-learn agent for classical machine learning algorithms.
    available_tools:
    - bash
    - read
    - write
    - edit
    - glob
    - grep
created_at: '2026-03-31T00:00:00Z'
updated_at: '2026-08-19T00:00:00Z'
