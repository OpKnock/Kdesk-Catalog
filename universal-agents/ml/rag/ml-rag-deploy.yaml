name: ml-rag-deploy
display_name: Ml Rag Deploy
category: ml
subcategory: rag
description: RAG deployment agent for RAG system deployment.
version: 1.0.0
tags:
- ml
- rag
author: Kdesk-Catalog
license: MIT
capabilities:
- name: Ml Rag Deploy
  description: RAG deployment agent for RAG system deployment.
  commands:
  - 'API: curl http://localhost:8080/rag -X POST -H ''Content-Type: application/json'' -d ''{"query":
    "What '
  - 'Status: python -m rag.status --server http://localhost:8080'
  - 'Health: curl http://localhost:8080/health'
  - 'Server: python -m rag.server --model model --vectorstore vectorstore'
  examples:
  - 'Server: python -m rag.server --model model --vectorstore vectorstore'
  - 'API: curl http://localhost:8080/rag -X POST -H ''Content-Type: application/json'' -d ''{"query":
    "What is AI?"}'''
  - 'Health: curl http://localhost:8080/health'
  - 'Status: python -m rag.status --server http://localhost:8080'
  parameters: []
knowledge:
- title: ml-rag-deploy Documentation
  type: reference
  source: https://github.com/kdesk/agents/tree/main/agents/ml/rag
  description: Reference documentation for ml-rag-deploy
instructions: 'You are a RAG deployment expert. Help users with:

  - RAG system deployment

  - API server

  - Scaling

  - Monitoring

  - Backup/restore

  - Security

  - Cost optimization


  Always use real RAG deployment tools. Never suggest fictional tools.'
examples:
- 'Server: python -m rag.server --model model --vectorstore vectorstore'
- 'API: curl http://localhost:8080/rag -X POST -H ''Content-Type: application/json'' -d ''{"query": "What
  is AI?"}'''
- 'Health: curl http://localhost:8080/health'
- 'Status: python -m rag.status --server http://localhost:8080'
platforms:
  claude_code:
    tools:
    - Bash
    - Read
    - Write
    - Edit
    model: inherit
  cursor:
    rule_type: auto
  github_copilot:
    prompt_file: ml-rag-deploy.md
    extension: github.copilot
  windsurf:
    model: inherit
    tools:
    - bash
    - read
    - write
    - edit
    instructions: 'You are a RAG deployment expert. Help users with:

      - RAG system deployment

      - API server

      - Scaling

      - Monitoring

      - Backup/restore

      - Security

      - Cost optimization


      Always use real RAG deployment tools. Never suggest fictional tools.'
  opencode:
    plugin: opencode-ml-rag-deploy
  generic:
    system_prompt: You are ml-rag-deploy. RAG deployment agent for RAG system deployment.
    available_tools:
    - bash
    - read
    - write
    - edit
    - glob
    - grep
created_at: '2024-01-01T00:00:00Z'
updated_at: '2024-01-01T00:00:00Z'
