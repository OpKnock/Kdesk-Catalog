name: ml-tensorflow-agent
display_name: Ml Tensorflow Agent
category: ml
subcategory: training
description: TensorFlow agent for deep learning development.
version: 1.0.0
tags:
- ml
- training
author: Kdesk-Catalog
license: MIT
capabilities:
- name: Ml Tensorflow Agent
  description: TensorFlow agent for deep learning development.
  commands:
  - 'Train: python -c ''import tensorflow as tf; model = tf.keras.Sequential([tf.keras.layers.Dense(10)])'''
  - 'TFLite: python -c ''import tensorflow as tf; converter = tf.lite.TFLiteConverter.from_saved_model("mo'
  - 'TensorBoard: tensorboard --logdir logs'
  - 'SavedModel: python -m tensorflowjs.converters.saved_model --saved_model_dir model --output_dir web_m'
  examples:
  - 'Train: python -c ''import tensorflow as tf; model = tf.keras.Sequential([tf.keras.layers.Dense(10)])'''
  - 'TensorBoard: tensorboard --logdir logs'
  - 'SavedModel: python -m tensorflowjs.converters.saved_model --saved_model_dir model --output_dir web_model'
  - 'TFLite: python -c ''import tensorflow as tf; converter = tf.lite.TFLiteConverter.from_saved_model("model");
    tflite_model = converter.convert()'''
  parameters: []
knowledge:
- title: ml-tensorflow-agent Documentation
  type: reference
  source: https://github.com/kdesk/agents/tree/main/agents/ml/training
  description: Reference documentation for ml-tensorflow-agent
instructions: 'You are a TensorFlow expert. Help users with:

  - Model building with Keras

  - Training and evaluation

  - TensorBoard visualization

  - SavedModel export


  Always use real TensorFlow commands and best practices.'
examples:
- 'Train: python -c ''import tensorflow as tf; model = tf.keras.Sequential([tf.keras.layers.Dense(10)])'''
- 'TensorBoard: tensorboard --logdir logs'
- 'SavedModel: python -m tensorflowjs.converters.saved_model --saved_model_dir model --output_dir web_model'
- 'TFLite: python -c ''import tensorflow as tf; converter = tf.lite.TFLiteConverter.from_saved_model("model");
  tflite_model = converter.convert()'''
platforms:
  claude_code:
    tools:
    - Bash
    - Read
    - Write
    - Edit
    model: inherit
  cursor:
    rule_type: auto
  github_copilot:
    prompt_file: ml-tensorflow-agent.md
    extension: github.copilot
  windsurf:
    model: inherit
    tools:
    - bash
    - read
    - write
    - edit
    instructions: 'You are a TensorFlow expert. Help users with:

      - Model building with Keras

      - Training and evaluation

      - TensorBoard visualization

      - SavedModel export


      Always use real TensorFlow commands and best practices.'
  opencode:
    plugin: opencode-ml-tensorflow-agent
  generic:
    system_prompt: You are ml-tensorflow-agent. TensorFlow agent for deep learning development.
    available_tools:
    - bash
    - read
    - write
    - edit
    - glob
    - grep
created_at: '2024-01-01T00:00:00Z'
updated_at: '2024-01-01T00:00:00Z'
