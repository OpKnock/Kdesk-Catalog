name: ml-tensorflow
display_name: Ml Tensorflow
category: ml
subcategory: training
description: TensorFlow agent for machine learning and deep learning.
version: 1.0.0
tags:
- ml
- training
author: Kdesk-Catalog
license: MIT
capabilities:
- name: Ml Tensorflow
  description: TensorFlow agent for machine learning and deep learning.
  commands:
  - 'GPU: python -c ''import tensorflow as tf; print(tf.config.list_physical_devices("GPU"))'''
  - 'Version: python -c ''import tensorflow as tf; print(tf.__version__)'''
  - 'Convert: tensorflowjs_converter --input_format=tf_saved_model saved_model model'
  - 'Model: python -c ''import tensorflow as tf; model = tf.keras.Sequential([tf.keras.layers.Dense(10)])'''
  examples:
  - 'Version: python -c ''import tensorflow as tf; print(tf.__version__)'''
  - 'GPU: python -c ''import tensorflow as tf; print(tf.config.list_physical_devices("GPU"))'''
  - 'Model: python -c ''import tensorflow as tf; model = tf.keras.Sequential([tf.keras.layers.Dense(10)])'''
  - 'Convert: tensorflowjs_converter --input_format=tf_saved_model saved_model model'
  parameters: []
knowledge:
- title: ml-tensorflow Documentation
  type: reference
  source: https://github.com/kdesk/agents/tree/main/agents/ml/training
  description: Reference documentation for ml-tensorflow
instructions: 'You are a TensorFlow expert. Help users with:

  - Keras models

  - tf.data pipelines

  - TensorBoard

  - TF Lite

  - TF.js

  - Distribution strategies

  - SavedModel


  Always use real TensorFlow tools. Never suggest fictional tools.'
examples:
- 'Version: python -c ''import tensorflow as tf; print(tf.__version__)'''
- 'GPU: python -c ''import tensorflow as tf; print(tf.config.list_physical_devices("GPU"))'''
- 'Model: python -c ''import tensorflow as tf; model = tf.keras.Sequential([tf.keras.layers.Dense(10)])'''
- 'Convert: tensorflowjs_converter --input_format=tf_saved_model saved_model model'
platforms:
  claude_code:
    tools:
    - Bash
    - Read
    - Write
    - Edit
    model: inherit
  cursor:
    rule_type: auto
  github_copilot:
    prompt_file: ml-tensorflow.md
    extension: github.copilot
  windsurf:
    model: inherit
    tools:
    - bash
    - read
    - write
    - edit
    instructions: 'You are a TensorFlow expert. Help users with:

      - Keras models

      - tf.data pipelines

      - TensorBoard

      - TF Lite

      - TF.js

      - Distribution strategies

      - SavedModel


      Always use real TensorFlow tools. Never suggest fictional tools.'
  opencode:
    plugin: opencode-ml-tensorflow
  generic:
    system_prompt: You are ml-tensorflow. TensorFlow agent for machine learning and deep learning.
    available_tools:
    - bash
    - read
    - write
    - edit
    - glob
    - grep
created_at: '2024-01-01T00:00:00Z'
updated_at: '2024-01-01T00:00:00Z'
