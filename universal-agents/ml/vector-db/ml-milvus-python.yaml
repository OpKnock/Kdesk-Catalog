name: ml-milvus-python
display_name: Ml Milvus Python
category: ml
subcategory: vector-db
description: Milvus Python SDK agent for vector database operations.
version: 1.0.0
tags:
- ml
- vector-db
author: Mehul Wagde
license: MIT
capabilities:
- name: Ml Milvus Python
  description: Milvus Python SDK agent for vector database operations.
  commands:
  - 'Client: from pymilvus import connections, Collection; connections.connect(''default'', host=''localhost'
  - 'Insert: collection.insert([ids, embeddings, metadata])'
  - 'Install: pip install pymilvus'
  - 'Search: collection.search(query_embeddings, anns_field=''embedding'', param={''metric_type'': ''L2'',
    ''par'
  - 'Collection: Collection(''my_collection'')'
  examples:
  - 'Install: pip install pymilvus'
  - 'Client: from pymilvus import connections, Collection; connections.connect(''default'', host=''localhost'',
    port=''19530'')'
  - 'Collection: Collection(''my_collection'')'
  - 'Insert: collection.insert([ids, embeddings, metadata])'
  - 'Search: collection.search(query_embeddings, anns_field=''embedding'', param={''metric_type'': ''L2'',
    ''params'': {''nprobe'': 10}}, limit=10)'
  parameters: []
knowledge:
- title: Milvus Documentation
  type: reference
  source: https://milvus.io/docs/
  description: Official documentation for milvus.
instructions: 'You are a Milvus Python SDK expert. Help users with:

  - Client initialization

  - Collection management

  - Vector operations

  - Index creation

  - Search

  - Insert

  - Delete


  Always use real Milvus Python SDK tools. Never suggest fictional tools.'
examples:
- 'Install: pip install pymilvus'
- 'Client: from pymilvus import connections, Collection; connections.connect(''default'', host=''localhost'',
  port=''19530'')'
- 'Collection: Collection(''my_collection'')'
- 'Insert: collection.insert([ids, embeddings, metadata])'
- 'Search: collection.search(query_embeddings, anns_field=''embedding'', param={''metric_type'': ''L2'',
  ''params'': {''nprobe'': 10}}, limit=10)'
platforms:
  claude_code:
    tools:
    - Bash
    - Read
    - Write
    - Edit
    model: inherit
  cursor:
    rule_type: auto
  github_copilot:
    prompt_file: ml-milvus-python.md
    extension: github.copilot
  windsurf:
    model: inherit
    tools:
    - bash
    - read
    - write
    - edit
    instructions: 'You are a Milvus Python SDK expert. Help users with:

      - Client initialization

      - Collection management

      - Vector operations

      - Index creation

      - Search

      - Insert

      - Delete


      Always use real Milvus Python SDK tools. Never suggest fictional tools.'
  opencode:
    plugin: opencode-ml-milvus-python
  generic:
    system_prompt: You are ml-milvus-python. Milvus Python SDK agent for vector database operations.
    available_tools:
    - bash
    - read
    - write
    - edit
    - glob
    - grep
created_at: '2026-04-20T00:00:00Z'
updated_at: '2026-08-19T00:00:00Z'
