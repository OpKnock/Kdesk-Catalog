name: command
display_name: Command
category: patterns
subcategory: general
description: 'Implements the Command pattern in Java with Gradle: encapsulating requests as objects with
  undo/redo support.'
version: 2.0.0
tags:
- command
author: Kdesk-Catalog
license: MIT
prerequisites:
- ./gradlew
- gradle
capabilities:
- name: java-gradle
  description: Implement and test Command pattern examples.
  commands:
  - gradle init --type java-library --dsl groovy --test-framework junit-jupiter
  - gradle wrapper
  - ./gradlew build
  - ./gradlew test --tests 'com.example.command.*'
  - ./gradlew run
  examples:
  - gradle init --type java-application --test-framework junit-jupiter
  - ./gradlew test --tests 'com.example.command.HistoryTest' --info
  - ./gradlew build -x test
  parameters:
  - name: test-framework
    type: string
    description: junit-jupiter, spock, or testng
  - name: tests
    type: string
    description: Test class filter
  - name: dsl
    type: string
    description: groovy or kotlin build DSL
knowledge:
- title: 'Refactoring Guru: Command'
  type: documentation
  source: https://refactoring.guru/design-patterns/command
  description: Command intent, structure, and usage
- title: Gradle Docs
  type: documentation
  source: https://docs.gradle.org/current/userguide/java_library_plugin.html
  description: Java builds and test tasks
instructions: "# Command Pattern\n\nTurn requests into objects: undoable, queueable, loggable.\n\n## When\
  \ to Use\n\n- Undo/redo in editors\n- Queuing or logging operations\n- Macro composition of actions\n\
  \n## Example (Java)\n\n```java\npublic interface Command {\n    void execute();\n    void undo();\n\
  }\n\npublic class AddTextCommand implements Command {\n    private final StringBuilder doc;\n    private\
  \ final String text;\n\n    public AddTextCommand(StringBuilder doc, String text) {\n        this.doc\
  \ = doc;\n        this.text = text;\n    }\n\n    public void execute() { doc.append(text); }\n    public\
  \ void undo() {\n        int start = doc.length() - text.length();\n        doc.delete(start, doc.length());\n\
  \    }\n}\n\npublic class History {\n    private final Deque<Command> undoStack = new ArrayDeque<>();\n\
  \n    public void push(Command c) { c.execute(); undoStack.push(c); }\n    public void undo() { undoStack.pop().undo();\
  \ }\n}\n```\n\n## Test\n\n```java\n@Test\nvoid addAndUndo() {\n    var doc = new StringBuilder();\n\
  \    var h = new History();\n    h.push(new AddTextCommand(doc, \"hello\"));\n    h.push(new AddTextCommand(doc,\
  \ \" world\"));\n    assertThat(doc.toString()).isEqualTo(\"hello world\");\n    h.undo();\n    assertThat(doc.toString()).isEqualTo(\"\
  hello\");\n}\n```\n\n```bash\n./gradlew test --tests 'com.example.command.HistoryTest'\n```\n\n## Best\
  \ practices\n\n- Commands are pure intent; state lives in receivers.\n- Make commands serializable for\
  \ queues and journals.\n- Implement undo as inverse ops, not snapshots, for large state.\n- Test execute/undo\
  \ pairs for every command.\n\n## Testing\n\nVerify execute-then-undo returns exact prior state."
examples:
- input: Add undo support to the document editor.
  output: Introduces AddTextCommand and History, wires UI actions to push(), and tests execute/undo round-trips.
- input: Queue editor actions for offline replay.
  output: Makes commands serializable and replays them against a log on reconnect.
platforms:
  claude_code:
    tools:
    - Bash
    - Read
    - Write
    - Edit
    model: inherit
  cursor:
    rule_type: auto
  github_copilot:
    prompt_file: command.md
    extension: github.copilot
  windsurf:
    model: inherit
    tools:
    - bash
    - read
    - write
    - edit
  opencode:
    plugin: opencode-command
  generic:
    system_prompt: 'You take the Command seat. You do it through java-gradle: Implement and test Command
      pattern examples. The commands are real, e.g. `gradle init --type java-library --dsl groovy --test-framework
      junit-jupiter`. You run and verify the real CLI commands before you claim anything.'
    available_tools:
    - bash
    - read
    - write
    - edit
created_at: '2024-01-01T00:00:00Z'
updated_at: '2024-01-01T00:00:00Z'
